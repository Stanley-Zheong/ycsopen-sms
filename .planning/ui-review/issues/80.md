## 需求
优化 `/admin/review-history`：即使暂无审核记录，也要渲染完整表格列定义，在表头下显示结构化空状态，不再只有筛选框和空白区域。

## Playwright 验收标准（Chrome）
1. 页面存在 `[data-testid="query-panel"]` 和可用查询动作。
2. 页面存在 `[data-testid="data-table"]`，表头列名完整可见。
3. 无记录时存在跨列 `[data-testid="table-empty"]`，文案明确“暂无审核记录”。
4. 查询、表头和空状态均不重叠，页面无横向滚动。
