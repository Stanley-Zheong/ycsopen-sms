# Issue 71 Query-Panel Test Matrix

| Obligation ID | Requirement IDs | Behavior ID | Catalog test/layer | Playwright ID | Page ID/route | data-testid | Case ID | Case | Command | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| OBL-ISSUE-71-QUERY-ACTION-POSITION | PROJECT-UI-CONTRACT | issue-71-query-action-position | T-ISSUE-71-QUERY-ACTION-POSITION:playwright | pw-issue-71-query-action-position | shared-query-panel, represented by admin-tenants `/admin/tenants` | query-panel,query-panel-fields,query-panel-toggle,query-input-keyword,query-input-operating-status,query-submit,query-reset | C-ISSUE-71-QUERY-ACTION-POSITION | Render the tenant query panel at 1440 px and prove the action group shares the visible field row and ends at the right edge. At 1201 px and 901 px, prove the configured field columns do not overlap the action group and the group moves to a separate right-aligned row. At 800 px, expand the wrapped field grid and prove the action group aligns with the final field row in the right-side region. At 600 px, prove the action group moves below the final field while remaining right-aligned. | From `web/`: `LD_LIBRARY_PATH=<chromium-libraries> YCSOPEN_CHROME_PATH=<chromium-executable> YCSOPEN_WEB_PORT=<unused-port> YCSOPEN_E2E_ISOLATED=1 ./node_modules/.bin/playwright test test/scripts/query-panel.spec.ts --config playwright.config.ts --project=local-google-chrome --workers=1 --grep 'pw-issue-(58|64|71)-' --reporter=json` | Executed Chromium evidence, source checksums, command, and the seven related query-panel cases are recorded in `EVIDENCE/playwright-query-panel-report.json`. The page APIs are intercepted, so this evidence proves rendered layout and query-panel interaction only. |
