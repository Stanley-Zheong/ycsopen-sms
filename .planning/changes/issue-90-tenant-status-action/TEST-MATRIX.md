# Issue 90 Release Tenant Status Action Test Matrix

| Obligation ID | Requirement IDs | Behavior ID | Catalog test/layer | Playwright ID | Page ID/route | data-testid | Case ID | Case | Command | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| OBL-ISSUE-90-RELEASE-TENANT-ACCOUNT | GITHUB-ISSUE-90,REQ-F-2-4 | issue-90-release-tenant-account | T-ISSUE-90-RELEASE-TENANT-ACCOUNT:migration+release-playwright | pw-issue-90-release-tenant-status-action | admin-tenants `/admin/tenants` | admin-tenant-qualification-tenants-status-action | C-ISSUE-90-RELEASE-TENANT-ACCOUNT | Execute the release seed on a fresh schema and assert exactly one normal version-zero account for `DEV-TENANT`; change its account values, execute the seed again, and assert the single operator-managed row is unchanged. In the Docker release, query the MySQL account row and verify an authorized admin sees the existing account-status action on the `DEV-TENANT` table row. | `mvn -f core/pom.xml -Dtest=ReleaseAcceptanceSeedMigrationTest test`; `mvn -f core/pom.xml test`; `npm --prefix web test`; `./scripts/verify-docker-release` | Local results and environment boundaries are recorded in `VERIFICATION.md`. The pull request's `Docker release / Google Chrome` check provides the MySQL and browser result; Phase 08 retains ownership of the unchanged status-action implementation and selector. |
