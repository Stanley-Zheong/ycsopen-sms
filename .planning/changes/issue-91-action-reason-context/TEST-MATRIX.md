# Issue 91 Action Reason Test Matrix

| Obligation ID | Requirement IDs | Behavior ID | Catalog test/layer | Playwright ID | Page ID/route | data-testid | Case ID | Case | Command | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| OBL-ISSUE-91-ACTION-REASON | PROJECT-UI-CONTRACT,GitHub-91 | issue-91-action-reason-context | T-ISSUE-91-ACTION-REASON:playwright | pw-issue-91-action-reason-context | `/admin/tenant-recharge-review`, `/admin/uplink`, `/admin/submission/details`, `/admin/send/details`, `/admin/receipt/details`, `/admin/error/details`, `/admin/alerts`, `/admin/push/failures`, `/admin/send/jobs` | modal plus each page-owned action dialog, target, consequence, reason, cancel, confirm, and existing action-trigger id | C-ISSUE-91-ACTION-REASON-CONTEXT | Isolated interaction coverage traverses every affected route; proves no detached reason field before selection, truthful action/target/effect context, blank blocking, cancellation, exact payloads, duplicate-activation suppression, same-ID and same-reason retry after failure, retry-reason immutability, and pending focus/dismissal locking. Shared-component and recharge-owner units additionally prove that immediate Escape/cancel in the synchronous pre-pending window cannot close the submitted dialog. Error details prove selected-group matching plus empty, truncated-target, and unmappable-`UNKNOWN` groups failing closed; UI unit coverage also proves loading, query-error, and over-limit behavior, while the backend service test proves same-code mappings do not multiply the distinct task aggregate and conflicts collapse conservatively. Installed-Google-Chrome Docker coverage loads all routes against real services without detached reason inputs, then approves a test-owned recharge and reads back its persisted reason and balance audit. | From `web/`: `npm run test:e2e -- issue-91-action-reason-context.spec.ts --project=local-google-chrome --grep pw-issue-91-action-reason-context`; `npm test -- test/unit/action-reason-dialog.test.tsx test/unit/tenant-recharge.test.tsx test/unit/message-operations.test.tsx`. From the repository root: `mvn -f core/pom.xml -Dtest=MessageReceiptErrorOperationsServiceTest test`. CI `Docker release / Google Chrome` runs `npm --prefix web run test:docker-release` against real Web/Core services. | Isolated report in `EVIDENCE/playwright-action-reason-report.json`; real-service installed-Chrome run and commit identity in the pull-request Docker CI log/artifact; complete results in `VERIFICATION.md`. |
