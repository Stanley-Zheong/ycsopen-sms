# Issue 91 Action Reason Test Matrix

| Obligation ID | Requirement IDs | Behavior ID | Catalog test/layer | Playwright ID | Page ID/route | data-testid | Case ID | Case | Command | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| OBL-ISSUE-91-ACTION-REASON | PROJECT-UI-CONTRACT,GitHub-91 | issue-91-action-reason-context | T-ISSUE-91-ACTION-REASON:playwright | pw-issue-91-action-reason-context | `/admin/tenant-recharge-review`, `/admin/uplink`, `/admin/submission/details`, `/admin/send/details`, `/admin/receipt/details`, `/admin/error/details`, `/admin/alerts`, `/admin/push/failures`, `/admin/send/jobs` | modal plus each page-owned action dialog, target, consequence, reason, cancel, confirm, and existing action-trigger id | C-ISSUE-91-ACTION-REASON-CONTEXT | Isolated interaction coverage traverses every affected route; proves no detached reason field before selection, truthful action/target/effect context, blank blocking, cancellation, exact payloads, duplicate-activation suppression, same-ID and same-reason retry after failure, retry-reason immutability, and pending focus/dismissal locking. Shared-component and recharge-owner units additionally prove that immediate Escape/cancel in the synchronous pre-pending window cannot close the submitted dialog. Error details prove selected-group matching plus empty and truncated targets failing closed, a null-derived `UNKNOWN（错误码为空）` group remaining disabled, and a distinct literal `UNKNOWN` group submitting the literal value and only its matching failed messages. UI unit coverage also proves loading, query-error, over-limit, and message/status-filter alignment, while the backend service test proves the same filter alignment, null/literal separation, same-code mappings not multiplying the distinct task aggregate, and conflicts collapsing conservatively. | From `web/`: `npm run test:e2e -- issue-91-action-reason-context.spec.ts --project=local-google-chrome --grep pw-issue-91-action-reason-context`; `npm test -- test/unit/action-reason-dialog.test.tsx test/unit/tenant-recharge.test.tsx test/unit/message-operations.test.tsx`. From the repository root: `mvn -f core/pom.xml -Dtest=MessageReceiptErrorOperationsServiceTest test`. | Isolated report in `EVIDENCE/playwright-action-reason-report.json`; complete results in `VERIFICATION.md`. |
| OBL-ISSUE-91-REAL-SERVICE | PROJECT-UI-CONTRACT,GitHub-91 | issue-91-action-reason-real-service | T-ISSUE-91-REAL-SERVICE:docker-playwright | pw-issue-91-docker-release | `/admin/tenant-recharge-review`, `/admin/uplink`, `/admin/submission/details`, `/admin/send/details`, `/admin/receipt/details`, `/admin/error/details`, `/admin/alerts`, `/admin/push/failures`, `/admin/send/jobs` | each page root and contextual reason id; recharge row, action dialog, target, consequence, reason, confirm, result message | C-ISSUE-91-REAL-SERVICE | Installed Google Chrome loads every affected route against packaged Web/Core services and proves each route starts without a detached reason input. It then approves one test-owned recharge, proves double activation creates one review request, and reads back the exact persisted reason plus one matching balance audit. | CI `Docker release / Google Chrome` runs `npm --prefix web run test:docker-release` against fresh, upgrade, and restart environments. | Commit-bound installed-Chrome report hashes and run link are recorded in `VERIFICATION.md`; CI retains the JSON reports as artifacts. |
