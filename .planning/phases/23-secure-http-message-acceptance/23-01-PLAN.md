# Phase 23 Plan

## TODO set

- Implement complete HMAC request authentication over method, URI, query, canonical headers, and exact body.
- Read App Secret through the existing protected credential boundary.
- Enforce timestamp, nonce, and IP allow-list before business processing.
- Add `submitId` as the HTTP business idempotency key.
- Persist an idempotency submission row, message task, billing reserve, and send intent in one acceptance transaction.
- Return existing accepted result for duplicate same-payload `submitId`; reject conflicting payloads.
- Verify field validation, security rejection, idempotency, and no duplicate side effects with automated tests.
- Record executable evidence and keep TODO empty before commit.
