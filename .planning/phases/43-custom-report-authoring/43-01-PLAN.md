# Phase 43 Plan — Custom report authoring

## TODO

- [x] Identify owned obligations with `validate-prd-obligations`.
- [x] Add backend tests for unsupported dimensions/measures, aggregate-backed preview, tenant scope, saved snapshots, export request snapshots, controller API, and migration.
- [x] Implement `CustomReportService`, `CustomReportController`, and `V5200__custom_report_authoring.sql`.
- [x] Add frontend API client, route, sidebar entry, management page, unit test, and Chrome Playwright script.
- [x] Record UI inventory and test matrix with stable `data-testid` selectors.
- [x] Run full backend/frontend verification.
- [x] Run Claude review and fix findings.
- [x] Commit and push Phase43 branch.

## Implementation notes

- Reuse `statistics_metric_registry` and `statistics_aggregates`.
- Use whitelist validation for every dynamic dimension and measure before composing SQL.
- Use generated keys for inserted definitions/export requests.
- Keep export as a request record only; file generation belongs to a later export phase.
