# Phase 47 PLAN

1. Add retention/archive schema for policies, manifests, restore jobs and permissions.
2. Implement a service boundary for policy save/list, whitelisted hot-data scan, encrypted archive creation, checksum verify, restore and archive export.
3. Add a console controller for archive policy/manifest/verify/restore/export operations.
4. Add admin UI for policy, scan, manifest status, deletion eligibility, verify, restore and export.
5. Add backend migration/service tests and frontend unit/Chrome Playwright tests.
6. Generate Phase 47 process/UI/evidence docs and run validators.
7. Review BLOCKER/HIGH findings, fix scoped issues, then commit/push/PR.

Verification commands:

- `mvn -q -f core/pom.xml -Dtest=RetentionArchiveServiceTest,RetentionArchiveRestoreMigrationTest,SecureAsyncExportServiceTest test`
- `mvn -f core/pom.xml test`
- `npm --prefix web ci`
- `npm --prefix web test`
- `npm --prefix web run build`
- `npm --prefix web exec -- playwright test retention-archive.spec.ts --config web/playwright.config.ts --project=local-google-chrome`
- `/usr/bin/env ruby .planning/tools/validate-prd-obligations.rb --owner retention-archive-restore --assert-unique --assert-traced`
- `/usr/bin/env ruby .planning/tools/validate-ui-contract.rb --phase 47 --package retention-archive-restore --stage design`
- `/usr/bin/env ruby .planning/tools/validate-ui-contract.rb --phase 47 --package retention-archive-restore --stage production`
