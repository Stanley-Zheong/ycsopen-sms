# Phase 44 Plan — Operational dashboards

## Tasks

1. Define scoped backend tests for dashboard source aggregation, tenant isolation, resource statistics, API status, and role configuration.
2. Implement the smallest backend read service/controller and migration needed by those tests.
3. Define frontend unit and local Chrome Playwright coverage for the owned pages and elements.
4. Implement API client, routes, navigation, dashboard pages, and tenant overview/template statistics additions.
5. Record UI design/production contract artifacts and obligation evidence.
6. Run scoped and full verification.
7. Run Claude review, resolve actionable findings, then commit and push the phase branch.

## Checks before closing

- `mvn -f core/pom.xml test`
- `npm --prefix web ci`
- `npm --prefix web test`
- `npm --prefix web run build`
- `npm --prefix web exec -- playwright test operational-dashboards.spec.ts --config web/playwright.config.ts --project=local-google-chrome --reporter=json`
- `/usr/bin/env ruby .planning/tools/validate-prd-obligations.rb --owner operational-dashboards --assert-unique --assert-traced`
- `/usr/bin/env ruby .planning/tools/validate-ui-contract.rb --phase 44 --package operational-dashboards --stage design`
- `/usr/bin/env ruby .planning/tools/validate-ui-contract.rb --phase 44 --package operational-dashboards --stage production`
