# Phase 26 Intent

The tenant console send page is for authenticated users who need to send a test or small batch without using HMAC API credentials. The page should be safe by default: only approved resources appear, preview is canonical, and network retry cannot create a new business submission identity.
