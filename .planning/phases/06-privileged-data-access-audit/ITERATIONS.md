# Iterations

| Iteration | Trigger | Decision/change | Recheck |
| --- | --- | --- | --- |
| I-06-001 | Phase entry | Reduced the module to audit, reveal, and three event sources; excluded generic alert/export implementations | Entry review and design validator |
| I-06-002 | Pencil MCP transport unavailable and no local Pencil application found | Reuse approved visual baseline without editing it; record the boundary and use clickable HTML for Phase 06-specific states | Source checksum and UI contract |
| I-06-003 | Real MySQL rejected trigger creation for the unprivileged migration user | Preserve database append protection and add the single documented `log_bin_trust_function_creators` prerequisite to the existing local MySQL harness | Harness self-test plus Phase 05/06 MySQL integration |
| I-06-004 | Implementation reconciliation found mismatched audit failure filters and partial reveal visibility | Align filters to `CLIENT_FAILURE`/`SERVER_FAILURE`; require button, API, and full-account-scope permissions before rendering reveal | Frontend unit, lint, build, and Chrome acceptance |
| I-06-005 | Audit sanitizer originally retained arbitrary parameter names | Replace arbitrary keys with a fixed safe-name allowlist and collapse every other key to `other` | Interceptor test and complete backend suite |
| I-06-006 | Independent review found three HIGH integrity gaps | Add pre-execution STARTED capture, least-privilege runtime grants with a definer finalizer, and record-count-aware exact bulk-export comparison | Focused tests and real MySQL runtime-account proof |
| I-06-007 | Review found proxy attribution, invalid filters, result drift, and a public raw decrypt seam | Share a loopback-only trusted-proxy resolver, return 400 for invalid controlled filters, share a four-value event result set, and make raw phone decrypt package-private | Java unit/controller/integration tests |
| I-06-008 | Review found aggregate test IDs insufficient for generated automation | Expand the implementation, HTML prototype, selector catalog, and Playwright sources from 15 to 53 literal selectors | Production UI contract, frontend tests, and installed-Chrome acceptance |
| I-06-009 | Re-review found append-style XFF spoofing and controller-only 400 assertions | Require the same-host Nginx to overwrite XFF, reject proxy chains, preserve `ResponseStatusException` and type-mismatch 400 responses, and exercise them through MockMvc advice | Resolver and real HTTP controller tests |
| I-06-010 | Claude questioned whether fifth-failure effects survive the expected business rejection | Retain the existing `noRollbackFor = BusinessException.class` transaction boundary and add a real MySQL proof for LOCKED state, five history rows, and one repeated-failure event | Phase 05/06 MySQL integration 3/3 |
| I-06-011 | Claude found that the V1500 binlog prerequisite was not operationally fail-fast | Check the MySQL variables immediately before V1500 and in `init-db.sh`, document the mandatory DBA setting, and keep migration/runtime accounts free of `SUPER`; also make proxy IP parsing DNS-free | Callback/resolver unit tests, shell syntax, and real MySQL migration |
