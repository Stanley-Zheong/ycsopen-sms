---
phase: 10-channel-configuration-lifecycle
plan: "03"
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - core/src/main/java/com/ycsopen/sms/core/service/channel/ChannelDependencyInventoryService.java
  - core/src/main/java/com/ycsopen/sms/core/service/channel/ChannelRetirementService.java
  - core/src/main/java/com/ycsopen/sms/core/web/controller/ChannelRetirementController.java
  - core/src/main/java/com/ycsopen/sms/core/web/dto/ChannelDependency*.java
  - core/src/test/java/com/ycsopen/sms/core/service/channel/ChannelDependencyInventoryTest.java
  - core/src/test/java/com/ycsopen/sms/core/service/channel/ChannelDependencyMigrationTest.java
  - core/src/test/java/com/ycsopen/sms/core/service/channel/ChannelOfflineTransitionTest.java
autonomous: true
requirements: [REQ-F-4-4]
must_haves:
  truths:
    - "Retirement returns every declared route, pool, filing, price, and active-task dependency before mutation."
    - "Offline/delete is blocked until each dependency has an explicit destination or resolution, with no hidden first-match fallback."
    - "A resolved dependency set transitions the channel to OFFLINE idempotently and records actor/time evidence."
  artifacts:
    - path: "core/src/main/java/com/ycsopen/sms/core/service/channel/ChannelDependencyInventoryService.java"
      provides: "Explicit dependency-provider inventory and refresh"
      contains: "route"
    - path: "core/src/main/java/com/ycsopen/sms/core/service/channel/ChannelRetirementService.java"
      provides: "Dependency-gated migration and offline transition"
      contains: "OFFLINE"
  key_links:
    - from: "ChannelRetirementService"
      to: "ChannelDependencyInventoryService"
      via: "refresh inventory before every offline/delete mutation"
    - from: "ChannelDependencyInventoryService"
      to: "route_rules/channel_group_members/signature_channel_registrations/message_tasks"
      via: "channel predicate and typed dependency provider"
---

<objective>
Implement dependency-first channel retirement and offline transition.

Purpose: Close F-4.4 and the offline state obligation without importing routing,
pool, health, or durable task migration behavior.
Output: Provider inventory, explicit destination migration API, offline guard,
and integration/state tests.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/phases/10-channel-configuration-lifecycle/10-SPEC.md
@.planning/phases/10-channel-configuration-lifecycle/DESIGN.md
@.planning/phases/10-channel-configuration-lifecycle/10-CONTEXT.md
@core/src/main/resources/db/migration/V1__init_schema.sql
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify dependency and offline boundary tests</name>
  <files>core/src/test/java/com/ycsopen/sms/core/service/channel/ChannelDependencyInventoryTest.java, core/src/test/java/com/ycsopen/sms/core/service/channel/ChannelDependencyMigrationTest.java, core/src/test/java/com/ycsopen/sms/core/service/channel/ChannelOfflineTransitionTest.java</files>
  <behavior>
    - Test 1: inventory includes route, pool, filing, price, and active-task provider results, including empty provider results as explicit facts.
    - Test 2: unresolved or ambiguous destination blocks offline/delete and exposes safe dependency IDs without mutating the channel.
    - Test 3: every explicit migration is persisted/verified before OFFLINE; repeat offline is idempotent and actor/time audit is present.
  </behavior>
  <action>Write lower-layer tests with synthetic rows and dependency providers. Cover cross-owner references, missing destination, duplicate destination, active task, concurrent migration/offline, and terminal OFFLINE. Cite OBL-F-4-4-A, OBL-F-4-4-B, OBL-STATE-CHANNEL-OFFLINE, D-10-004. Do not assume a route/pool implementation that belongs to another phase.</action>
  <verify><automated>mvn -f core/pom.xml -Dtest=ChannelDependencyInventoryTest,ChannelDependencyMigrationTest,ChannelOfflineTransitionTest test</automated></verify>
  <done>Tests prove dependency completeness, explicit migration, no hidden fallback, and offline state guards before service implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement provider inventory and retirement gate</name>
  <files>core/src/main/java/com/ycsopen/sms/core/service/channel/ChannelDependencyInventoryService.java, core/src/main/java/com/ycsopen/sms/core/service/channel/ChannelRetirementService.java, core/src/main/java/com/ycsopen/sms/core/web/controller/ChannelRetirementController.java, core/src/main/java/com/ycsopen/sms/core/web/dto/ChannelDependency*.java</files>
  <behavior>
    - Test 1: GET dependencies invokes every declared provider and returns typed source, reference, state, and destination metadata.
    - Test 2: migration accepts explicit channel/group/route destination mapping, refreshes all providers, and rejects unresolved references.
    - Test 3: offline transition is transactionally guarded by an empty refreshed inventory and produces redacted audit.
  </behavior>
  <action>Implement one inventory service with explicit provider registrations for existing route, pool, filing, price, and message-task references. Use typed dependency IDs and require a destination/resolution per item; reject ambiguous or browser-selected foreign records. Re-read inventory under the offline transaction, set OFFLINE only after zero unresolved items, and keep repeated requests idempotent. Apply D-10-004; do not create a workflow engine, first-match behavior, health/pause recovery, route engine, or task lease migration.</action>
  <verify><automated>mvn -f core/pom.xml -Dtest=ChannelDependencyInventoryTest,ChannelDependencyMigrationTest,ChannelOfflineTransitionTest test</automated></verify>
  <done>Dependency preview/migration/offline APIs enforce explicit resolution and preserve safe tenant/platform authorization and audit.</done>
</task>

<task type="auto">
  <name>Task 3: Verify dependency persistence and state transition</name>
  <files>core/src/test/java/com/ycsopen/sms/core/service/channel/ChannelDependencyInventoryTest.java, core/src/test/java/com/ycsopen/sms/core/service/channel/ChannelDependencyMigrationTest.java, core/src/test/java/com/ycsopen/sms/core/service/channel/ChannelOfflineTransitionTest.java</files>
  <action>Run focused and full backend suites. Inspect every dependency provider for an explicit channel predicate, no first-match fallback, and no mutation before recheck. Record the migration/offline evidence and close OBL-F-4-4-A, OBL-F-4-4-B, and OBL-STATE-CHANNEL-OFFLINE only after executable results.</action>
  <verify><automated>mvn -f core/pom.xml -Dtest=ChannelDependencyInventoryTest,ChannelDependencyMigrationTest,ChannelOfflineTransitionTest test && mvn -f core/pom.xml test</automated></verify>
  <done>Dependency and offline obligations have passing integration/state evidence and full backend verification remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| operator -> retirement API | Dependency IDs and destination selections are untrusted input. |
| inventory provider -> shared reference tables | Cross-module references must be read with a channel predicate. |
| migration -> offline state | A destructive lifecycle transition crosses a transaction boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-03-01 | Tampering | destination mapping | mitigate | Require explicit typed destination and reject ambiguous/missing references. |
| T-10-03-02 | Information disclosure | dependency preview | mitigate | Return safe reference metadata only; omit credentials and arbitrary request JSON. |
| T-10-03-03 | Denial of service | offline race | mitigate | Refresh inventory under guarded transaction and block while any item remains. |
| T-10-03-SC | Tampering | package installs | mitigate | No package install is planned. |
</threat_model>

<verification>
Run focused dependency/state tests and full backend checks. Evidence must show
the inventory before and after explicit migration and the guarded OFFLINE
transition.
</verification>

<success_criteria>
Retirement discovers all declared dependencies, migration is explicit and
repeat-safe, unresolved references block mutation, and resolved channels become
OFFLINE with audit evidence.
</success_criteria>

<output>
Create `.planning/phases/10-channel-configuration-lifecycle/10-03-SUMMARY.md` when done.
</output>
