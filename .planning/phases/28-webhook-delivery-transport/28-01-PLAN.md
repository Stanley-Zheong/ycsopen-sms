# Phase 28 Plan

## TODO source

The authoritative TODO set is:

`/usr/bin/env ruby .planning/tools/validate-prd-obligations.rb --owner webhook-delivery-transport --assert-unique --assert-traced`

## Implementation steps

1. Add V3700 schema for callback config state, webhook events, and attempt evidence.
2. Implement a thin `WebhookDeliveryTransportService` with config validation, idempotent enqueue, signing, delivery retry, failure listing, replay, pause, and resume.
3. Add focused backend tests for SSRF validation, signed idempotent delivery, terminal retry, replay destination immutability, pause/resume, and migration shape.
4. Implement tenant Webhook configuration UI and admin push-failure operations UI with stable `data-testid` values.
5. Add frontend unit and local Chrome Playwright tests mapped to the direct UI obligations.
6. Capture PRD/UI/focused/full verification evidence and close TODO only when scoped TODO is empty.
7. Commit, push, and create a stacked PR after verification.
