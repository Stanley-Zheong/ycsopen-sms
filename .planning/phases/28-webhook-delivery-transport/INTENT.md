# Phase 28 Intent

The product intent is operational trust: tenants can configure where callbacks go, and operators can prove what was attempted when downstream endpoints fail.

Implementation intent is narrow: one transport module and two UI surfaces, with no new generalized scheduler or domain-specific duplicated callback logic.
