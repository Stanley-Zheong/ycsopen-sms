# Phase 48 Short-link Safety Review Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement short-link creation, automated safety review, human review, public redirect fence, target inspection and analytics.

**Architecture:** Extend existing short-link schema, add a single service boundary, expose tenant/admin/public APIs, and add two React console pages plus public safe page. Security checks are deterministic and testable: no arbitrary outbound fetch is required to prove private-network rejection.

**Tech Stack:** Java 21, Spring Boot, JdbcTemplate, Flyway SQL, React, TanStack Query, Vitest, Playwright local Google Chrome.

**Spec:** `.planning/phases/48-shortlink-safety-review/48-SPEC.md`

## Global Constraints

- No time estimates.
- Chrome-only browser verification.
- Keep scope to short-link safety review; no marketing automation or new external infrastructure.

### Task 1: Backend short-link safety boundary

**Files:** `V5700__shortlink_safety_review.sql`, `ShortLinkSafetyService.java`, `ShortLinkSafetyController.java`, backend tests.

- [x] Extend existing tables and add domain/click evidence tables.
- [x] Create deterministic URL/domain/validity/private-network checks.
- [x] Implement pending, approved, rejected, expired and offline redirect states.
- [x] Implement privacy-safe click analytics and target inspection offline audit.
- [x] Verify with `mvn -q -f core/pom.xml -Dtest=ShortLinkSafetyServiceTest,ShortLinkSafetyReviewMigrationTest test`.

### Task 2: Frontend and browser contract

**Files:** `shortLinkApi.ts`, `TenantShortLinkPage.tsx`, `AdminShortLinkReviewPage.tsx`, `PublicShortLinkSafePage.tsx`, routes, layout nav, CSS, frontend tests.

- [x] Add tenant create/list/analytics UI with stable `data-testid`.
- [x] Add admin review/approve/reject/inspect UI with immutable target evidence.
- [x] Add public safe page states without target jump action.
- [x] Verify with Vitest, build and local Chrome Playwright.

### Task 3: Evidence, review and delivery

- [x] Run full backend/frontend verification.
- [x] Run PRD owner and UI production validators.
- [x] Run review and fix BLOCKER/HIGH findings.
- [x] Commit, push and open PR.
