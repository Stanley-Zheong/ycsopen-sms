# Plan 13-02: Template lifecycle UI and evidence

## Tasks

- Add tenant template page and API client.
- Add admin template review page and navigation.
- Add UI element inventory, test matrix, Chrome Playwright script, and evidence files.
- Run UI contract validator and browser acceptance with local Google Chrome only.

## Verification

- `npm --prefix web test -- template-lifecycle.test.tsx`
- `npm --prefix web test`
- `npm --prefix web run build`
- `npm exec -- playwright test template-lifecycle.spec.ts --project=local-google-chrome --reporter=json`
- `ruby .planning/tools/validate-ui-contract.rb --phase 13 --package template-lifecycle-compliance --stage production`
