# Plan 13-01: Backend template lifecycle and send compliance

## Tasks

- Add template lifecycle DTOs and service.
- Extend template persistence with variables, version linkage, amendment-required state, and review history.
- Add shared domestic send-compliance validator.
- Replace local template/signature/variable checks in `MessageSubmitService` with the shared validator.
- Add tests for application, field validation, preview, review, resubmit, and send compliance rejection.

## Verification

- `mvn -q -f core/pom.xml -Dtest=TemplateLifecycleServiceTest,TemplateSendComplianceServiceTest,MessageSubmitServiceTest test`
- `mvn -q -f core/pom.xml test`
