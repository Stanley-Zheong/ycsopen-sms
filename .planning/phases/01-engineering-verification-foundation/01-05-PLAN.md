---
phase: 01-engineering-verification-foundation
plan: 05
type: execute
wave: 3
depends_on: [01-00, 01-01, 01-04]
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/.eslintrc.cjs
  - web/playwright.config.ts
  - web/src/pages/LoginPage.tsx
  - web/src/pages/tenant/send/SendPage.tsx
  - web/test/phase01/login-scenario.spec.ts
  - web/verification/ui-manifest.json
  - web/verification/browser-scenarios.schema.json
  - web/verification/browser-scenarios.json
  - web/scripts/serve-browser-scenario.mjs
  - web/scripts/test-browser-scenario-server.mjs
  - web/scripts/validate-browser-scenario.mjs
  - web/scripts/test-browser-scenario-validator.mjs
autonomous: true
requirements: [REQ-NFR-COMPATIBILITY]
obligations: [OBL-FOUND-TRACE-003, OBL-FOUND-UI-DRIFT-001, OBL-NFR-BROWSER]
entry_blockers:
  - "BLOCKED until Plan 00's standard-path local Google Chrome version/headless-synthetic-page probe passes independent review and bootstrap-phase-01.rb validates all 13 plans with zero exit under DR-01-016 and DR-01-017."
must_haves:
  truths:
    - "The existing /login route exposes stable shared-auth-login-page, card, username, password, submit, and error selectors; no /verification route is added."
    - "Per D-03, D-04, DR-01-002, and DR-01-006, one versioned login scenario closes route, component, selector, action, HTTP-401 responder, assertion, and visual-rule relations without relabelling engines."
    - "Per D-01, D-02, D-05, D-06, and D-07, structural evidence is fail-closed, synthetic, schema-free, and does not claim backend login or Phase 56 product acceptance."
    - "Per DR-01-009 and DR-01-016, LOGIN-SMOKE-V1 uses one repository-owned real HTTP responder and the current standard-path local Google Chrome at 1440x900; Playwright request interception cannot satisfy the contract."
    - "Per DR-01-010, LOGIN-CARD-IN-VIEWPORT-V2 rejects viewport escape, ancestor clipping, clip-path/mask/hidden state, and center-or-corner overlay obstruction under one canonical rule digest."
  artifacts:
    - path: web/src/pages/LoginPage.tsx
      provides: "Existing login page with stable shared-auth-login-* automation selectors"
    - path: web/verification/browser-scenarios.json
      provides: "Versioned shared smoke/action/assertion/visual contract consumed by every browser adapter"
    - path: web/test/phase01/login-scenario.spec.ts
      provides: "Project-local structural execution of the shared scenario"
    - path: web/scripts/serve-browser-scenario.mjs
      provides: "Local-direct dist/SPA server, deterministic 401 responder, subject-bound health contract, and marker headers"
    - path: web/scripts/validate-browser-scenario.mjs
      provides: "Canonical scenario and LOGIN-CARD-IN-VIEWPORT-V2 evaluator shared by structural diagnostics and the Chrome-only runner"
  key_links:
    - from: web/verification/browser-scenarios.json
      to: web/test/phase01/login-scenario.spec.ts
      via: "exact scenario ID, /login route, selectors, action/assertion, and visual rule"
    - from: web/src/pages/LoginPage.tsx
      to: web/verification/ui-manifest.json
      via: "shared-auth-login-* selector relation tuple"
    - from: web/scripts/serve-browser-scenario.mjs
      to: web/verification/browser-scenarios.json
      via: "health scenario ID/tested-subject/subject-manifest/scenario digests plus deterministic POST responder"
---

<objective>
Establish the shared `/login` browser scenario and stable production selectors used by UI drift, copy runtime, and the current local-Chrome runner.

Purpose: Close the route-selector ownership gap without inventing synthetic production routes, while giving the single 1440x900 local Google Chrome run one executable semantic contract.
Output: Pinned Playwright tooling, stable LoginPage test IDs, versioned scenario/schema, real HTTP scenario server, canonical visual evaluator with destructive fixtures, manifest relation, and structural test.

Schema migrations: none.
</objective>

<execution_context>
@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@/Users/laosanzheong/.codex/gsd-core/templates/summary.md
</execution_context>

<entry_preconditions>
Implementation is not authorized until Plan 00 writes valid entry-only `EVIDENCE/local-chrome-entry.json` from the standard-path fixed-argv version and isolated-profile headless synthetic-page probe, an independent reviewer records current ENTRY PASS, and the revised bootstrap validates exactly 13 plans with zero exit under DR-01-016 and DR-01-017. Entry has no Playwright dependency. Attempt 3's downloaded source chain is superseded. This plan downloads no browser, uses no ChromeDriver, creates no version/viewport matrix, and adds no provider, tunnel, secret, VM, non-Chrome lane, or browser-blocking UI.
</entry_preconditions>

<execution_state_boundary>
Work already landed before interruption is unaccepted, not complete: package/lock and lint/Playwright configuration, LoginPage/SendPage edits, the login scenario/manifest/schema, and scenario server/validator files must be inspected against this plan and rerun through every declared verification before any acceptance claim. Resume inside the 14 implementation files listed in `files_modified`; do not add another formal phase, plan, lane, browser, or implementation file. `web/src/pages/tenant/send/SendPage.tsx` is already inside those 14 files and may receive only the authorized Rule 3 type/lint repair. Remaining closure is limited to removing obsolete CfT/driver assumptions from these owned files, using the current local Chrome project, completing the real `/login` structural contract, and producing passing focused checks. Any defect outside this ownership is recorded for its owning plan rather than expanding Plan 05.
</execution_state_boundary>

<context>
@AGENTS.md
@web/src/router/routes.tsx
@web/src/pages/LoginPage.tsx
@web/src/api/auth.ts
@.planning/UI-TEST-CONTRACT.md
@.planning/phases/01-engineering-verification-foundation/01-CONTEXT.md
@.planning/phases/01-engineering-verification-foundation/DECISIONS.md
@.planning/phases/01-engineering-verification-foundation/TEST-MATRIX.md
@.planning/phases/01-engineering-verification-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin project-local Playwright, restore lint, and configure deterministic reporters</name>
  <files>web/package.json, web/package-lock.json, web/.eslintrc.cjs, web/playwright.config.ts, web/src/pages/tenant/send/SendPage.tsx</files>
  <behavior>
    - The audited `@playwright/test@1.62.1` is exact and the current-branch lockfile is regenerated.
    - Non-watch scripts expose the local-Chrome `/login` command with deterministic JSON/JUnit output plus screenshots/traces on failure and a local-direct `PHASE01_BASE_URL` contract.
    - `npm --prefix web run lint` uses the installed ESLint 8, `@typescript-eslint`, `react-hooks`, and `react-refresh` stack through a committed human-readable config; a missing config, lint error, or warning fails nonzero.
    - Lint includes business source and relevant tests/scripts/configuration, while tracked generated artifacts are rejected by repository checks rather than using ignore patterns to hide business code.
    - The only browser project uses Playwright `channel: "chrome"` or the canonical standard executable at 1440x900; no downloaded, driver-backed, or other-browser project exists.
  </behavior>
  <action>Inspect and reconcile the already-landed package/lock changes against the package approved by the research Package Legitimacy Audit; regenerate the lockfile only if reconciliation requires it, then prove reproducibility with `npm ci`. Inspect and complete the already-landed `web/.eslintrc.cjs` for ESLint 8 using the installed `@typescript-eslint`, `react-hooks`, and `react-refresh` plugins; keep the config readable, make warnings blocking, include application source plus relevant TypeScript tests/scripts/configuration, and do not weaken or disable rules merely to get green. Do not ignore business source. Apply the already identified minimal type/lint repair in `web/src/pages/tenant/send/SendPage.tsx` under execution Rule 3: change only the code necessary for the Phase 1 lint lane, add no sending behavior, UI action, API change, selector, or refactor, and record the exact deviation in `01-05-SUMMARY.md`. Reconcile the existing Playwright config to one project using the repository-owned scenario server plus local-direct `PHASE01_BASE_URL` and the standard-path current Google Chrome at 1440x900 through `channel: "chrome"` or the exact executable. No browser download, ChromeDriver, `page.route`, request interception, browser credential, provider, tunnel, VM, or non-Chrome project is allowed. Do not create files beyond the 14-file implementation ownership, do not copy the divergent candidate branch, and implement D-04, D-08, DR-01-016, and DR-01-017.</action>
  <verify>
    <automated>npm --prefix web ci &amp;&amp; npm --prefix web run lint &amp;&amp; npm --prefix web test &amp;&amp; npm --prefix web run build &amp;&amp; test -z "$(git ls-files web/dist web/node_modules)"</automated>
  </verify>
  <done>The exact audited package installs from the regenerated lockfile; lint passes with zero warnings alongside web tests/build; no business source is hidden; the SendPage Rule 3 repair is minimal and recorded; config/reporters are deterministic; the sole browser project resolves the current standard-path Google Chrome at 1440x900.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the existing login page to the shared scenario contract</name>
  <files>web/src/pages/LoginPage.tsx, web/test/phase01/login-scenario.spec.ts, web/verification/ui-manifest.json, web/verification/browser-scenarios.schema.json, web/verification/browser-scenarios.json</files>
  <behavior>
    - `/login` renders visible `shared-auth-login-page`, `shared-auth-login-card`, `shared-auth-login-username`, `shared-auth-login-password`, and `shared-auth-login-submit`; the rejected-login branch renders `shared-auth-login-error`.
    - The scenario fills synthetic credentials, submits through real HTTP to the scenario server's canonical `POST /console/auth/login` responder (with the existing `/api/v1` client prefix mapped to that same responder), observes exact HTTP 401, the fixed safe JSON body, and the fixed scenario marker header, then asserts the stable simplified-Chinese error without claiming backend authentication evidence.
    - Canonical `LOGIN-CARD-IN-VIEWPORT-V2` requires each registered element border-box rectangle to be positive and inside the viewport, then walks the element and ancestor chain: `display`, `visibility`, `content-visibility`, zero effective opacity, non-`none` clip-path/mask, and axis-specific overflow clipping are fail conditions; every clipping ancestor's visible intersection must fully cover the required element rectangle.
    - The same visual rule hit-tests the center and four deterministically inset corners with `elementFromPoint`; each point must hit the required element or its descendant. Exact-full-visibility IntersectionObserver output may corroborate the result but cannot replace ancestor intersection or five-point hit-testing. Internal anonymous/input-content clipping is excluded by evaluating the registered element border box and its DOM ancestors, not descendants.
  </behavior>
  <action>Add only stable `data-testid` attributes to the existing `LoginPage.tsx`; keep `/login` in the existing router and do not modify `routes.tsx` unless execution reveals an actual defect. Define schema-versioned `LOGIN-SMOKE-V1` with route, required selectors, synthetic input actions, canonical responder path and `/api/v1` prefix mapping, exact 401/status/body/marker assertions, console/page-error policy, and canonical `LOGIN-CARD-IN-VIEWPORT-V2`. Canonicalize and SHA-256 hash the complete visual rule object and complete scenario object; the structural test, validator, server health, later adapters, and evidence must carry the exact rule ID/digest and scenario ID/digest. The Playwright test must navigate to the real server, wait for and inspect the UI submit response rather than intercept it, and contain exact OBL, Case, Playwright, scenario, route, action, assertion, selector, responder, and visual-rule identities. Emit DOM/ancestor/intersection/hit-test observations plus screenshot checksums. Update only the `/login` relation in the UI manifest. This implements D-03, D-07, DR-01-006, and DR-01-008 without creating a synthetic production route.</action>
  <verify>
    <automated>npm --prefix web run build &amp;&amp; node web/scripts/validate-ui-drift.mjs --manifest web/verification/ui-manifest.json --routes web/src/router/routes.tsx --playwright web/test/phase01/login-scenario.spec.ts</automated>
  </verify>
  <done>The existing `/login` route, selectors, responder assertions, canonical rule object, and exact digests are represented in one versioned scenario; UI reconciliation passes and no nonexistent route or backend-completion claim exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the local-direct responder and destructive visual-contract suite</name>
  <files>web/scripts/serve-browser-scenario.mjs, web/scripts/test-browser-scenario-server.mjs, web/scripts/validate-browser-scenario.mjs, web/scripts/test-browser-scenario-validator.mjs</files>
  <behavior>
    - The Node HTTP server serves `web/dist` with SPA fallback, exposes `GET /__phase01/health` containing only scenario ID, `subject_manifest_digest`, `tested_subject_digest`, and recomputed scenario-contract digest, and maps both canonical `POST /console/auth/login` and the existing `/api/v1/console/auth/login` request to one responder that always returns HTTP 401 with `Content-Type: application/json`, exact body `{"code":"AUTH_INVALID_CREDENTIALS","message":"用户名或密码错误"}`, and marker `X-YCS-Scenario: LOGIN-SMOKE-V1`.
    - The server accepts only code-owned validated argv for host, port, dist directory, subject-manifest path/digests, and contract path; it independently validates the manifest through Plan 01 and never reads secrets, evaluates commands, spawns dynamic commands, proxies arbitrary destinations, or includes submitted credentials in output/artifacts.
    - Independent visual mutations reject, with stable distinct diagnostics: an ancestor `overflow:hidden` clipping an edge while the target rectangle remains viewport-valid; element or ancestor clip-path; element or ancestor mask; zero-opacity/hidden state; a pointer-capturing transparent overlay; and an opaque overlay that obstructs any inset corner. A normal input whose internal content clips but whose registered border box and ancestors are fully visible passes.
  </behavior>
  <action>Implement the local-direct server and canonical evaluator from the schema rather than duplicating constants in tests, per DR-01-009, DR-01-010, and DR-01-011. Validate the canonical tested-input manifest through Plan 01 before serving, and bind health, structural execution, later Chrome cells, and evidence to the same `subject_manifest_digest` and `tested_subject_digest` plus the independently recomputed scenario digest. Define exact clipping axes and ancestor padding-box intersections; require the full target rectangle after each applicable ancestor intersection. Define five hit-test coordinates as center plus four rule-specified inset corners and require the hit node to equal the target or be its descendant. Record every computed style, ancestor intersection, point, hit target, and optional IntersectionObserver corroboration in deterministic observations. Build one known-good fixture and one isolated mutation for each subject mismatch, clipping, clip-path, mask, hidden/opacity, transparent-overlay, opaque-overlay, and normal-input case. Recompute canonical subject/scenario/rule digests, reject a wrong/stale subject or stale rule/scenario digest before browser execution, start the real server for structural diagnostics, and prove the browser-observed submit response carries exact 401/body/marker. Do not use Playwright routing or a runner-side HTTP client as scenario evidence.</action>
  <verify>
    <automated>node web/scripts/test-browser-scenario-server.mjs &amp;&amp; node web/scripts/test-browser-scenario-validator.mjs &amp;&amp; npm --prefix web run test:browser:structural</automated>
  </verify>
  <done>The subject-bound health and 401 contracts pass over real HTTP, every isolated subject/visual mutation fails with its stable diagnostic, the normal internal-input-clipping fixture passes, and structural Playwright proves the exact subject/scenario/rule digests plus responder marker without request interception.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Scenario JSON to browser test | Human-editable scenario data cannot introduce arbitrary code or omit required semantic fields. |
| Browser session to scenario HTTP server | The repository-owned 401 responder proves only the UI scenario and must never be labelled backend integration. |
| Required element to rendered ancestors/overlays | Viewport-valid rectangles alone do not prove unclipped or unobstructed interaction. |
| Playwright project name to browser identity | Labels cannot establish supported Google Chrome identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-01-13 | Spoofing | project/browser identity | mitigate | Canonical standard path, `Google Chrome` full-version runtime identity, and successful Playwright launch; durable single-run closure is delegated to Plan 06. |
| T-01-14 | Tampering | scenario contract | mitigate | Versioned schema, fixed action vocabulary, exact relation validation, and destructive fixtures. |
| T-01-15 | Information disclosure | login evidence | mitigate | Synthetic credentials, no secret values, redacted traces, and no production payload. |
| T-01-15A | Tampering/Spoofing | responder and visual observations | mitigate | Recomputed scenario/rule digests, exact 401 marker, ancestor intersections, five-point hit-tests, and isolated mutations. |
| T-01-SC | Tampering | npm install | mitigate | Exact audited Playwright package and regenerated current-branch lockfile. |
</threat_model>

<verification>
Run all three focused tasks, the existing web test/build commands, UI drift validation, real-HTTP server tests, and the visual mutation suite. Confirm the router diff is empty, LoginPage changes are test IDs only, the SendPage change is only the recorded minimal Rule 3 lint/type repair, and the deterministic 401 responder is labelled non-backend evidence.
</verification>

<success_criteria>
- `/login` owns every selector required by browser and copy scenarios.
- The same versioned route/action/real-HTTP-responder/assertion contract is executable by later adapters without Playwright interception.
- Visual PASS binds one `LOGIN-CARD-IN-VIEWPORT-V2` digest and depends on viewport rectangles, ancestor clipping/style checks, and five-point hit-tests, not screenshot presence.
- No synthetic production route, schema change, business behavior, or Phase 56 acceptance claim is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-engineering-verification-foundation/01-05-SUMMARY.md` after execution and leave it uncommitted for the single final Phase 1 commit.
</output>
