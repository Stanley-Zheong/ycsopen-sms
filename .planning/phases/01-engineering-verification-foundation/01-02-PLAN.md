---
phase: 01-engineering-verification-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .planning/tools/validate-trace-closure.rb
  - .planning/tools/test-trace-closure.rb
  - .planning/tools/validate-phase-lifecycle.rb
  - .planning/tools/test-phase-lifecycle.rb
autonomous: true
requirements:
  - REQ-NFR-COMPATIBILITY
obligations:
  - OBL-FOUND-TRACE-001
  - OBL-FOUND-TRACE-002
  - OBL-FOUND-TRACE-004
entry_blockers:
  - "This plan's original implementation is recorded; its lifecycle output is regression-checked only after Plan 00 independent ENTRY PASS and revised 13-plan bootstrap PASS."
must_haves:
  truths:
    - "Per D-01 and D-02, exact obligation/TODO/review/lifecycle queries fail closed with affected IDs and lines; only verified evidence can close work."
    - "Per D-03 and D-06, trace closure consumes normalized UI relations and emits terminal plus JSON diagnostics without claiming UI implementation."
    - "Per D-04 and D-05, unavailable compatibility evidence remains BLOCKED and lifecycle validation introduces no business schema or Flyway migration."
  artifacts:
    - path: .planning/tools/validate-trace-closure.rb
      provides: "Bidirectional requirement-obligation-behavior-test-evidence ownership reconciliation"
    - path: .planning/tools/validate-phase-lifecycle.rb
      provides: "Explicit entry, pre-push-exit, and post-push-delivery lifecycle stages"
  key_links:
    - from: .planning/tools/validate-trace-closure.rb
      to: .planning/PRD-OBLIGATIONS.md
      via: "exact nine-field records and reverse artifact references"
    - from: .planning/tools/validate-phase-lifecycle.rb
      to: .planning/phases/01-engineering-verification-foundation/TODO.md
      via: "stage-specific exact checkbox and evidence validation"
---

<objective>
Implement the destructive trace and lifecycle gates for engineering-verification-foundation-01, -02, and -04.

Purpose: Prove missing trace fields, reverse orphans, duplicate ownership, malformed reviews, invalid evidence, and incorrect TODO state cannot pass a phase boundary under D-01 through D-06.
Output: Standard-library Ruby trace/lifecycle validators with isolated one-mutation fixtures.

Schema migrations: none.
</objective>

<execution_context>
@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@/Users/laosanzheong/.codex/gsd-core/templates/summary.md
</execution_context>

<entry_preconditions>
This plan's functional implementation is recorded and is not repeated. Its lifecycle output must later be regression-checked after Plan 00 entry-only evidence, independent ENTRY PASS, and revised 13-plan bootstrap PASS. Attempt 3's browser-source files are superseded history and cannot be lifecycle inputs.
</entry_preconditions>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/PRD-OBLIGATIONS.md
@.planning/EXECUTION-STANDARD.md
@.planning/tools/validate-prd-obligations.rb
@.planning/tools/validate-phase-entry.rb
@.planning/tools/test-planning-validators.rb
@.planning/phases/01-engineering-verification-foundation/01-SPEC.md
@.planning/phases/01-engineering-verification-foundation/01-CONTEXT.md
@.planning/phases/01-engineering-verification-foundation/DECISIONS.md
@.planning/phases/01-engineering-verification-foundation/TODO.md
@.planning/phases/01-engineering-verification-foundation/TEST-MATRIX.md
@.planning/phases/01-engineering-verification-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove exact catalog and reverse trace closure</name>
  <files>.planning/tools/validate-trace-closure.rb, .planning/tools/test-trace-closure.rb</files>
  <behavior>
    - The real owner query resolves exactly the seven Phase 1 obligation IDs and rejects every missing owner, behavior, automated test, evidence target, requirement, or unknown reference.
    - Requirement-to-obligation and obligation-to-requirement orphans are distinct from duplicate owner, test, and evidence diagnostics.
    - Artifact references in SPEC, TODO, TEST-MATRIX, plans, and evidence are exact ID sets rather than substring matches.
  </behavior>
  <action>Build `validate-trace-closure.rb` on top of the current nine-field catalog contract without weakening its existing global checks. Normalize requirement, obligation, behavior, verification, catalog test/layer, case, evidence, owner, and direct-UI relation edges; report missing, foreign, orphan, and duplicate sets independently with exact source location. Its fixture suite starts from a complete synthetic phase and mutates one field or reverse edge at a time, including wrong field count, unknown requirement/owner, missing behavior/test/evidence, duplicate obligation/test/evidence, both orphan directions, and comment-only fake references. Add a real-catalog owner-set assertion that the Phase 1 set is exactly TRACE-001..004, UI-DRIFT-001..002, and NFR-BROWSER, while Phase 56 owns the two product compatibility rows per D-07 and DR-01-008. Generate evidence through Plan 01's kernel. This task is the exclusive primary closure for OBL-FOUND-TRACE-001 and OBL-FOUND-TRACE-002 under D-01, D-02, D-03, D-06, and D-07.</action>
  <verify>
    <automated>/usr/bin/env ruby .planning/tools/test-trace-closure.rb &amp;&amp; /usr/bin/env ruby .planning/tools/validate-prd-obligations.rb --owner engineering-verification-foundation --assert-unique --assert-traced</automated>
  </verify>
  <done>The positive graph selects exactly seven owned obligations and rejects either product-acceptance obligation as foreign to Phase 1; every destructive mutation fails with a direction-specific stable diagnostic and affected ID/location; OBL-FOUND-TRACE-001 and OBL-FOUND-TRACE-002 TODOs remain open until their real EVIDENCE summaries pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stage-explicit phase lifecycle and effective TODO gates</name>
  <files>.planning/tools/validate-phase-lifecycle.rb, .planning/tools/test-phase-lifecycle.rb</files>
  <behavior>
    - Entry rejects missing artifacts, empty task fields, malformed or blocking review rows, prechecked owned TODOs, incomplete trace, and non-runnable checks.
    - Pre-push exit rejects unchecked non-delivery TODOs, invalid/missing evidence, unresolved GSD/Claude BLOCKER or HIGH findings, and a review cycle left escalated.
    - Post-push delivery cannot infer remote truth locally and delegates only the reserved external delivery item to the delivery-attestation validator created in Plan 03.
  </behavior>
  <action>Implement explicit `entry`, `pre-push-exit`, and `post-push-delivery` modes with strict OptionParser arguments, stable uppercase diagnostics, accumulated errors, and nonzero on any contradiction. Parse only real checkbox and review-table rows; report exact file and line; require every checked non-delivery item to cite independently valid evidence from Plan 01 whose subject manifest and tested-subject digest still match. Enforce the bounded review cycle, including non-decreasing blocker counts and unresolved third-attempt escalation, without a proceed-anyway path. Use temporary phase directories and a local bare Git repository in fixtures; mutate missing artifacts, task files/action/verify/done, malformed review, BLOCKER/HIGH, prechecked entry TODO, unchecked exit TODO, absent evidence, wrong/stale tested subject, and premature local-only delivery. Do not implement remote resolution here; Plan 03 supplies that dependency. This is OBL-FOUND-TRACE-004's artifact/TODO/review portion per D-01, D-02, D-04, D-05, D-06, and DR-01-011.</action>
  <verify>
    <automated>/usr/bin/env ruby .planning/tools/test-phase-lifecycle.rb</automated>
  </verify>
  <done>Each lifecycle stage accepts only its own complete positive fixture and rejects every single-invariant mutation with an exact stable error; the reserved post-push item is never locally self-certified; no TODO is checked by the fixture runner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Catalog and phase Markdown to normalized graph | Human-editable text is untrusted and may contain fake, duplicate, or malformed references. |
| Review/TODO artifacts to lifecycle state | A checkbox or prose claim is not evidence without a validated linked result. |
| Local Git state to delivery state | Local refs cannot establish configured-remote visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-01-04 | Tampering | trace graph | mitigate | Exact parsed IDs, bidirectional set comparison, source locations, and one-mutation fixtures. |
| T-01-05 | Spoofing/Repudiation | TODO and reviews | mitigate | Structured rows, evidence checksum validation, bounded-cycle state, and stage-specific rules. |
| T-01-06 | Spoofing | delivery state | mitigate | Refuse local-only proof and delegate to the independent live-remote validator in Plan 03. |
| T-01-SC | Tampering | dependencies | mitigate | Standard-library Ruby only; no package installation. |
</threat_model>

<verification>
Run both task commands, the existing planning validator suite, and the real seven-obligation owner query. Confirm every fixture resets to a known-good baseline and each negative case changes one invariant. The current full entry command must continue to return nonzero until the local-Chrome runtime probe and independent entry review truly pass.
</verification>

<success_criteria>
- OBL-FOUND-TRACE-001 and OBL-FOUND-TRACE-002 have exact destructive trace evidence.
- Lifecycle entry and exit stages reject missing artifacts, evidence, TODO closure, and review clearance.
- Remote delivery is explicitly unresolved until Plan 03's live attestation proof.
- No business behavior, visual design, or schema migration enters the phase.
</success_criteria>

<output>
Create `.planning/phases/01-engineering-verification-foundation/01-02-SUMMARY.md` after execution. Do not commit it separately; final delivery creates the sole Phase 1 implementation commit.
</output>
