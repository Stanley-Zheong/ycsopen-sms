---
phase: 01-engineering-verification-foundation
plan: 08
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - core/pom.xml
  - core/src/test/resources/application-phase01-integration.yml
  - core/src/test/resources/verification/timezone-contract.json
  - core/src/test/java/com/ycsopen/sms/core/verification/Phase01MySqlIntegrationTest.java
  - core/src/test/java/com/ycsopen/sms/core/verification/Phase01RedisIntegrationTest.java
  - core/src/test/java/com/ycsopen/sms/core/verification/Phase01TimezoneContractTest.java
  - scripts/lib/phase-01/service_checks.rb
  - scripts/lib/phase-01/test_service_checks.rb
autonomous: true
requirements: [REQ-NFR-COMPATIBILITY]
obligations: [OBL-FOUND-TRACE-003]
entry_blockers:
  - "This plan's original implementation is recorded; its service/timezone output is regression-checked only after Plan 00 independent ENTRY PASS and revised 13-plan bootstrap PASS."
must_haves:
  truths:
    - "Disposable real MySQL and Redis prove authenticated functional and Spring wiring, not readiness-only or mock behavior."
    - "The timezone verifier proves explicit Asia/Shanghai JVM/JDBC/MySQL/API-contract behavior plus preservation of an IANA zone in a synthetic contract fixture under deliberately different defaults."
    - "Per D-05, D-07, and DR-01-008, no Flyway migration, product route/test ID, or international-message persistence claim is created."
  artifacts:
    - path: scripts/lib/phase-01/service_checks.rb
      provides: "Pinned disposable MySQL/Redis lifecycle and evidence capture"
    - path: core/src/test/java/com/ycsopen/sms/core/verification/Phase01TimezoneContractTest.java
      provides: "Real-service and serialization verifier contract without business persistence"
    - path: core/src/test/resources/verification/timezone-contract.json
      provides: "Synthetic instant/offset/IANA identity fixture"
  key_links:
    - from: scripts/lib/phase-01/service_checks.rb
      to: core/src/test/java/com/ycsopen/sms/core/verification
      via: "test-only connection identity and pinned disposable services"
    - from: core/src/test/resources/verification/timezone-contract.json
      to: Phase01TimezoneContractTest.java
      via: "synthetic IANA zone/instant/offset round-trip contract"
---

<objective>
Add real MySQL/Redis verification and a reusable UTC+8/IANA-timezone contract to the Phase 1 evidence foundation.

Purpose: Prove production dependency semantics and host-independent time verification without H2/mocks, browser-only proof, or nonexistent international-message persistence.
Output: Isolated integration profile, pinned service harness, functional Spring tests, and synthetic timezone contract fixture.

Schema migrations: none.
</objective>

<execution_context>
@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@/Users/laosanzheong/.codex/gsd-core/templates/summary.md
</execution_context>

<entry_preconditions>
This plan's functional implementation is recorded and is not repeated. Its service/timezone output must later be regression-checked after Plan 00 entry-only evidence, independent ENTRY PASS, and revised 13-plan bootstrap PASS. Docker availability does not override entry authorization. Attempt 3's source chain is superseded history.
</entry_preconditions>

<context>
@AGENTS.md
@core/pom.xml
@core/src/test/resources/application-test.yml
@core/src/main/resources/application-dev.yml
@core/src/main/resources/db/migration/V1__init_schema.sql
@.planning/phases/01-engineering-verification-foundation/01-RESEARCH.md
@.planning/phases/01-engineering-verification-foundation/DECISIONS.md
@.planning/phases/01-engineering-verification-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove real MySQL migration, UTF-8, transaction, and session identity</name>
  <files>core/pom.xml, core/src/test/resources/application-phase01-integration.yml, core/src/test/java/com/ycsopen/sms/core/verification/Phase01MySqlIntegrationTest.java, scripts/lib/phase-01/service_checks.rb, scripts/lib/phase-01/test_service_checks.rb</files>
  <behavior>
    - Pinned isolated MySQL accepts authenticated SELECT 1, applies existing V1 through Flyway, and reports version/digest/session identity.
    - Spring transaction and simplified-Chinese UTF-8 round trips pass against MySQL.
    - Readiness-only, access denied, persistent stale state, wrong checksum, or unavailable daemon/service returns nonzero with evidence.
  </behavior>
  <action>Add a Phase 1 integration Maven profile separate from H2 unit tests. Use official researched MySQL 8.4.11 pinned by resolved digest, random container/network names, no volume, test-only credentials omitted from recorded argv, bounded readiness followed by functional checks, and guaranteed cleanup. Assert the existing V1 checksum; never create or modify a migration per D-05.</action>
  <verify>
    <automated>/usr/bin/env ruby scripts/lib/phase-01/test_service_checks.rb --mysql &amp;&amp; mvn -f core/pom.xml -Pphase01-integration -Dtest=Phase01MySqlIntegrationTest test</automated>
  </verify>
  <done>The real-MySQL positive path passes and every credential/readiness/checksum/state/unavailable mutation fails; evidence records digest and functional assertions without a migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove real Redis TTL and Spring application wiring</name>
  <files>core/src/test/java/com/ycsopen/sms/core/verification/Phase01RedisIntegrationTest.java, scripts/lib/phase-01/service_checks.rb, scripts/lib/phase-01/test_service_checks.rb</files>
  <behavior>
    - Pinned isolated Redis performs PING, SET+TTL, GET, TTL assertion, DEL, and one Spring StringRedisTemplate round trip under a random synthetic prefix.
    - Missing TTL, cross-run key visibility, readiness-only proof, failed Spring wiring, or unavailable service fails nonzero.
    - No FLUSHALL, production key, shared unauthenticated host, volume, or committed runtime state is used.
  </behavior>
  <action>Extend the service harness with official Redis 8.4.5 pinned by resolved digest and invoke redis-cli inside the container. Use isolated names/network, test-only configuration, a random synthetic prefix, explicit TTL/delete assertions, Spring wiring, and guaranteed cleanup. Treat missing Docker/service as BLOCKED/nonzero under D-02.</action>
  <verify>
    <automated>/usr/bin/env ruby scripts/lib/phase-01/test_service_checks.rb --redis &amp;&amp; mvn -f core/pom.xml -Pphase01-integration -Dtest=Phase01RedisIntegrationTest test</automated>
  </verify>
  <done>The real Redis functional and Spring paths pass; every negative mutation fails distinctly; no persistent/shared state remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the UTC+8 and IANA identity verifier contract</name>
  <files>core/src/test/resources/verification/timezone-contract.json, core/src/test/java/com/ycsopen/sms/core/verification/Phase01TimezoneContractTest.java, scripts/lib/phase-01/service_checks.rb, scripts/lib/phase-01/test_service_checks.rb</files>
  <behavior>
    - With host/JVM defaults deliberately outside Shanghai, the verifier asserts explicit Asia/Shanghai policy, MySQL session zone, temporal round trip, and API-contract offset/instant.
    - A synthetic international fixture retains IANA zone ID with instant/offset through serialization and verification; removed/mutated identity fails.
    - No product entity, repository, route, DOM selector, database column, or migration is added or claimed.
  </behavior>
  <action>Create a test-only JSON fixture and JUnit verifier using the real MySQL service from Task 1. Force a non-Shanghai default, set/inspect the MySQL session timezone, round-trip a boundary instant, serialize/parse a test DTO contract, and assert IANA zone identity. Add destructive fixture mutations for host-default leakage, wrong offset, missing IANA ID, changed instant, and missing session proof. This is the reusable D-07/DR-01-008 verifier under OBL-FOUND-TRACE-003, not product persistence evidence.</action>
  <verify>
    <automated>/usr/bin/env ruby scripts/lib/phase-01/test_service_checks.rb --timezone-contract &amp;&amp; mvn -f core/pom.xml -Pphase01-integration -Dtest=Phase01TimezoneContractTest test</automated>
  </verify>
  <done>Real-service and serialization contract tests pass under different defaults; every timezone/IANA mutation fails; no production route, selector, entity, field, or migration exists in the diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Test process to services | Readiness, credentials, schema, isolation, and application wiring need functional proof. |
| Host/JVM/JDBC defaults to time contract | Defaults can silently alter results and must not be trusted. |
| Synthetic fixture to product persistence | A verifier DTO cannot establish international-message storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-01-22 | Tampering/Repudiation | service state | mitigate | Pinned digests, isolated containers/networks, functional assertions, and cleanup. |
| T-01-23 | Spoofing | readiness/time defaults | mitigate | Authenticated operations and explicit JVM/JDBC/session/serialization assertions. |
| T-01-24 | Spoofing | product acceptance | mitigate | Test-only fixture namespace plus owner-set and no-production-artifact assertions. |
| T-01-SC | Tampering | service images | mitigate | Official researched images pinned by resolved digest; no application package install. |
</threat_model>

<verification>
Run all task commands plus `mvn -f core/pom.xml test`. Validate evidence through Plan 01 and confirm no services/volumes, secrets, target output, production schema, or product time fields are committed.
</verification>

<success_criteria>
- MySQL/Flyway/UTF-8/transaction and Redis TTL/Spring wiring use real isolated services.
- UTC+8/IANA verification is host-independent and destructive-fixture protected.
- All results contribute only to OBL-FOUND-TRACE-003.
- No business persistence, web route/test ID, or schema migration is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-engineering-verification-foundation/01-08-SUMMARY.md` after execution and leave it for the final atomic commit.
</output>
