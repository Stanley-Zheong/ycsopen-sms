---
phase: 01-engineering-verification-foundation
plan: 07
type: execute
wave: 4
depends_on: [01-00, 01-05]
files_modified:
  - web/package.json
  - web/playwright.config.ts
  - web/verification/copy.zh-CN.json
  - web/verification/fixtures/zh-cn-export.csv
  - web/scripts/validate-copy-zh-cn.mjs
  - web/scripts/test-copy-zh-cn.mjs
  - web/test/phase01/chinese-copy.spec.ts
autonomous: true
requirements: [REQ-NFR-COMPATIBILITY]
obligations: [OBL-FOUND-TRACE-003]
entry_blockers:
  - "BLOCKED with the whole phase until the standard-path local Google Chrome runtime probe, independent rerun, revised bootstrap, and final Phase 1 entry gate pass."
must_haves:
  truths:
    - "The versioned copy/export contract rejects unregistered English, traditional-only variants, hidden runtime copy, missing export headers, and broad technical allowlists."
    - "Runtime validation navigates the existing /login route and uses shared-auth-login-page/error selectors from Plan 05; it creates no new route or JSX owner."
    - "Per D-07 and DR-01-008, the result proves the reusable fail-closed contract under OBL-FOUND-TRACE-003 and never closes Phase 56 product acceptance."
  artifacts:
    - path: web/verification/copy.zh-CN.json
      provides: "Versioned simplified-Chinese literal/export-header registry and narrow technical-token allowlist"
    - path: web/scripts/validate-copy-zh-cn.mjs
      provides: "AST/runtime/error/export-contract validator"
    - path: web/test/phase01/chinese-copy.spec.ts
      provides: "Existing /login runtime copy assertion using stable selectors"
  key_links:
    - from: web/test/phase01/chinese-copy.spec.ts
      to: web/src/pages/LoginPage.tsx
      via: "/login plus shared-auth-login-page and shared-auth-login-error"
    - from: web/scripts/validate-copy-zh-cn.mjs
      to: web/verification/copy.zh-CN.json
      via: "exact registered surface and technical-token policy"
---

<objective>
Build the reusable simplified-Chinese UI/error/export contract validator and prove it against `/login` plus synthetic export fixtures.

Purpose: Supply later phases and Phase 56 with a fail-closed copy gate without treating a foundation fixture as complete product coverage.
Output: Versioned registry, AST/runtime/export validator, destructive fixtures, and `/login` runtime test.

Schema migrations: none.
</objective>

<execution_context>
@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@/Users/laosanzheong/.codex/gsd-core/templates/summary.md
</execution_context>

<entry_preconditions>
Do not execute behind the Phase 1 local-Chrome Gate D blocker. Plan 00 must produce valid entry-only fixed-path version/headless-synthetic-page evidence, an independent reviewer must record current ENTRY PASS, and the real bootstrap must validate all 13 plans with zero exit. This plan adds no browser choice, download, driver, or matrix and cannot make phase entry PASS by itself.
</entry_preconditions>

<context>
@AGENTS.md
@web/src/pages/LoginPage.tsx
@web/verification/browser-scenarios.json
@web/scripts/serve-browser-scenario.mjs
@.planning/phases/01-engineering-verification-foundation/DECISIONS.md
@.planning/phases/01-engineering-verification-foundation/TEST-MATRIX.md
@.planning/phases/01-engineering-verification-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and enforce the versioned copy/export contract</name>
  <files>web/verification/copy.zh-CN.json, web/verification/fixtures/zh-cn-export.csv, web/scripts/validate-copy-zh-cn.mjs</files>
  <behavior>
    - Reviewed JSX/user-visible attributes, errors, and export-contract headers match registered simplified-Chinese entries or exact narrow technical tokens.
    - Unregistered English, traditional-only variants, broad wildcard/regex allowlists, missing surfaces, and malformed CSV fail with distinct diagnostics.
    - Registry coverage labels each entry as foundation fixture or production surface so synthetic rows cannot imply product acceptance.
  </behavior>
  <action>Use the TypeScript Compiler API for JSX/TS literals and a real CSV parser path already available through standard project/runtime facilities; do not use a Han-character presence heuristic. Register `/login` visible/error copy and a synthetic export contract containing headers and rows. Keep allowlists exact and reviewed. Emit copy-contract subresults into OBL-FOUND-TRACE-003 evidence under D-02, D-06, D-07, and DR-01-008; never create a separate Phase 1 product-obligation evidence file.</action>
  <verify>
    <automated>node web/scripts/validate-copy-zh-cn.mjs --registry web/verification/copy.zh-CN.json --export-fixture web/verification/fixtures/zh-cn-export.csv --source web/src/pages/LoginPage.tsx</automated>
  </verify>
  <done>The valid registry/source/export fixture passes; every missing, untranslated, traditional-only, broad-allowlist, malformed, and mislabeled-production mutation fails; output explicitly identifies foundation scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove runtime login copy and destructive coverage</name>
  <files>web/package.json, web/playwright.config.ts, web/scripts/test-copy-zh-cn.mjs, web/test/phase01/chinese-copy.spec.ts</files>
  <behavior>
    - The runtime test loads `/login` from the Plan 05 scenario server, validates registered DOM/ARIA text, submits synthetic invalid credentials through the shared real-HTTP 401 responder, observes its exact marker, and asserts registered error copy via stable selectors.
    - Hidden untranslated runtime text, selector drift, absent error state, or unregistered DOM/ARIA content fails nonzero.
    - The mutation runner changes one invariant per case and asserts stable diagnostics.
  </behavior>
  <action>Consume the Plan 05 selector/scenario/server contracts instead of modifying `LoginPage.tsx` or the router. Register the non-watch `test:copy:zh-cn` script in `web/package.json` using web-working-directory paths; its exact first command is `playwright test test/phase01/chinese-copy.spec.ts`, followed by `node scripts/validate-copy-zh-cn.mjs --registry verification/copy.zh-CN.json --export-fixture verification/fixtures/zh-cn-export.csv --source src/pages/LoginPage.tsx` and `node scripts/test-copy-zh-cn.mjs` joined with fail-fast `&amp;&amp;`. Reconcile `web/playwright.config.ts` so that an absent `PHASE01_BASE_URL` selects one code-owned fixed loopback default for this isolated command, while a caller-supplied value remains restricted to a valid loopback URL for the Plan 05 free-port runner; no remote/default external origin or browser override is permitted. Link the runtime test block to CASE-FOUND-TRACE-003 and PW-FOUND-LOGIN-SMOKE, require the real-HTTP scenario 401/body/marker while clearly labelling it non-backend evidence, and combine AST/runtime/error/export-contract results under the trace evidence kernel. Do not add Playwright request interception. Include mutations for hidden text, English ARIA label, traditional-only copy, missing error selector, missing responder marker, missing export header, and overbroad allowlist.</action>
  <verify>
    <automated>npm --prefix web run test:copy:zh-cn</automated>
  </verify>
  <done>`web/package.json` owns one defined non-watch `test:copy:zh-cn` command that runs the exact Playwright file, copy validator, and mutation suite from the web cwd; `/login` runtime and all static/error/export contract checks pass; each destructive case fails exactly; the Phase 1 owner query remains seven and no product-level Chinese TODO/evidence is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Source/runtime/export text to registry | Visible text can be missing, hidden, untranslated, or over-allowlisted. |
| Synthetic fixture to product claim | Foundation evidence must not masquerade as complete production coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-01-19 | Tampering | registry/allowlist | mitigate | Exact versioned entries and destructive broad/missing mutation tests. |
| T-01-20 | Spoofing | product-coverage label | mitigate | Explicit fixture/production surface classification and DR-01-008 owner check. |
| T-01-21 | Information disclosure | runtime/export fixtures | mitigate | Synthetic values only and no production data or credentials. |
| T-01-SC | Tampering | dependencies | mitigate | Reuse audited project packages and standard runtimes; no new install. |
</threat_model>

<verification>
Run both task commands and verify the trace evidence envelope contains copy-contract subresults but no Phase 1 product-level Chinese completion artifact.
</verification>

<success_criteria>
- Copy validation covers source, `/login` runtime/error, and export-contract fixtures.
- Every omission or policy violation fails closed.
- Existing `/login` selectors are reused without router or LoginPage ownership conflict.
- Phase 56 retains complete product-acceptance responsibility.
</success_criteria>

<output>
Create `.planning/phases/01-engineering-verification-foundation/01-07-SUMMARY.md` after execution and leave it uncommitted.
</output>
