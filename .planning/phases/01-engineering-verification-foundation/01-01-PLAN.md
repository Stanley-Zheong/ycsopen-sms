---
phase: 01-engineering-verification-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/tools/verification-evidence.rb
  - .planning/tools/validate-verification-evidence.rb
  - .planning/tools/test-repository-verification.rb
  - .planning/phases/01-engineering-verification-foundation/EVIDENCE/schema/evidence-envelope.schema.json
  - .planning/phases/01-engineering-verification-foundation/EVIDENCE/schema/aggregate.schema.json
  - .planning/phases/01-engineering-verification-foundation/EVIDENCE/schema/tested-inputs.schema.json
  - .planning/phases/01-engineering-verification-foundation/EVIDENCE/fixtures/evidence-mutations.json
  - scripts/verify-phase-01
  - scripts/lib/phase-01/run_checks.rb
  - scripts/lib/phase-01/test_run_checks.rb
autonomous: true
requirements:
  - REQ-NFR-COMPATIBILITY
obligations:
  - OBL-FOUND-TRACE-003
entry_blockers:
  - "This plan's original implementation is recorded, but its output requires regression under the revised contract after Plan 00 independent ENTRY PASS and the 13-plan bootstrap; do not rerun its business slice while entry is BLOCKED."
must_haves:
  truths:
    - "Per D-01 and D-02, aggregate PASS is possible only when every required child result validates as PASS; FAIL or BLOCKED returns nonzero and leaves the linked TODO open."
    - "Per D-04 and D-06, terminal status is concise and machine-readable evidence records honest environment identity without relabelling a browser engine."
    - "D-03 UI reconciliation, D-05 no-schema scope, and the remaining D-01 through D-06 contracts are represented as allowlisted child-check interfaces rather than business behavior."
  artifacts:
    - path: .planning/tools/verification-evidence.rb
      provides: "Versioned evidence envelope, redaction, checksum, atomic write, and deterministic reduction library"
    - path: .planning/tools/validate-verification-evidence.rb
      provides: "Independent fail-closed subject-manifest, envelope, and aggregate validator"
    - path: .planning/phases/01-engineering-verification-foundation/EVIDENCE/schema/tested-inputs.schema.json
      provides: "Canonical tested-input manifest entry and exclusion contract"
    - path: scripts/verify-phase-01
      provides: "Single repository-root Phase 1 verification command"
  key_links:
    - from: scripts/verify-phase-01
      to: scripts/lib/phase-01/run_checks.rb
      via: "argv-only invocation with unchanged aggregate exit status"
    - from: scripts/lib/phase-01/run_checks.rb
      to: .planning/tools/validate-verification-evidence.rb
      via: "validate every child envelope before status reduction"
---

<objective>
Create the fail-closed evidence kernel and one root verification entry required by engineering-verification-foundation-03 and engineering-verification-foundation-06.

Purpose: Give every later Phase 1 lane one deterministic status protocol that preserves completed diagnostics and cannot convert an unavailable environment into PASS, implementing D-01, D-02, D-04, and D-06 while reserving D-03 consumers and enforcing D-05.
Output: Standard-library Ruby evidence validation, destructive fixtures, an allowlisted runner, and `./scripts/verify-phase-01`.

Schema migrations: none.
</objective>

<execution_context>
@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@/Users/laosanzheong/.codex/gsd-core/templates/summary.md
</execution_context>

<entry_preconditions>
This plan's functional implementation predates DR-01-017 and is not repeated. Its evidence-kernel output must be regression-checked by the later root/evidence plans only after Plan 00 produces entry-only `local-chrome-entry.json`, an independent reviewer records current ENTRY PASS, and the revised bootstrap validates all 13 plans with zero exit. Attempt 3 source-chain artifacts are superseded history and cannot authorize entry or become tested-subject inputs.
</entry_preconditions>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/EXECUTION-STANDARD.md
@.planning/phases/01-engineering-verification-foundation/01-SPEC.md
@.planning/phases/01-engineering-verification-foundation/01-CONTEXT.md
@.planning/phases/01-engineering-verification-foundation/01-RESEARCH.md
@.planning/phases/01-engineering-verification-foundation/01-PATTERNS.md
@.planning/phases/01-engineering-verification-foundation/DESIGN.md
@.planning/phases/01-engineering-verification-foundation/DECISIONS.md
@.planning/phases/01-engineering-verification-foundation/TEST-MATRIX.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the canonical tested-subject and versioned evidence protocols</name>
  <files>.planning/tools/verification-evidence.rb, .planning/tools/validate-verification-evidence.rb, .planning/tools/test-repository-verification.rb, .planning/phases/01-engineering-verification-foundation/EVIDENCE/schema/evidence-envelope.schema.json, .planning/phases/01-engineering-verification-foundation/EVIDENCE/schema/aggregate.schema.json, .planning/phases/01-engineering-verification-foundation/EVIDENCE/schema/tested-inputs.schema.json, .planning/phases/01-engineering-verification-foundation/EVIDENCE/fixtures/evidence-mutations.json</files>
  <behavior>
    - A canonical `tested-inputs.json` contains stable repository-relative entries sorted by path; every entry has path, file mode, SHA-256, and one code-owned role from implementation, test, config, contract, or validator.
    - The subject builder independently derives the required union from code-owned per-check input registries. It explicitly excludes the subject manifest itself, generated execution-EVIDENCE summaries/artifacts (including entry-only `local-chrome-entry.json` and Plan 06 `local-chrome-runtime.json`), TODO/SUMMARY, later review records, and delivery metadata; mutable JSON cannot exclude or conceal a source, test, config, contract, or validator. Lifecycle validation checks the independent ENTRY verdict separately; the browser envelope checksums and links only Plan 06 runtime/smoke evidence without making generated evidence self-referential.
    - A valid envelope omits final commit identity and instead binds `subject_manifest_path`, `subject_manifest_digest`, and `tested_subject_digest` together with schema/run/check/phase/obligation/case identity, argv array, repository-relative cwd, UTC timestamps, sanitized environment identity, status, exit code, stable errors, diagnostics, and artifact checksums.
    - Missing/extra subject input, content or mode mismatch, illegal exclusion, wrong/stale subject, manifest-digest mismatch, malformed/stale-running evidence, checksum mismatch, secret-bearing output, PASS-with-nonzero, FAIL, and BLOCKED all return nonzero with distinct stable error IDs.
    - FAIL dominates aggregate reduction; otherwise BLOCKED dominates; only all required PASS results reduce to PASS.
  </behavior>
  <action>Implement DR-01-011 as revised by DR-01-016 and DR-01-017 in the standard-library Ruby evidence module using JSON, Digest, Pathname, Time, and atomic temporary-file rename patterns from `planning-validator-support.rb`. Canonicalize the required source subject as stable path/mode/SHA-256/role entries and hash the entries without hashing the manifest itself. Keep inclusion roles and generated-metadata exclusions in schema/code, not producer-controlled JSON; remove all hard-coded admission/probe/CfT-attestation inputs. Exclude both generated `local-chrome-entry.json` and `local-chrome-runtime.json` from source membership. Lifecycle/bootstrap validation owns the entry artifact and independent ENTRY review; the browser envelope/evidence manifest separately validates and checksums only Plan 06 `local-chrome-runtime.json` path/version/brand/viewport/launch/smoke facts. Reject unknown roles, path overlap, missing/extra source inputs, illegal exclusions, content/mode drift, wrong runtime artifact checksum, path/brand/version/viewport/launch inconsistency, and stale subject linkage. Enforce repository containment and regular files; serialize only allowlisted non-secret facts; redact before hashing; never accept producer PASS without independently recomputing subject digests, exit/status consistency, artifact checksums, and exact case set. Preserve earlier child envelopes after later failure/interruption. This implements D-01, D-02, D-04, D-06, D-08, DR-01-011, DR-01-016, and DR-01-017; it creates no application data or migration per D-05.</action>
  <verify>
    <automated>/usr/bin/env ruby .planning/tools/test-repository-verification.rb</automated>
  </verify>
  <done>The canonical subject, envelope, and aggregate positive fixtures pass; every named subject/evidence mutation returns nonzero with its exact stable diagnostic; no committed envelope requires final commit identity; no fixture contains credentials, production payloads, phone numbers, message bodies, or absolute developer paths; OBL-FOUND-TRACE-003 remains open until the complete repository matrix passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the allowlisted evidence-preserving root orchestrator</name>
  <files>scripts/verify-phase-01, scripts/lib/phase-01/run_checks.rb, scripts/lib/phase-01/test_run_checks.rb</files>
  <behavior>
    - Unknown flags, missing tools, malformed child output, child FAIL, child BLOCKED, timeout, and interruption all return nonzero and retain every completed child envelope.
    - The wrapper accepts only documented selectors such as `--self-test`, `--all`, and `--timezone` plus an explicit evidence directory; it never evaluates command text from mutable JSON.
    - Terminal rows and aggregate JSON list the same stable ordered check IDs and evidence paths.
  </behavior>
  <action>Create a small strict root wrapper and a Ruby runner with a code-owned argv registry. Run all selected independent checks even when an earlier one fails, write each result immediately through `VerificationEvidence`, then independently validate and reduce them. Reject evidence directories outside the repository, unknown flags, duplicate check IDs, and empty selections. Include synthetic runner tests for child PASS/FAIL/BLOCKED, missing executable, timeout, interruption, and a later child failing after an earlier PASS. Do not register browser engine aliases as branded checks per D-04; later plans add concrete check argv to this stable interface.</action>
  <verify>
    <automated>/usr/bin/env ruby scripts/lib/phase-01/test_run_checks.rb &amp;&amp; ./scripts/verify-phase-01 --self-test --evidence-dir .planning/phases/01-engineering-verification-foundation/EVIDENCE</automated>
  </verify>
  <done>The root command returns zero only for the all-PASS self-test, returns nonzero for every destructive child condition, prints each completed result, and writes independently valid aggregate evidence without closing any TODO from a synthetic run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Child process to evidence validator | Child output is untrusted and cannot self-assert PASS. |
| Filesystem path to committed/CI evidence | Paths, content, checksums, size, and repository containment require validation. |
| Environment to diagnostics | Process environment may contain secrets and is never serialized wholesale. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-01-01 | Spoofing/Tampering | subject manifest and evidence envelopes | mitigate | Recompute code-owned subject membership, path/mode/SHA-256 entries, both subject digests, status/exit consistency, exact case set, schema, containment, and artifact checksums before reduction. |
| T-01-02 | Information disclosure | diagnostics/environment | mitigate | Explicit non-secret field allowlist, synthetic fixtures, redaction tests, and no command-string serialization. |
| T-01-03 | Denial of service/Repudiation | child runner | mitigate | Bounded process handling, immediate per-child atomic evidence, stable errors, and preserved partial results. |
| T-01-SC | Tampering | package installs | mitigate | This plan installs no package; the later Playwright install is permitted only by the recorded Package Legitimacy Audit. |
</threat_model>

<verification>
Run both focused test commands, then validate every emitted JSON with `.planning/tools/validate-verification-evidence.rb`. Confirm the working tree contains no build output or secrets and `git diff --check` passes. A BLOCKED local-Chrome entry condition is expected until the revised probe/review/bootstrap pass and must not be represented as plan completion.
</verification>

<success_criteria>
- Every selected child has a validated immutable result and diagnostic path.
- Aggregate status is deterministic and fail-closed.
- `./scripts/verify-phase-01` is the only public Phase 1 orchestration entry.
- D-01 through D-06 remain enforceable without adding business behavior or schema changes.
</success_criteria>

<output>
Create `.planning/phases/01-engineering-verification-foundation/01-01-SUMMARY.md` after execution, but do not create a task-level commit; all Phase 1 work is committed once at the final delivery plan.
</output>
