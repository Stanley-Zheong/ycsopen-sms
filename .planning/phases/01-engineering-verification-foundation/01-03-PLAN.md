---
phase: 01-engineering-verification-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - .planning/tools/validate-delivery-attestation.rb
  - .planning/tools/test-delivery-attestation.rb
  - .planning/tools/planning-validator-support.rb
  - .planning/tools/validate-phase-entry.rb
  - .planning/EXECUTION-STANDARD.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - REQ-NFR-COMPATIBILITY
obligations:
  - OBL-FOUND-TRACE-004
entry_blockers:
  - "This plan's original implementation is recorded; its delivery output is regression-checked only after Plan 00 independent ENTRY PASS and revised 13-plan bootstrap PASS."
must_haves:
  truths:
    - "Per D-01 and D-02 plus DR-01-004 and DR-01-011, one atomic Phase commit is proved remotely by a live annotated tag while committed evidence binds a canonical tested subject rather than its own commit ID."
    - "Per D-04 and D-06, branch, tag, target commit, tree, tested-subject digest, subject-manifest digest, evidence-manifest digest, PR/check locator, and PASS payload are independently resolved and reported without trusting labels."
    - "D-03 consumers use the same dependency proof, while D-05 keeps delivery tooling free of application schema changes."
    - "OBL-FOUND-TRACE-004 is fail-closed only when both the lifecycle destructive suite and delivery-attestation destructive suite execute under the exact TEST-MATRIX command."
  artifacts:
    - path: .planning/tools/validate-delivery-attestation.rb
      provides: "Live configured-remote branch and annotated-tag attestation validator"
    - path: .planning/tools/test-delivery-attestation.rb
      provides: "Local bare-remote positive and destructive attestation suite"
    - path: .planning/EXECUTION-STANDARD.md
      provides: "Canonical tested-subject, external delivery, and effective TODO contract"
  key_links:
    - from: .planning/tools/validate-phase-entry.rb
      to: .planning/tools/validate-delivery-attestation.rb
      via: "completed dependency validation resolves the declared delivery tag live"
    - from: .planning/tools/validate-delivery-attestation.rb
      to: configured Git remote
      via: "strict refs plus git ls-remote/fetch, annotated-tag peel, and target-tree subject recomputation"
---

<objective>
Synchronize the canonical tested-subject protocol with the accepted external annotated-attestation protocol for engineering-verification-foundation-04.

Purpose: Preserve one atomic Phase commit while proving its configured branch, exact commit/tree, tested-subject digest, subject-manifest digest, evidence-manifest digest, PR/check, and PASS state on the live remote under D-01 through D-06, DR-01-004, and DR-01-011.
Output: Hardened remote attestation validator, local bare-remote mutation suite, and synchronized lifecycle/dependency documentation and consumers.

Schema migrations: none.
</objective>

<execution_context>
@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@/Users/laosanzheong/.codex/gsd-core/templates/summary.md
</execution_context>

<entry_preconditions>
This plan's functional implementation is recorded and is not repeated. Its delivery output must later be regression-checked after Plan 00 entry-only evidence, independent ENTRY PASS, and revised 13-plan bootstrap PASS. The accepted Git delivery protocol is a plan contract, not current remote evidence. Attempt 3 browser-source artifacts are superseded and have no delivery-attestation role.
</entry_preconditions>

<context>
@AGENTS.md
@.planning/EXECUTION-STANDARD.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/tools/planning-validator-support.rb
@.planning/tools/validate-phase-entry.rb
@.planning/phases/01-engineering-verification-foundation/01-CONTEXT.md
@.planning/phases/01-engineering-verification-foundation/01-RESEARCH.md
@.planning/phases/01-engineering-verification-foundation/DECISIONS.md
@.planning/phases/01-engineering-verification-foundation/ITERATIONS.md
@.planning/phases/01-engineering-verification-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement live annotated delivery-attestation validation</name>
  <files>.planning/tools/validate-delivery-attestation.rb, .planning/tools/test-delivery-attestation.rb, .planning/tools/planning-validator-support.rb, .planning/tools/validate-phase-entry.rb</files>
  <behavior>
    - A valid protected delivery ref is an annotated tag whose peeled target equals the configured remote branch target and declared phase commit.
    - Tag payload phase, branch, commit, tree, tested-subject digest, subject-manifest digest, evidence-manifest digest, PR/check locator, and PASS status match independently computed values.
    - The validator reads `tested-inputs.json` from the peeled target commit, rebuilds the code-owned canonical path/mode/SHA-256/role set from that target tree, and requires exact equality with committed evidence and tag payload.
    - Missing or lightweight tags, moved tags, and mismatched remote, branch, target, tree, subject membership/content/mode/digests, evidence-manifest digest, PR locator, or required PR check fail nonzero with independent stable diagnostics.
  </behavior>
  <action>Implement the DR-01-004 and DR-01-011 validator using argv-based Git commands, a strict deterministic tag namespace, exact single-match remote ref parsing, annotated-tag peeling, repository/remote allowlists, and independent target-tree/evidence digest calculation. Never `eval`, interpolate credentials, or trust local tracking refs. Extend dependency evidence consumption so completed phases declare a delivery-tag locator and the validator resolves live branch/tag equality instead of searching `SUMMARY.md` for a literal self identity. From the peeled tag target, read the committed subject manifest, apply the schema/code-owned inclusion roles and generated-metadata exclusions, hash every allowed input's repository-relative path, file mode, and bytes, and compare `tested_subject_digest`, `subject_manifest_digest`, and `evidence_manifest_digest` against every committed envelope/review and the tag payload. Build a temporary repository plus local bare remote fixture; test the valid branch/tag/payload and each destructive mutation independently: missing/lightweight/moved tag, mismatched remote/branch/target/tree, wrong/stale subject, missing/extra input, changed content/mode, illegal exclusion, either subject digest mismatch, evidence-manifest mismatch, PR locator, and required PR-check state. The test may create multiple fixture commits, but production Phase 1 delivery remains one implementation commit plus an annotated tag object. Keep the authoritative OBL-FOUND-TRACE-004 boundary as the ordered conjunction of `test-phase-lifecycle.rb` and `test-delivery-attestation.rb`; neither suite may stand in for the other.</action>
  <verify>
    <automated>/usr/bin/env ruby .planning/tools/test-phase-lifecycle.rb &amp;&amp; /usr/bin/env ruby .planning/tools/test-delivery-attestation.rb &amp;&amp; /usr/bin/env ruby .planning/tools/test-planning-validators.rb</automated>
  </verify>
  <done>The valid bare-remote fixture passes only after branch and annotated tag are both visible and the target-tree subject recomputation matches every evidence/review/tag digest; every mutation returns nonzero with an exact stable diagnostic; dependency entry cannot accept local-only or self-referential evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize the governing delivery and effective-TODO contract</name>
  <files>.planning/EXECUTION-STANDARD.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Update only governing evidence/delivery semantics that conflict with DR-01-004 and DR-01-011. State that committed envelopes bind the canonical tested-input manifest and its two subject digests without requiring their final commit identity. State that the atomic commit records configured remote, full branch ref, and deterministic delivery-tag locator; after push, one annotated tag targets that commit and carries commit/tree/tested-subject/subject-manifest/evidence-manifest digests plus PR-check/PASS payload; the post-push validator reads the subject manifest from the target tree, recomputes permitted inputs, resolves live state, and closes only the reserved external-delivery TODO in the effective scoped query. Remove claims that `SUMMARY.md` or committed evidence contains or proves its own final identity. Preserve branch/PR delivery, bounded reviews, TODO-only completion, all 56 phase boundaries, and evidence-based status. Cite D-01, D-02, D-04, D-06, DR-01-004, and DR-01-011. Do not change roadmap scope, requirement ownership, phase count, business behavior, or schema decisions.</action>
  <verify>
    <automated>/usr/bin/env ruby .planning/tools/test-phase-lifecycle.rb &amp;&amp; /usr/bin/env ruby .planning/tools/test-delivery-attestation.rb &amp;&amp; /usr/bin/env ruby .planning/tools/test-planning-validators.rb &amp;&amp; ! rg -n "same commit.*own.*SHA|SUMMARY\.md.*literal.*SHA" .planning/EXECUTION-STANDARD.md .planning/ROADMAP.md .planning/STATE.md</automated>
  </verify>
  <done>Every governing artifact describes the same canonical-subject plus one-commit/annotated-tag protocol, future dependency entry consumes the recomputed target-tree attestation, no text requires same-commit self identity, and unrelated roadmap/state content is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Local repository to configured remote | Local commits and tracking refs do not establish remote visibility. |
| Mutable branch/tag names to immutable commit/tree | Ref names can move and payload can lie unless targets are resolved and recomputed. |
| PR/check locator to review truth | A locator must exist in the attestation but cannot replace final GSD/Claude evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-01-07 | Spoofing/Tampering | remote refs | mitigate | Strict ref policy, `git ls-remote`, safe fetch, annotated peel, branch/tag/commit/tree equality. |
| T-01-08 | Repudiation | attestation payload | mitigate | Recompute target-tree subject membership and all three digests; require phase, PR/check locator, and PASS fields. |
| T-01-09 | Elevation/Information disclosure | Git argv/remote | mitigate | No eval, validated configured remote, no credential logging, and synthetic bare-remote fixtures. |
| T-01-SC | Tampering | dependencies | mitigate | Git and standard-library Ruby only; no package installation. |
</threat_model>

<verification>
Run the lifecycle, delivery, and existing planning validator suites. Inspect the resulting diff to confirm only the accepted delivery contradiction changed. Do not create or push the production Phase 1 tag in this plan; Plan 12 performs it after all reviews, evidence, and non-delivery TODOs pass.
</verification>

<success_criteria>
- One implementation commit plus one external annotated tag is machine-verifiable.
- Live branch/tag/commit/tree/tested-subject/subject-manifest/evidence-manifest/PR-check equality is mandatory.
- Dependency gates fail closed on absent, moved, lightweight, or mismatched attestations.
- Governing docs no longer require a commit to contain its own SHA.
</success_criteria>

<output>
Create `.planning/phases/01-engineering-verification-foundation/01-03-SUMMARY.md` after execution. Keep it uncommitted until the final atomic Phase 1 commit.
</output>
