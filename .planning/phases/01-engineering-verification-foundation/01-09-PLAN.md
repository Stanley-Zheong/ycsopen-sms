---
phase: 01-engineering-verification-foundation
plan: 09
type: execute
wave: 5
depends_on: [01-00, 01-02, 01-03, 01-04, 01-06, 01-07, 01-08]
files_modified:
  - .github/workflows/ci.yml
  - scripts/lib/phase-01/run_checks.rb
  - .planning/phases/01-engineering-verification-foundation/01-VALIDATION.md
autonomous: true
requirements: [REQ-NFR-COMPATIBILITY]
obligations: [OBL-FOUND-TRACE-001, OBL-FOUND-TRACE-002, OBL-FOUND-TRACE-003, OBL-FOUND-TRACE-004, OBL-FOUND-UI-DRIFT-001, OBL-FOUND-UI-DRIFT-002, OBL-NFR-BROWSER]
entry_blockers:
  - "BLOCKED until the standard-path local Chrome probe, independent rerun, revised bootstrap, and final Phase 1 entry gate pass; missing/non-Google/unlaunchable Chrome must remain BLOCKED, not skipped green."
must_haves:
  truths:
    - "The root command and CI consume the same code-owned registry for all seven obligation rows and every supporting lane. Local --all invokes frontend lint and the exact owned copy command; portable --ci invokes the copy command's static validator/mutation components and proves the exact browser argv remains registered without launching it."
    - "Real services, structural diagnostics, copy/timezone contracts, and the single current local Google Chrome 1440x900 run preserve FAIL/BLOCKED evidence and never become skipped PASS."
    - "CI runs the portable non-browser registry and validates browser evidence structure without downloading a browser; local pre-push execution is the only source of Chrome runtime PASS."
    - "Validation status becomes compliant only after every referenced command and destructive fixture exists and passes."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Java 21, Node 20+, real-service, structural, trace, and portable browser-evidence validation gates"
    - path: scripts/lib/phase-01/run_checks.rb
      provides: "Complete allowlisted Phase 1 child registry"
    - path: .planning/phases/01-engineering-verification-foundation/01-VALIDATION.md
      provides: "Executable Nyquist map for exactly seven owned obligations"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/verify-phase-01
      via: "same root/focused repository commands with artifacts uploaded on failure"
---

<objective>
Wire every implemented verification lane into the common root command and CI without mixing in final evidence review or delivery.

Purpose: Keep portable local/remote checks identical while reserving the standard-path Chrome launch for the local pre-push acceptance command.
Output: Root registry, CI jobs, and finalized validation map.

Schema migrations: none.
</objective>

<execution_context>
@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@/Users/laosanzheong/.codex/gsd-core/templates/summary.md
</execution_context>

<entry_preconditions>
Require Plan 00 entry-only fixed-path version/headless-synthetic-page evidence, final independent ENTRY review PASS, and revised 13-plan bootstrap PASS. Plan 06 must then provide the separate 1440x900 Playwright runtime/smoke evidence. CI configuration is not a substitute for either executable local-Chrome result and must not download another browser to manufacture it.
</entry_preconditions>

<context>
@AGENTS.md
@.planning/phases/01-engineering-verification-foundation/01-VALIDATION.md
@.planning/phases/01-engineering-verification-foundation/TEST-MATRIX.md
@.planning/phases/01-engineering-verification-foundation/01-02-SUMMARY.md
@.planning/phases/01-engineering-verification-foundation/01-03-SUMMARY.md
@.planning/phases/01-engineering-verification-foundation/01-04-SUMMARY.md
@.planning/phases/01-engineering-verification-foundation/01-06-SUMMARY.md
@.planning/phases/01-engineering-verification-foundation/01-07-SUMMARY.md
@.planning/phases/01-engineering-verification-foundation/01-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register every focused lane in the fail-closed root command</name>
  <files>scripts/lib/phase-01/run_checks.rb, .planning/phases/01-engineering-verification-foundation/01-VALIDATION.md</files>
  <behavior>
    - The code-owned registry covers catalog/trace, the exact combined lifecycle-plus-delivery TRACE-004 lane, Java unit, web install/lint/test/build, UI AST, `/login`, MySQL, Redis, the exact `npm --prefix web run test:copy:zh-cn` command, timezone contract, and the single local-Chrome runtime/smoke validator.
    - Unknown/missing/duplicate commands, missing tools, or any child FAIL/BLOCKED returns nonzero while preserving completed evidence.
    - The validation map names exactly seven owned obligations and no product-level Chinese/timezone row.
  </behavior>
  <action>Add explicit argv arrays and code-owned subject-input roles only; never load executable command strings or arbitrary subject exclusions from manifests. Register OBL-FOUND-TRACE-004 as one ordered fail-closed lane that executes `/usr/bin/env ruby .planning/tools/test-phase-lifecycle.rb` and `/usr/bin/env ruby .planning/tools/test-delivery-attestation.rb`; preserve both child results and fail the obligation if either is absent or nonzero. Under OBL-FOUND-TRACE-003, register `npm --prefix web run lint` as required, `npm --prefix web run test:copy:zh-cn` as the sole local copy command, its static validator/mutation components as the portable CI copy lane, and the timezone contract. The registry and self-tests must prove the exact local copy argv remains present even though `--ci` does not execute its browser segment. Register implementation, tests, configs, contracts, and validators so Plan 01 builds the complete canonical source subject. Remove every historical browser-source/admission/probe/attestation file from the subject registry and lifecycle fixtures. Wire OBL-NFR-BROWSER only to Plan 06's `local-chrome-runtime.json` probe/smoke validator. The local `--all` path requires the exact copy command and real Chrome command; a portable `--ci` selector runs all non-browser checks, the copy static/mutation components, and validates any supplied browser artifact without claiming a runtime PASS. Update validation flags only after focused commands and fixtures pass; keep TODOs open.</action>
  <verify>
    <automated>/usr/bin/env ruby scripts/lib/phase-01/test_run_checks.rb &amp;&amp; /usr/bin/env ruby .planning/tools/test-phase-lifecycle.rb &amp;&amp; /usr/bin/env ruby .planning/tools/test-delivery-attestation.rb &amp;&amp; ./scripts/verify-phase-01 --self-test --evidence-dir .planning/phases/01-engineering-verification-foundation/EVIDENCE</automated>
  </verify>
  <done>The root registry reaches every lane in stable order; lint and the exact owned copy script are present under TRACE-003; all destructive runner cases pass; the validation map selects seven obligations; missing local-Chrome execution remains nonzero and CI cannot substitute a download or another browser.</done>
</task>

<task type="auto">
  <name>Task 2: Make CI execute the portable registry and preserve failure diagnostics</name>
  <files>.github/workflows/ci.yml</files>
  <action>Retain Java 21 and Node 20+, invoke the portable root/focused repository commands rather than duplicating logic, and require `npm --prefix web run lint` plus the portable static validator/mutation components of the copy contract under TRACE-003. The shared registry/self-test must also prove the exact local `npm --prefix web run test:copy:zh-cn` argv is present, but CI must not execute that browser-bearing command. Pin MySQL/Redis digests and upload redacted diagnostics on failure. Do not install/download a browser, invoke ChromeDriver, or add a provider/tunnel/remote-host/VM/non-Chrome job. CI runs the non-browser selector and the local-Chrome evidence schema/destructive validator, but does not claim it launched the user's Mac browser. The required exact copy/browser runs remain local pre-push TEST-MATRIX commands; their absence keeps phase acceptance open. CI may add runtime commit SHA only to non-committed diagnostics; committed envelopes remain bound to the canonical source subject.</action>
  <verify>
    <automated>/usr/bin/env ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")' &amp;&amp; /usr/bin/env ruby .planning/tools/test-repository-verification.rb</automated>
  </verify>
  <done>The workflow parses, invokes every portable registry command, preserves diagnostics on failure, downloads no browser, makes no local-Chrome runtime claim, and cannot close the phase while the required local run is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Repository runner to CI status | YAML can diverge, skip, or hide required failures. |
| Local-only Chrome execution to CI status | A portable CI job must not fabricate or replace the required local browser run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-01-25 | Tampering | CI registry | mitigate | Same code-owned portable argv, explicit required jobs, workflow parse/self-tests, and no embedded logic. |
| T-01-26 | Information disclosure | CI artifacts | mitigate | Redaction, synthetic fixtures, and access controls for screenshots/traces; no browser secret. |
| T-01-27 | Spoofing | skipped job status | mitigate | Required unavailable lane emits BLOCKED/nonzero and cannot be treated as PASS. |
| T-01-SC | Tampering | dependencies/images | mitigate | Audited Playwright plus official service images pinned by digest. |
</threat_model>

<verification>
Run both tasks, project Java/web checks, YAML parsing, root runner tests, and a missing-local-Chrome-evidence fixture. Do not create review, final evidence, TODO, commit, tag, or PR state in this plan.
</verification>

<success_criteria>
- Local and CI share the same code-owned non-browser commands; only the local pre-push path executes the standard-path Chrome.
- Every supporting lane, including frontend lint and the owned copy script, is reachable and fail-closed.
- Required browser execution cannot be skipped green.
- Validation documentation represents exactly seven owned obligations.
</success_criteria>

<output>
Create `.planning/phases/01-engineering-verification-foundation/01-09-SUMMARY.md` after execution and leave it uncommitted.
</output>
