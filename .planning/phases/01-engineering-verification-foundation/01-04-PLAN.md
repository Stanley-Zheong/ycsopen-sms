---
phase: 01-engineering-verification-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - web/scripts/validate-ui-drift.mjs
  - web/scripts/test-ui-drift-validator.mjs
  - web/verification/ui-manifest.schema.json
  - web/verification/ui-manifest.json
  - web/verification/row-key-registry.schema.json
  - web/verification/row-key-registry.json
  - web/verification/fixtures/ui-drift-cases.json
autonomous: true
requirements:
  - REQ-NFR-COMPATIBILITY
obligations:
  - OBL-FOUND-UI-DRIFT-001
  - OBL-FOUND-UI-DRIFT-002
entry_blockers:
  - "This plan's original implementation is recorded; its UI-drift output is regression-checked only after Plan 00 independent ENTRY PASS and revised 13-plan bootstrap PASS."
must_haves:
  truths:
    - "Per D-03 and DR-01-006, route, page, component, selector, row contract, and Playwright action/assertion are compared as normalized relation tuples in both directions."
    - "Per D-02 and D-04, comment/string/dead-source/engine-alias evidence cannot close a UI or browser relation."
    - "Per D-01, D-05, D-06, and D-07, destructive AST evidence is machine-readable, TODO-neutral until PASS, and introduces no business behavior, product compatibility claim, or schema migration."
  artifacts:
    - path: web/scripts/validate-ui-drift.mjs
      provides: "TypeScript Compiler API route/JSX/Playwright relation extractor and comparator"
    - path: web/verification/ui-manifest.json
      provides: "Versioned page-scoped route/component/selector/row relation contract"
    - path: web/scripts/test-ui-drift-validator.mjs
      provides: "Positive and one-mutation bidirectional drift suite"
    - path: web/verification/row-key-registry.schema.json
      provides: "Versioned allow/deny contract for reviewed non-sensitive immutable row-key classes"
  key_links:
    - from: web/src/router/routes.tsx
      to: web/verification/ui-manifest.json
      via: "normalized navigable leaf route and routed-component tuples"
    - from: web/verification/ui-manifest.json
      to: web test sources
      via: "exact route, selector action/assertion, obligation, case, and Playwright relation"
---

<objective>
Implement engineering-verification-foundation-05 as a TypeScript-AST relation validator with the semantic repeated-row law.

Purpose: Make missing and stale routes, manifest pages, routed components, rendered test IDs, row metadata, and Playwright locators fail in both directions under D-01 through D-06, DR-01-003, and DR-01-006.
Output: Versioned UI manifest/schema, AST validator, and destructive fixture suite.

Schema migrations: none.
</objective>

<execution_context>
@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@/Users/laosanzheong/.codex/gsd-core/templates/summary.md
</execution_context>

<entry_preconditions>
This plan's functional implementation is recorded and is not repeated. Its UI-drift output must later be regression-checked after Plan 00 entry-only evidence, independent ENTRY PASS, and revised 13-plan bootstrap PASS. Static relations never substitute for the separate real local-Chrome smoke-and-visual closure owned by Plans 05-06.
</entry_preconditions>

<context>
@AGENTS.md
@.planning/UI-TEST-CONTRACT.md
@.planning/tools/validate-ui-contract.rb
@.planning/tools/planning-validator-support.rb
@web/src/router/routes.tsx
@.planning/phases/01-engineering-verification-foundation/01-SPEC.md
@.planning/phases/01-engineering-verification-foundation/01-CONTEXT.md
@.planning/phases/01-engineering-verification-foundation/01-RESEARCH.md
@.planning/phases/01-engineering-verification-foundation/01-PATTERNS.md
@.planning/phases/01-engineering-verification-foundation/DECISIONS.md
@.planning/phases/01-engineering-verification-foundation/TEST-MATRIX.md
@.planning/phases/01-engineering-verification-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and extract normalized UI relation tuples</name>
  <files>web/scripts/validate-ui-drift.mjs, web/verification/ui-manifest.schema.json, web/verification/ui-manifest.json, web/verification/row-key-registry.schema.json, web/verification/row-key-registry.json</files>
  <behavior>
    - Nested `createBrowserRouter` objects resolve absolute/relative paths, navigable leaves, index redirects, redirect targets, parameter patterns, and routed component imports.
    - JSX `data-testid` values and Playwright test/title/goto/getByTestId/action-or-assertion calls become source-located relation tuples; unknown computed syntax fails as unsupported.
    - Missing, foreign/stale, duplicate, dead-component, dead-locator, and wrong-route tuples are diagnosed separately.
  </behavior>
  <action>Use the repository's TypeScript 5.6.2 Compiler API, not regex or grep, to parse router objects, imports, JSX attributes, and Playwright calls. Define a versioned JSON schema whose records are page-scoped and include full route, routed component source, selector, obligation, requirement, behavior, catalog test/layer, Case ID, Playwright ID, and optional row contract. Create the versioned row-key registry required by DR-01-003: only explicitly reviewed synthetic/non-sensitive immutable business-key classes are allowed; phone numbers, database IDs, localized labels, mutable names/values, credentials, and message content are denied. Normalize DR-01-006 redirect/index/parameter semantics exactly; reject nonliteral dynamic construction with a source-located `UI_UNSUPPORTED_SYNTAX` result. Populate only synthetic fixture relations plus the existing `/login` production route facts; do not add `/verification/*`, invent Phase 2's full UI registry, or claim placeholder routes as implemented business pages. Emit Plan 01 evidence under D-02 and D-06.</action>
  <verify>
    <automated>node web/scripts/validate-ui-drift.mjs --manifest web/verification/ui-manifest.json --routes web/src/router/routes.tsx --check-static</automated>
  </verify>
  <done>The manifest validates against its schema; every navigable declared tuple has an exact source-located reverse relation; redirects and parameter patterns follow DR-01-006; unknown dynamic syntax returns nonzero rather than being guessed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove every bidirectional drift and semantic row-key failure</name>
  <files>web/scripts/test-ui-drift-validator.mjs, web/verification/fixtures/ui-drift-cases.json</files>
  <behavior>
    - One known-good synthetic TS/TSX/manifest/spec graph passes.
    - Missing and stale route, page, component, DOM selector, and Playwright locator mutations fail in both directions with exact error IDs and locations.
    - A repeated row passes only with a constant semantic `data-testid`, declared immutable non-sensitive key class, and separate `data-row-key`; value-derived selectors and forbidden key classes fail.
  </behavior>
  <action>Create a table-driven fixture document containing source text and one named mutation per case. Cover comment/string-only fakes, prefix collisions, duplicate tuples, dead component, dead locator, route without selector closure, nonliteral route/test ID, template-literal/data-derived test ID, missing row metadata, missing separate key attribute, and phone/database-ID/localized-label/mutable-name key classes. The runner writes each case into an isolated temporary directory, invokes the production validator via argv, and asserts exact nonzero status plus stable diagnostic. Constant semantic selectors use synthetic non-sensitive business keys only, implementing D-03 and DR-01-003 without leaking any row value.</action>
  <verify>
    <automated>node web/scripts/test-ui-drift-validator.mjs</automated>
  </verify>
  <done>The positive relation graph passes and every isolated mutation fails with its expected direction-specific diagnostic; both OBL-FOUND-UI-DRIFT TODOs remain open until Plan 05 executes the linked route-selector browser closure and final evidence validates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| TypeScript/JSX source to relation graph | Source can contain dead code, comments, strings, computed syntax, and misleading occurrences. |
| Manifest to production/test sources | A human-edited manifest can be stale, duplicated, or scoped to the wrong route. |
| Row data to automation attributes | Mutable or sensitive values must not become selectors or evidence names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-01-10 | Spoofing/Tampering | AST relations | mitigate | Compiler API node-kind validation, import/route resolution, exact tuples, and source locations. |
| T-01-11 | Information disclosure | repeated-row attributes | mitigate | Constant semantic test ID, separate classified synthetic key, forbidden key-class fixtures. |
| T-01-12 | Repudiation | manifest drift | mitigate | Bidirectional missing/foreign/duplicate sets and checksum-bound evidence. |
| T-01-SC | Tampering | packages | mitigate | Reuse the already-installed TypeScript package; no new install in this plan. |
</threat_model>

<verification>
Run both task commands and confirm each emitted obligation summary validates with Plan 01's evidence validator. Static PASS alone cannot close either UI obligation; Plan 05 must execute the exact route→rendered-selector action/assertion chain.
</verification>

<success_criteria>
- Missing and stale relations fail in both directions.
- Dynamic rows use semantic selectors and separate non-sensitive key identity.
- Unsupported dynamic syntax fails closed.
- No Phase 2 UI inventory, visual design, business page implementation, or schema change is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-engineering-verification-foundation/01-04-SUMMARY.md` after execution and leave it for the single final Phase 1 commit.
</output>
