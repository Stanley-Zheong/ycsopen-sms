---
phase: 01-engineering-verification-foundation
plan: 06
type: execute
wave: 4
depends_on: [01-00, 01-05]
files_modified:
  - web/playwright.config.ts
  - web/scripts/probe-local-chrome.mjs
  - web/scripts/run-local-chrome-smoke.mjs
  - web/scripts/validate-local-chrome-evidence.mjs
  - web/scripts/test-local-chrome-evidence.mjs
  - web/verification/local-chrome-runtime.schema.json
  - .planning/phases/01-engineering-verification-foundation/EVIDENCE/local-chrome-runtime.json
autonomous: true
requirements: [REQ-NFR-COMPATIBILITY]
obligations: [OBL-FOUND-TRACE-003, OBL-NFR-BROWSER]
entry_blockers:
  - "BLOCKED until the revised repository entry probe and an independent reviewer both validate the standard-path local Google Chrome, and bootstrap-phase-01.rb exits zero under DR-01-016."
must_haves:
  truths:
    - "Per D-04, D-08, and DR-01-016, exactly one current local Google Chrome execution runs LOGIN-SMOKE-V1 at 1440x900; version 151.0.7922.174 is an observed fact, not a pinned requirement."
    - "Runtime evidence records the canonical executable path, Google Chrome brand, full and major version, fixed command identity, viewport, successful Playwright launch, scenario result, and artifact checksums."
    - "The browser observes the real repository-owned HTTP 401/body/marker, selector actions/assertions, console/page-error policy, and canonical LOGIN-CARD-IN-VIEWPORT-V2 geometry/clipping/style/five-point checks; screenshot presence alone never passes."
    - "A missing path, non-regular executable, non-Google brand, failed version command, failed Playwright launch, wrong viewport, another browser, or incomplete evidence is BLOCKED/FAIL and nonzero."
    - "No browser or driver download, ChromeDriver process, current/previous pair, viewport matrix, provider, tunnel, secret, remote host, or VM exists."
  artifacts:
    - path: web/scripts/probe-local-chrome.mjs
      provides: "Fixed-path local Google Chrome path/version/brand/Playwright-launch probe"
    - path: web/scripts/run-local-chrome-smoke.mjs
      provides: "Single 1440x900 LOGIN-SMOKE-V1 and visual execution"
    - path: web/scripts/validate-local-chrome-evidence.mjs
      provides: "Independent runtime, semantic, visual, and artifact validator"
    - path: .planning/phases/01-engineering-verification-foundation/EVIDENCE/local-chrome-runtime.json
      provides: "Execution-time local Chrome runtime and smoke/visual facts"
  key_links:
    - from: web/scripts/probe-local-chrome.mjs
      to: /Applications/Google Chrome.app/Contents/MacOS/Google Chrome
      via: "code-owned canonical path, fixed argv --version, and Playwright launch"
    - from: web/scripts/run-local-chrome-smoke.mjs
      to: web/verification/browser-scenarios.json
      via: "LOGIN-SMOKE-V1 actions/assertions and LOGIN-CARD-IN-VIEWPORT-V2"
    - from: .planning/phases/01-engineering-verification-foundation/EVIDENCE/local-chrome-runtime.json
      to: EVIDENCE/OBL-NFR-BROWSER.json
      via: "independent schema, runtime, scenario, visual, and checksum validation"
---

<objective>
Execute and validate one real `/login` smoke-and-visual scenario with the current Google Chrome already installed at the standard macOS path.

Purpose: Close OBL-NFR-BROWSER with truthful runtime evidence while eliminating downloaded browsers, drivers, version matrices, and external browser infrastructure.
Output: Fixed-path runtime probe, single local-Chrome runner, destructive validator, schema, and `local-chrome-runtime.json`.

Schema migrations: none.
</objective>

<execution_context>
@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@/Users/laosanzheong/.codex/gsd-core/templates/summary.md
</execution_context>

<entry_preconditions>
The obsolete Attempt 3 browser-source admission/probe/attestation chain cannot authorize this plan. Before execution, Plan 00 must produce valid entry-only `EVIDENCE/local-chrome-entry.json` from the fixed-path version and isolated-profile headless synthetic-page probe, an independent reviewer must record current ENTRY PASS, the revised plan checker must report no blocker, and the real bootstrap must validate all 13 plans with zero exit. This plan neither creates nor rewrites entry evidence or ENTRY-REVIEW; it owns only Playwright `local-chrome-runtime.json` and the `/login` 1440x900 smoke/visual evidence. The accepted Chrome version is whatever the standard path reports when the command runs.
</entry_preconditions>

<context>
@AGENTS.md
@web/verification/browser-scenarios.json
@web/scripts/serve-browser-scenario.mjs
@web/scripts/validate-browser-scenario.mjs
@.planning/phases/01-engineering-verification-foundation/01-CONTEXT.md
@.planning/phases/01-engineering-verification-foundation/DECISIONS.md
@.planning/phases/01-engineering-verification-foundation/TEST-MATRIX.md
@.planning/phases/01-engineering-verification-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Probe and launch the standard-path local Google Chrome</name>
  <files>web/playwright.config.ts, web/scripts/probe-local-chrome.mjs, web/scripts/test-local-chrome-evidence.mjs, web/verification/local-chrome-runtime.schema.json, .planning/phases/01-engineering-verification-foundation/EVIDENCE/local-chrome-runtime.json</files>
  <behavior>
    - The canonical executable is exactly `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`, a regular executable, and no environment/manifest value can substitute another path.
    - Fixed argv `--version` must match `Google Chrome <four-part-version>`; the major is derived from that execution-time value.
    - Playwright launches and closes that same browser at 1440x900 and records a successful session without ChromeDriver.
    - Missing path, symlink/non-regular/non-executable file, wrong brand/version grammar, launch failure, wrong viewport, or another-browser configuration returns stable BLOCKED/nonzero diagnostics.
  </behavior>
  <action>Define one strict schema and table-driven fixture suite first. Implement a Node probe using filesystem canonical-path checks, `spawn`/`execFile` with fixed argv and `shell: false`, bounded output, and Playwright launch through the exact executable or `channel: "chrome"` only after proving it resolves to the standard path. Write `local-chrome-runtime.json` atomically with schema/run/command/path/brand/full_version/major/viewport/launch/status/diagnostic fields and no credentials or local profile contents. Record the observed version rather than comparing it to 151 or any other pinned value. The positive fixture may inject a code-owned fake process adapter; only the real command produces acceptance evidence. Never read, create, or modify `local-chrome-entry.json` or `ENTRY-REVIEW.md` in this plan. Remove all active browser-source admission, archive checksum, driver, two-version, and matrix assumptions from this lane per DR-01-016 and DR-01-017.</action>
  <verify>
    <automated>node web/scripts/test-local-chrome-evidence.mjs --case probe &amp;&amp; node web/scripts/probe-local-chrome.mjs --output .planning/phases/01-engineering-verification-foundation/EVIDENCE/local-chrome-runtime.json</automated>
  </verify>
  <done>The real standard-path Google Chrome reports a valid execution-time version and launches at 1440x900; the runtime JSON validates; every path/brand/version/launch/viewport mutation fails distinctly; no download or driver process occurs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute and independently validate the single login smoke-and-visual result</name>
  <files>web/scripts/run-local-chrome-smoke.mjs, web/scripts/validate-local-chrome-evidence.mjs, web/scripts/test-local-chrome-evidence.mjs, .planning/phases/01-engineering-verification-foundation/EVIDENCE/local-chrome-runtime.json</files>
  <behavior>
    - The runner starts the Plan 05 local-direct scenario server, verifies matching subject/scenario health, and launches only the probed local Chrome at 1440x900.
    - The browser navigates to `/login`, proves required selectors visible, fills synthetic credentials, submits, observes exact 401/body/marker, asserts the stable Chinese error, and executes the canonical visual rule.
    - Evidence contains screenshot, DOM/ancestor/hit-test observations, browser transcript, console/page errors, scenario/rule/subject linkage, and artifact checksums for exactly one run.
    - Duplicate runs masquerading as a matrix, request interception, runner-only HTTP proof, another browser, screenshot-only evidence, stale runtime facts, or any missing semantic/visual field fails nonzero.
  </behavior>
  <action>Consume the single non-watch `test:browser:compat` command registered by Plan 05. Start the repository server with fixed validated argv, require loopback health to match the canonical tested subject and scenario digest, then execute LOGIN-SMOKE-V1 through the same browser proven in Task 1. Do not intercept requests. Extend `local-chrome-runtime.json` or atomically replace it with the complete run facts while retaining the probe fields. Independently recompute schema, path/version/brand/viewport, subject/scenario/rule links, browser-observed response, visual observations, and artifact checksums. Add one mutation for every required field and prohibited substitution. Generate OBL-NFR-BROWSER evidence only after this validator passes.</action>
  <verify>
    <automated>node web/scripts/test-local-chrome-evidence.mjs &amp;&amp; npm --prefix web run test:browser:compat &amp;&amp; node web/scripts/validate-local-chrome-evidence.mjs --runtime .planning/phases/01-engineering-verification-foundation/EVIDENCE/local-chrome-runtime.json --scenario web/verification/browser-scenarios.json</automated>
  </verify>
  <done>Exactly one real standard-path Google Chrome run at 1440x900 passes the shared HTTP, selector, error, visual, subject, and checksum contracts; every incomplete or substituted case fails; OBL-NFR-BROWSER remains open until the durable obligation summary is sealed in Plan 10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Local application path to runtime identity | A path or project label cannot self-assert Google Chrome brand or launchability. |
| Browser session to scenario server | A listening port or runner-side request cannot certify browser-observed behavior. |
| Screenshot to visual verdict | Image existence does not prove geometry, clipping, obstruction, or interaction acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-01-16 | Spoofing/Tampering | local Chrome runtime | mitigate | Exact canonical path, regular executable check, fixed-argv brand/version probe, same-browser Playwright launch, and destructive substitutions. |
| T-01-17 | Tampering/Repudiation | runtime/smoke evidence | mitigate | Canonical subject/scenario/rule linkage, independent semantic/geometry validation, and artifact checksums. |
| T-01-18 | Information disclosure | browser artifacts | mitigate | Synthetic data, redaction, repository-relative locators, and no browser secret or profile capture. |
| T-01-19 | Spoofing/SSRF | local base URL and responder | mitigate | Loopback allowlist, exact health/subject/scenario/401 marker, browser-observed transcript, and no dynamic command. |
| T-01-SC | Tampering | dependencies | mitigate | Reuse the audited Playwright package from Plan 05; install or download nothing in this plan. |
</threat_model>

<verification>
Run both task commands and the exact TEST-MATRIX browser command. Missing or unlaunchable standard-path Chrome remains BLOCKED/nonzero. Historical CfT files are never read as current evidence.
</verification>

<success_criteria>
- One current standard-path Google Chrome execution at 1440x900 passes.
- Runtime path, brand, full/major version, Playwright launch, subject/scenario/rule, browser-observed HTTP result, visual observations, screenshot/transcript/errors, and checksums are linked.
- Missing Chrome, launch failure, stale/incomplete evidence, another browser, request interception, or screenshot-only proof prevents PASS.
- No browser/driver download, ChromeDriver, version/viewport matrix, provider, tunnel, browser secret, remote host, VM, unsupported-browser lane, or blocking UI is implemented.
</success_criteria>

<output>
Create `.planning/phases/01-engineering-verification-foundation/01-06-SUMMARY.md` after execution; do not commit separately.
</output>
