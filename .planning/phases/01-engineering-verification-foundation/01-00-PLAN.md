---
phase: 01-engineering-verification-foundation
plan: 00
type: execute
wave: 0
depends_on: []
entry_remediation: true
revision_inputs: [ENTRY-04-NO-LEGACY-CHAIN, ENTRY-07-BOOTSTRAP]
files_modified:
  - .planning/tools/bootstrap-phase-01.rb
  - .planning/tools/phase01-chrome-entry-contract.rb
  - .planning/tools/produce-phase-01-chrome-entry.rb
  - .planning/tools/test-bootstrap-phase-01.rb
  - .planning/tools/test-produce-phase-01-chrome-entry.rb
  - .planning/tools/verification-evidence.rb
  - .planning/tools/test-repository-verification.rb
  - .planning/tools/test-phase-lifecycle.rb
  - scripts/lib/phase-01/test_run_checks.rb
  - .planning/tools/validate-delivery-attestation.rb
  - .planning/tools/test-delivery-attestation.rb
  - .planning/phases/01-engineering-verification-foundation/EVIDENCE/local-chrome-entry.json
  - .planning/phases/01-engineering-verification-foundation/ENTRY-REVIEW.md
  - .planning/phases/01-engineering-verification-foundation/01-00-SUMMARY.md
autonomous: false
requirements: [REQ-NFR-COMPATIBILITY]
obligations: [OBL-FOUND-TRACE-003, OBL-FOUND-TRACE-004, OBL-NFR-BROWSER]
entry_blockers: []
must_haves:
  truths:
    - "This is the only plan executable while Phase 1 ENTRY is BLOCKED; it repairs entry authorization but closes no product or Phase 1 obligation TODO."
    - "Gate D probes only `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`, derives the installed version at execution time, and never downloads a browser or invokes ChromeDriver or Playwright."
    - "The fixed-argv probe proves `Google Chrome <full-version>` and an isolated-profile headless launch of a synthetic local page; missing path, wrong brand, or failed launch is BLOCKED/nonzero."
    - "Only an independent reviewer may convert the current ENTRY review to PASS, using the exact four-column criterion table; the executor cannot self-approve."
    - "The real bootstrap authorizes the other plans only after the current independent review is PASS and exactly 13 Phase 1 plans validate."
    - "The first independent review's 6 PASS / 2 BLOCKER verdict is the revision input: ENTRY-04 and ENTRY-07 stay open until all six active evidence/lifecycle/delivery consumers stop requiring the superseded browser-source chain."
    - "Plan 00 owns exactly 14 files and may not expand beyond them; it changes repository verification tooling and evidence records only, never application or business code."
  artifacts:
    - path: .planning/tools/phase01-chrome-entry-contract.rb
      provides: "Strict local-Chrome entry evidence and independent-review contract"
    - path: .planning/tools/produce-phase-01-chrome-entry.rb
      provides: "No-network fixed-path version and headless synthetic-page launch probe"
    - path: .planning/phases/01-engineering-verification-foundation/EVIDENCE/local-chrome-entry.json
      provides: "Entry-only path/version/brand/headless-launch evidence, separate from Plan 06 runtime/smoke evidence"
    - path: .planning/phases/01-engineering-verification-foundation/ENTRY-REVIEW.md
      provides: "Independent four-column current-attempt verdict"
    - path: .planning/tools/verification-evidence.rb
      provides: "Current local-Chrome entry/evidence consumer with no legacy Gate D subject membership"
    - path: .planning/tools/validate-delivery-attestation.rb
      provides: "Generic target-tree/subject/evidence/delivery attestation without browser-source inputs"
  key_links:
    - from: .planning/tools/produce-phase-01-chrome-entry.rb
      to: /Applications/Google Chrome.app/Contents/MacOS/Google Chrome
      via: "canonical path plus fixed argv --version and isolated-profile headless synthetic-page launch"
    - from: .planning/tools/bootstrap-phase-01.rb
      to: EVIDENCE/local-chrome-entry.json
      via: "strict schema/path/brand/version/launch validation without legacy admission or attestation digests"
    - from: .planning/tools/bootstrap-phase-01.rb
      to: ENTRY-REVIEW.md
      via: "exact four-column unique criteria, no BLOCKER, final PASS, and independent reviewer identity"
    - from: scripts/lib/phase-01/test_run_checks.rb
      to: EVIDENCE/local-chrome-entry.json
      via: "current entry prerequisite rather than admission, dual-probe, or attestation files"
    - from: .planning/tools/validate-delivery-attestation.rb
      to: EVIDENCE/tested-inputs.json
      via: "generic target-tree subject/evidence digest recomputation with all browser-source history excluded"
---

<objective>
Repair Phase 1 entry so the already installed local Google Chrome can authorize remaining execution without any downloaded browser, driver, Playwright dependency, or version matrix.

Purpose: Remove the circular blocker by giving entry remediation one explicitly executable Wave 0 plan while keeping Plan 06 responsible only for final `/login` smoke-and-visual evidence.
Output: Revised entry contract/producer/bootstrap, migrated evidence/lifecycle/delivery consumers and fixtures, `local-chrome-entry.json`, independent review checkpoint, and `01-00-SUMMARY.md`.

Schema migrations: none.
</objective>

<execution_context>
@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@/Users/laosanzheong/.codex/gsd-core/templates/summary.md
</execution_context>

<entry_preconditions>
This plan is the sole exception to the current Phase 1 ENTRY BLOCKED state. Its first independent review is the immutable revision input `6 PASS / 2 BLOCKER`: only ENTRY-04 and ENTRY-07 are open. It has no ordinary entry precondition and may modify exactly the 14 files declared in `files_modified`, with no additional file allowed. It must not execute Plan 05/06 behavior, modify application/business code, close an obligation TODO, delete superseded history, or claim phase completion.
</entry_preconditions>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engineering-verification-foundation/01-CONTEXT.md
@.planning/phases/01-engineering-verification-foundation/DECISIONS.md
@.planning/phases/01-engineering-verification-foundation/ENTRY-REVIEW.md
@.planning/phases/01-engineering-verification-foundation/TODO.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve and regression-check the no-network local-Chrome entry primitive</name>
  <files>.planning/tools/phase01-chrome-entry-contract.rb, .planning/tools/produce-phase-01-chrome-entry.rb, .planning/tools/bootstrap-phase-01.rb, .planning/tools/test-produce-phase-01-chrome-entry.rb, .planning/tools/test-bootstrap-phase-01.rb, .planning/phases/01-engineering-verification-foundation/EVIDENCE/local-chrome-entry.json</files>
  <behavior>
    - The only accepted executable path is the canonical standard macOS Google Chrome path, and it must be a regular executable without caller-controlled substitution.
    - Fixed argv `--version` must return `Google Chrome <four-part-version>`; full and major versions are recorded but never compared to 151 or another pinned value.
    - A fresh private temporary profile and synthetic local HTML page launch through Chrome's own headless fixed argv, return zero, and expose an exact marker; no Playwright or ChromeDriver process is used.
    - Network access, cache/platform arguments, archive/source fields, admission/probe/attestation digests, driver fields, version pairs, matrices, providers, tunnels, secrets, and VMs are rejected or absent.
    - Bootstrap validates exactly 13 plan files dynamically or through the revised explicit contract and retains strict task/artifact/trace/four-column review checks.
  </behavior>
  <action>Retain the implementation already independently proven by ENTRY-00 through ENTRY-03 and ENTRY-05 through ENTRY-06; do not rewrite those PASS claims or broaden the primitive. Regression-check the standard-library Ruby contract, producer, bootstrap, and destructive fixtures while Task 2 migrates their consumers. The only accepted implementation remains canonical/lstat checks, fixed-argv `--version`, bounded output, a private temporary profile, a synthetic local marker page, direct Chrome headless argv, and strict `phase01-local-chrome-entry-v1` evidence. It must continue rejecting downloads, ZIP/source/cache/platform admission, drivers/WebDriver, Playwright, fixed majors, version pairs, matrices, providers, tunnels, secrets, and VMs. Regenerate `EVIDENCE/local-chrome-entry.json` only through the real standard-path producer if consumer integration changes require resealing; never hand-edit it. Preserve exact 13-plan, seven-obligation, four-column review, and executor/reviewer separation checks. This implements D-01, D-02, D-04, D-08, DR-01-016, DR-01-017, and DR-01-018.</action>
  <verify>
    <automated>/usr/bin/env ruby .planning/tools/test-produce-phase-01-chrome-entry.rb &amp;&amp; /usr/bin/env ruby .planning/tools/test-bootstrap-phase-01.rb</automated>
  </verify>
  <done>The first review's six PASS criteria remain reproducible, producer/bootstrap fixtures pass, real entry evidence validates, no source/driver/download path or fixed Chrome version returns, and bootstrap still rejects a review containing ENTRY-04/07 BLOCKER.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate every active entry, evidence, lifecycle, runner, and delivery consumer</name>
  <files>.planning/tools/verification-evidence.rb, .planning/tools/test-repository-verification.rb, .planning/tools/test-phase-lifecycle.rb, scripts/lib/phase-01/test_run_checks.rb, .planning/tools/validate-delivery-attestation.rb, .planning/tools/test-delivery-attestation.rb</files>
  <behavior>
    - `verification-evidence.rb` has no current `GATE_D_INPUTS`, admission/attestation API call, dual-probe ID, or browser-source tested-subject requirement; entry/lifecycle validates `local-chrome-entry.json` and the current independent review separately.
    - Repository-verification, lifecycle, and root-runner fixtures use the current local-Chrome entry contract and reject any reintroduction of browser-source/admission/dual-probe/attestation membership.
    - Delivery validation preserves generic target-tree, tested-subject, subject-manifest, evidence-manifest, branch/tag/commit/tree, PR/check, and fail-closed digest rules while removing every browser-source-specific input and mutation.
    - Historical `EVIDENCE/browser-source-*` JSON remains present as superseded audit history but is never copied, required, hashed, validated, or accepted as current subject/evidence.
    - Repository-verification, phase-lifecycle, delivery-attestation, runner, producer/bootstrap, planning, and catalog tests all pass before re-review; the real bootstrap remains BLOCKED only because the preserved review still contains ENTRY-04/07 BLOCKER.
  </behavior>
  <action>Use ENTRY-04 and ENTRY-07 as the only revision inputs. In `verification-evidence.rb`, replace the old Gate D required-input/API branch with the current entry/lifecycle boundary: generated `local-chrome-entry.json` stays outside the canonical source subject, is schema/live-file validated through the current contract, and is bound to the independently reviewed entry verdict without becoming OBL-NFR-BROWSER runtime/smoke evidence. Update `test-repository-verification.rb`, `test-phase-lifecycle.rb`, and `scripts/lib/phase-01/test_run_checks.rb` so positive fixtures use the current entry JSON and independent-review contract; add isolated negative mutations proving that any legacy browser-source file, digest, probe ID, or removed API cannot regain active membership. In `validate-delivery-attestation.rb` and `test-delivery-attestation.rb`, remove admission, chrome-151, chrome-152, attestation, and legacy digest variants from the Phase 1 target-tree subject while retaining every generic source/evidence/delivery digest, remote ref, annotated tag, PR/check, and fail-closed mutation. Do not delete or rewrite historical browser-source JSON, weaken general subject/digest/delivery rules, touch application/business code, or add files beyond the 14-file Plan 00 hard cap. Record the consumer migration and still-required independent re-review in `01-00-SUMMARY.md` only at the Task 3 boundary; do not edit `ENTRY-REVIEW.md` as executor.</action>
  <verify>
    <automated>/usr/bin/env ruby .planning/tools/test-produce-phase-01-chrome-entry.rb &amp;&amp; /usr/bin/env ruby .planning/tools/test-bootstrap-phase-01.rb &amp;&amp; /usr/bin/env ruby .planning/tools/test-repository-verification.rb &amp;&amp; /usr/bin/env ruby .planning/tools/test-phase-lifecycle.rb &amp;&amp; /usr/bin/env ruby .planning/tools/test-delivery-attestation.rb &amp;&amp; /usr/bin/env ruby scripts/lib/phase-01/test_run_checks.rb &amp;&amp; /usr/bin/env ruby .planning/tools/test-planning-validators.rb &amp;&amp; /usr/bin/env ruby .planning/tools/validate-prd-obligations.rb --owner engineering-verification-foundation --assert-unique --assert-traced &amp;&amp; ! /usr/bin/env ruby .planning/tools/bootstrap-phase-01.rb --phase-dir .planning/phases/01-engineering-verification-foundation --catalog .planning/PRD-OBLIGATIONS.md</automated>
  </verify>
  <done>All six consumers and their fixtures use only the current local-Chrome entry boundary; all focused/planning/catalog tests pass; legacy JSON remains untouched and unreachable from current subject/evidence; the real bootstrap fails only on the preserved 6 PASS / 2 BLOCKER review pending independent re-review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Obtain independent entry review and real bootstrap PASS</name>
  <files>.planning/phases/01-engineering-verification-foundation/ENTRY-REVIEW.md, .planning/phases/01-engineering-verification-foundation/01-00-SUMMARY.md</files>
  <action>After the executor records the six-consumer migration and verification results in `01-00-SUMMARY.md`, pause it. The orchestrator must dispatch a new independent reviewer/subagent that did not implement the consumer migration. Only that reviewer may revise the preserved first review, rerun all eight criteria with special attention to ENTRY-04 and ENTRY-07, and authorize the real bootstrap. Do not allow the executor or planner to convert the existing 6 PASS / 2 BLOCKER verdict.</action>
  <what-built>The executor retained the independently proven local-Chrome primitive and migrated all six active consumers without touching business code or deleting historical JSON; the first independent verdict remains 6 PASS / 2 BLOCKER until re-review.</what-built>
  <how-to-verify>
    1. The orchestrator must dispatch an independent reviewer/subagent that did not implement Tasks 1-2.
    2. The reviewer reruns the fixed-path `--version` and isolated-profile headless synthetic-page launch independently, validates `local-chrome-entry.json`, all 13 plans, exact seven-obligation trace, no-download/no-driver contract, and the executor/reviewer identity separation.
    3. The reviewer independently runs repository-verification, phase-lifecycle, delivery-attestation, runner, producer/bootstrap self-tests, planning validator, and catalog validator; it scans the six consumers and proves old browser-source JSON appears only as untouched superseded history or negative fixtures, never as active subject/evidence.
    4. Only that reviewer may update the current rows in `ENTRY-REVIEW.md` using exactly `Criterion ID | Verdict | Evidence | Command or inspection rule`; ENTRY-04 becomes PASS only with the full consumer suite green, and ENTRY-07 becomes PASS only after final real-bootstrap zero exit with no other BLOCKER.
    5. After the reviewer writes PASS, run `/usr/bin/env ruby .planning/tools/bootstrap-phase-01.rb --phase-dir .planning/phases/01-engineering-verification-foundation --catalog .planning/PRD-OBLIGATIONS.md` and require zero exit with 13 plans.
    6. If review or bootstrap is nonzero, keep ENTRY and every downstream plan BLOCKED; the executor must not rewrite the reviewer verdict.
  </how-to-verify>
  <verify>
    <automated>/usr/bin/env ruby .planning/tools/bootstrap-phase-01.rb --phase-dir .planning/phases/01-engineering-verification-foundation --catalog .planning/PRD-OBLIGATIONS.md</automated>
  </verify>
  <done>An independent reviewer records a current four-column PASS with zero BLOCKER, the real bootstrap validates all 13 plans and exits zero, reviewer and executor identities differ, and no downstream plan ran before authorization.</done>
  <resume-signal>Resume only after the independent reviewer records PASS and the real bootstrap exits zero; otherwise report the blocking criterion.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Local application path to entry evidence | A path or version label cannot self-prove a runnable Google Chrome installation. |
| Executor evidence to independent review | The implementer cannot approve its own entry remediation. |
| Legacy artifacts to revised bootstrap | Superseded admission/probe/attestation files must not retain hidden authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-01-00-01 | Spoofing/Tampering | local Chrome identity | mitigate | Canonical fixed path, lstat/executable checks, fixed-argv brand/version, real headless marker launch, and destructive substitutions. |
| T-01-00-02 | Spoofing/Repudiation | independent review | mitigate | Distinct reviewer identity, exact four-column criteria, no BLOCKER, final PASS, and executor self-approval rejection. |
| T-01-00-03 | Tampering | legacy Gate D inputs | mitigate | Remove legacy digest requirements and reject forbidden source/driver/admission fields. |
| T-01-SC | Tampering | dependencies | mitigate | Standard-library Ruby and installed Google Chrome only; no package installation or download. |
</threat_model>

<verification>
Run producer/bootstrap self-tests, repository-verification, phase-lifecycle, delivery-attestation, runner tests, planning/catalog validators, the real fixed-path producer, independent re-review, real bootstrap, all 13 plan structure/dependency checks, and `git diff --check`. Before re-review the real bootstrap must remain BLOCKED by the preserved ENTRY-04/07 verdict; after independent PASS it must exit zero. A partial fixture PASS or executor-authored review cannot authorize downstream execution.
</verification>

<success_criteria>
- The sole currently executable plan repairs entry without broadening Phase 1 scope.
- Entry evidence uses only installed Google Chrome and proves version plus a real headless synthetic-page launch.
- All six active consumers use `local-chrome-entry.json`/current review boundaries and exclude every superseded browser-source JSON from current subject/evidence without weakening generic digest/delivery rules.
- Independent review uses the exact four-column contract and ends with no BLOCKER.
- Real bootstrap passes with exactly 13 plans and no legacy admission/attestation digest dependency.
- Other plans remain blocked until all four conditions hold.
</success_criteria>

<output>
Update `.planning/phases/01-engineering-verification-foundation/01-00-SUMMARY.md` with the six-consumer migration and first-review revision inputs before the independent checkpoint. Do not create a separate commit; Phase 1 still has one final atomic commit and push.
</output>
