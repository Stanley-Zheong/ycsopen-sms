# Phase 29 Plan

TODO is the only completion metric.

1. Extend `bulk_sendings` and `bulk_sending_items` without introducing parallel task tables.
2. Add a service that validates import rows, creates stable batch/item identities, and calls `MessageSubmitService.submit` for valid rows.
3. Add tenant/admin controllers for preview, creation, task listing, detail, and state controls.
4. Add tenant/admin React pages with stable `data-testid` coverage.
5. Add backend, unit, and local Chrome Playwright verification.
6. Run review, close TODO, commit, push, and open PR.
