# Phase 42 Plan

Goal: close all scoped tenant risk warning and auto-pause TODOs with executable verification evidence.

Execution items:

1. Backend RED: add service, migration, and controller tests for rule configuration, source snapshot evaluation, unknown denominator behavior, one episode per source key, auto-pause, submission fence, and reviewed recovery.
2. Backend GREEN: implement V5100, service, and controller using existing tenant lifecycle eligibility; do not introduce a generic rule engine.
3. Frontend RED: add unit and Chrome Playwright tests for rules, source evaluation, pause detail, unknown rate display, and recovery.
4. Frontend GREEN: add tenant risk API client, admin page, route, and navigation.
5. Documentation: record spec, intent, design, UI elements, test matrix, decisions, iterations, schema claims, and evidence.
6. Verification: run PRD obligation validation, UI design/production validation, targeted/full backend tests, frontend tests/build, Chrome Playwright, diff checks, and TODO-empty query.
7. Review and commit: run external review boundary and commit only Phase42 files plus ROADMAP update.

Review standard:

- Scope remains tenant risk warning and auto-pause only.
- Chrome is the only browser validation target.
- Completion requires no open scoped TODO.
