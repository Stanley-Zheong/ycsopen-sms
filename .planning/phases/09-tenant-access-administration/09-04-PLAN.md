---
phase: 09-tenant-access-administration
plan: "04"
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - web/src/api/tenantAccessApi.ts
  - web/src/pages/tenant/TenantAdministratorsPage.tsx
  - web/src/pages/tenant/TenantApiKeysPage.tsx
  - web/src/pages/tenant/TenantCmppAccessPage.tsx
  - web/src/router/routes.tsx
  - web/src/components/layout/TenantLayout.tsx
  - web/src/styles/tenant-access.css
  - web/test/unit/tenant-access.test.tsx
  - web/test/scripts/tenant-access.spec.ts
autonomous: true
requirements: [REQ-F-1-3, REQ-F-2-6, REQ-F-2-7]
must_haves:
  truths:
    - "The tenant administrator, API-key, and CMPP pages are reachable on the Phase 02 routes with role-correct navigation."
    - "Every interactive/asserted page element has the documented stable test ID and renders loading, empty, error, denied, success, and confirmation states."
    - "Installed desktop Chrome at 1440x900 proves create/one-time handoff/revoke and cross-tenant or role-denied paths against real services."
  artifacts:
    - path: "web/src/pages/tenant/TenantAdministratorsPage.tsx"
      provides: "Tenant subaccount management page"
      contains: "tenant-tenant-access-administrators-create"
    - path: "web/src/pages/tenant/TenantApiKeysPage.tsx"
      provides: "HTTP API-key management page"
      contains: "tenant-tenant-access-api-keys-secret-once"
    - path: "web/src/pages/tenant/TenantCmppAccessPage.tsx"
      provides: "Downstream CMPP access page"
      contains: "tenant-tenant-access-cmpp-access-page"
  key_links:
    - from: "web/src/router/routes.tsx"
      to: "three tenant access pages"
      via: "exact Phase 02 routes"
    - from: "web/test/scripts/tenant-access.spec.ts"
      to: "real APIs and selectors"
      via: "installed Chrome page.goto + awaited action/assertion"
---

<objective>
Implement and verify the three production tenant-access pages with exact
selector/test evidence.

Purpose: Close the direct UI obligations for subaccounts, HTTP API keys, and
CMPP access while preserving the Chrome-only and no-mobile project boundary.
Output: Typed API client, React routes/pages, unit tests, real Playwright blocks,
and phase evidence consumed by the production UI validator.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/UI-TEST-CONTRACT.md
@.planning/phases/09-tenant-access-administration/09-SPEC.md
@.planning/phases/09-tenant-access-administration/09-UI-SPEC.md
@.planning/phases/09-tenant-access-administration/UI-ELEMENTS.md
@.planning/phases/09-tenant-access-administration/TEST-MATRIX.md
@web/src/router/routes.tsx
@web/src/components/layout/TenantLayout.tsx
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Build role-aware tenant access pages and typed API client</name>
  <files>web/src/api/tenantAccessApi.ts, web/src/pages/tenant/TenantAdministratorsPage.tsx, web/src/pages/tenant/TenantApiKeysPage.tsx, web/src/pages/tenant/TenantCmppAccessPage.tsx, web/src/router/routes.tsx, web/src/components/layout/TenantLayout.tsx, web/src/styles/tenant-access.css, web/test/unit/tenant-access.test.tsx</files>
  <action>Implement the exact Phase 02 routes `/tenant/administrators`, `/tenant/api/keys`, and `/tenant/cmpp/access` using typed API functions and safe response types. Render table/form/dialog elements from `UI-ELEMENTS.md`; use tenant admin-only subaccount controls and tenant admin/developer credential controls, with business-user denied state. Preserve one-time secret/password handoff in a dialog that clears on close/navigation and never re-fetches. Add loading, empty, API error/retry, validation, revoke confirmation/cancel, stale/conflict, and success feedback. Reuse Phase 02 shell/tokens and cite D-09-001, D-09-002, D-09-004, and D-09-005 in implementation comments where policy is not obvious. Do not add mobile styles, other browser branches, generic config abstractions, or hard-coded secrets.</action>
  <verify><automated>npm --prefix web test && npm --prefix web run build</automated></verify>
  <done>All three routes render typed real API states, role/tenant visibility is explicit, and the unit/build checks pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author obligation-linked real Chrome acceptance</name>
  <files>web/test/scripts/tenant-access.spec.ts, .planning/phases/09-tenant-access-administration/EVIDENCE/OBL-F-1-3-A.json, .planning/phases/09-tenant-access-administration/EVIDENCE/OBL-F-1-3-B.json, .planning/phases/09-tenant-access-administration/EVIDENCE/OBL-F-2-6-A.json, .planning/phases/09-tenant-access-administration/EVIDENCE/OBL-F-2-6-B.json, .planning/phases/09-tenant-access-administration/EVIDENCE/OBL-F-2-6-C.json, .planning/phases/09-tenant-access-administration/EVIDENCE/OBL-F-2-7-A.json, .planning/phases/09-tenant-access-administration/EVIDENCE/playwright-execution.json</files>
  <behavior>
    - Test 1: `pw-p9-tenant-administrators` navigates `/tenant/administrators`, creates/assigns a permitted subaccount, and verifies the row; a foreign tenant/role path is denied.
    - Test 2: `pw-p9-api-key-create` navigates `/tenant/api/keys`, creates a policy-bound key, asserts one-time secret handoff, refreshes and proves masking, then confirms revoke.
    - Test 3: `pw-p9-cmpp-create` navigates `/tenant/cmpp/access`, requests metadata/password handoff, asserts safe later read, and confirms revoke.
    - Test 4: direct obligation blocks contain exact OBL, Case, Playwright, linked route, and selector IDs and awaited actions/assertions.
  </behavior>
  <action>Write one meaningful Playwright block per direct UI obligation in `TEST-MATRIX.md`; use real Spring/Vite/MySQL/Phase 03 protected storage and installed local Google Chrome only, at runtime-asserted 1440x900 with one worker. Do not use `page.route`, API interception, browser downloads, mocks, Chrome for Testing, ChromeDriver, mobile emulation, or cross-browser projects. Capture sanitized result/evidence metadata and database/event facts needed for the seven obligation files. Keep selectors exactly aligned with the UI inventory and preserve secret values only in process memory long enough to assert the handoff.</action>
  <verify><automated>npx playwright test web/test/scripts/tenant-access.spec.ts --project=chromium --workers=1</automated></verify>
  <done>Real installed-Chrome blocks pass for all direct obligations; evidence files contain command, application/config identity, PASS, and no secret plaintext.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile production UI contract and acceptance artifacts</name>
  <files>.planning/phases/09-tenant-access-administration/EVIDENCE/ui-contract.json, .planning/phases/09-tenant-access-administration/EVIDENCE/playwright-execution.json, .planning/phases/09-tenant-access-administration/TEST-MATRIX.md, .planning/phases/09-tenant-access-administration/UI-ELEMENTS.md</files>
  <action>Run the exact design and production UI validators and compare route/selector/test-ID sets against the source inventory. Update UI-ELEMENTS, DECISIONS, and TEST-MATRIX together for any real drift; never hide a mismatch in a registry-only file. Record the installed Chrome path/version, 1440x900 assertion, real-service command, report hash, and exact Case-ID set. Keep OBL-DATA-10-6-ACCESS lower-layer-only in the matrix while its backend evidence remains in Plan 02/03.</action>
  <verify><automated>/usr/bin/env ruby .planning/tools/validate-ui-contract.rb --phase 09 --package tenant-access-administration --stage design && /usr/bin/env ruby .planning/tools/validate-ui-contract.rb --phase 09 --package tenant-access-administration --stage production</automated></verify>
  <done>Design and production UI contract validators pass with exact documented routes/selectors and executed real-Chrome evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| tenant browser -> React/API | Untrusted UI input and credential IDs cross into authenticated APIs. |
| API response -> DOM | Secret-producing responses must be displayed once and cleared without accidental persistence. |
| browser -> real service evidence | Acceptance must prove actual service behavior without network substitution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-09-04-01 | Information disclosure | secret handoff dialog | mitigate | Render once, clear on close/navigation, never place secret in list state or logs. |
| T-09-04-02 | Elevation of privilege | role-aware navigation/actions | mitigate | Server permission remains authoritative; UI hidden/denied states mirror documented role matrix. |
| T-09-04-03 | Repudiation | destructive revoke action | mitigate | Confirmation, redacted audit result, and event evidence are asserted. |
| T-09-04-SC | Tampering | package installs | mitigate | No package install is planned; use existing web dependencies and local Chrome. |
</threat_model>

<verification>
Run frontend unit/build, real installed-Chrome Playwright, both UI-contract stages,
and the complete phase obligation/evidence query. Review all selectors against
the documented inventory before declaring TODO empty.
</verification>

<success_criteria>
All three production routes and six direct UI obligation blocks pass against real
services in local Chrome; the production contract passes and all seven phase
obligations have evidence-ready trace rows.
</success_criteria>

<output>
Create `.planning/phases/09-tenant-access-administration/09-04-SUMMARY.md` when done.
</output>
