# Phase 35 Schema Claims

Schema owner: `SCHEMA-P35 | Alerts and notification routing | V4400-V4499`.

Claims:

- V4400 additively extends `alert_rules`.
- V4400 additively extends `alert_records`.
- V4400 creates `alert_delivery_attempts`.
- V4400 creates `alert_mutes`.
- No existing alert columns are dropped or renamed.
