# Phase 35 Intent

Deliver the alert loop users actually need:

source breach → one alert episode → notification evidence → operator action → auditable final state.

The phase deliberately avoids building a generic observability platform. Later phases can add more source metrics and richer dashboards on top of the same rule/episode/delivery tables.
