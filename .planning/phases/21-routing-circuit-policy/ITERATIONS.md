# Iterations

| ID | Observation | Action |
| --- | --- | --- |
| P21-I01 | Existing `ChannelSelector` was explicitly simplified. | Added separate policy service instead of destabilizing the current send chain. |
| P21-I02 | Phase obligations require UI and runtime-only evidence. | Split UI rows and runtime matrix rows. |

