# Intent

Make a small, safe set of runtime settings operable without restart or silent drift. The boundary is deliberately closed: registered keys only, one local atomic snapshot, and immutable database versions. This phase does not build a general configuration platform.
