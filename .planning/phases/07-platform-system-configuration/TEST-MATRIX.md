# Phase 07 test matrix

The HTML Playwright source proves design wiring. Production Playwright in the locally installed Google Chrome proves rendered administrator interactions. Java and real-MySQL tests prove registry validation, authorization, version integrity, concurrency, atomic reload, rollback, and audit metadata.

| Obligation ID | Requirement IDs | Behavior ID | Catalog test/layer | Playwright ID | Page ID/route | data-testid | Case ID | Case | Command | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| OBL-IA-ADMIN-SYSTEM-CONFIG | PROJECT-SYSTEM-CONFIG | platform-system-configuration-01 | T-IA-ADMIN-SYSTEM-CONFIG:playwright | pw-p7-system-configuration | admin-system-configuration /admin/system/configuration | admin-platform-system-configuration-page | C-P7-SYSTEM-CONFIG | Unit tests prove registry, safe secret preservation, prepare rejection, transaction/apply ordering, reload-error rendering, stale rejection, rollback, permission, and actor evidence; the integration profile proves Flyway/current data under real MySQL; the real-service installed-Chrome harness uses real login/API/database/runtime for happy path, no-read/read-only/no-activate denial, validation, two-session stale response, persisted rejection display, browser-offline retry, history, and rollback without platform request interception | `mvn -f core/pom.xml test && mvn -f core/pom.xml -Pphase01-integration -Dtest=Phase07ConfigurationMySqlIntegrationTest test && mvn -f core/pom.xml -Pphase01-integration -Dtest=Phase07RealServicePlaywrightTest test` | `EVIDENCE/OBL-IA-ADMIN-SYSTEM-CONFIG.json` |
