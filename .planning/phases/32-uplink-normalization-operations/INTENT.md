# Phase 32 Intent

Deliver the smallest production slice that makes uplinks operationally usable:

- one normalized record for HTTP and CMPP uplinks;
- tenant-safe search;
- admin detail/replay;
- push monitor based on real webhook delivery rows;
- tenant auto-reply config with audit reason and loop guard.

Completion metric: scoped TODO set is empty with executable evidence.
