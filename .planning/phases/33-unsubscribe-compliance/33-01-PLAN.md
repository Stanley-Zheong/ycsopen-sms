# Phase 33 Plan

## Backend

- Extend legacy `unsubscribe_records` and `unsubscribe_keywords` via V4200 without recreating V1 tables.
- Add `UnsubscribeComplianceService` for keyword maintenance, matching, evidence, statistics, alerts, and export requests.
- Add `UnsubscribeComplianceController` admin and tenant APIs.
- Add `WebhookDeliveryTransportService.enqueueUnsubscribeEvent`.
- Invoke unsubscribe matching from `UplinkNormalizationService` after a new normalized uplink is persisted.

## Frontend

- Add admin `/admin/unsubscribes` page for keyword library, evidence, statistics, and alert evaluation.
- Add tenant `/tenant/unsubscribes` page for scoped evidence search, keyword extension, and export request.
- Add route and sidebar entries with explicit test IDs.

## Verification

- Backend targeted tests for service, migration, webhook transport, and uplink integration.
- Full backend suite.
- Frontend unit test, full web test, build.
- Local Google Chrome Playwright script for Phase33.
- PRD obligation and UI contract validators.
- Review boundary via Claude CLI when available.
