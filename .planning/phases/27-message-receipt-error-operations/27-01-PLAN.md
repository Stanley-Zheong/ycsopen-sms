# Phase 27 Plan

## TODO source

The authoritative TODO set is the owner query:

`/usr/bin/env ruby .planning/tools/validate-prd-obligations.rb --owner message-receipt-error-operations --assert-unique --assert-traced`

## Implementation steps

1. Add operation evidence migration for idempotent actions.
2. Implement backend operations service/controller over existing message, outbox, receipt, status, and recovery services.
3. Add backend tests for query masking, cross-tenant status denial, resend idempotency, receipt correction, error grouping, bulk action partial outcomes, export handoff, and migration shape.
4. Implement frontend API adapter and records operations page/routes.
5. Add frontend unit and local Chrome Playwright tests mapped to UI obligations.
6. Capture Phase27 evidence and close TODO only when scoped TODO is empty.
7. Commit, push, and create PR after verification.
