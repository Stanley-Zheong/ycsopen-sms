# Intent

Provide one focused risk-control slice that decides whether a message should be blocked before any send task or charge exists.

The phase intentionally keeps the external provider as a deterministic local contract because real provider SDK onboarding is outside this module. The durable behavior this phase owns is source precedence, protected-number handling, fallback evidence, operator UI, and appeal traceability.
