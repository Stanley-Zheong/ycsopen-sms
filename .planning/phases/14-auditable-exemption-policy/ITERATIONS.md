# Iterations

## Iteration 1

- Define Phase 14 scoped TODOs and UI catalog.
- Implement exemption policy API, persistence, Admin page, and tests.
- Run GSD/code/Claude review and close only verified TODOs.

## Iteration 2

- Fixed code-review findings for permission enforcement, wildcard specificity, legacy migration backfill, version uniqueness, preview input coverage, null request handling, and protected query scoping.

## Iteration 3

- Fixed focused migration review finding by assigning deterministic legacy `version_no` values before adding the unique key.
