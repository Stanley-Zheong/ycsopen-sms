# Phase 18 Plan

## TODO

1. Extend frequency schema/entity safely.
2. Add shared Redis fixed-window atomic counter.
3. Complete runtime FrequencyChecker dimensions, scopes, exemptions, and hit evidence.
4. Enforce API key second/minute/hour/day limits before message submission.
5. Add Admin frequency rule service/controller and permissions.
6. Add Admin UI/API/tests and tenant API key rate-limit selector.
7. Record design/test/evidence artifacts.
8. Run full verification and review before commit.

## Verification commands

- `mvn -q -f core/pom.xml test`
- `npm --prefix web ci`
- `npm --prefix web test`
- `npm --prefix web run build`
- `npm --prefix web exec -- playwright test frequency-rules.spec.ts --config web/playwright.config.ts --project=local-google-chrome --reporter=json`
- `/usr/bin/env ruby .planning/tools/validate-prd-obligations.rb --owner frequency-api-rate-controls --assert-unique --assert-traced`
- `/usr/bin/env ruby .planning/tools/validate-ui-contract.rb --phase 18 --package frequency-api-rate-controls --stage production`
