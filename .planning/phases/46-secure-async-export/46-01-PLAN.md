# Phase 46 PLAN

## TODO

1. Extend `export_tasks` with secure job metadata and permissions.
2. Implement unified create/list/retry/download service and controller.
3. Connect send-detail, receipt-detail, tenant-unsubscribe and balance-audit export actions.
4. Add admin export center UI and stable test IDs.
5. Add migration, backend service, frontend unit and Chrome Playwright tests.
6. Generate Phase 46 process/UI/evidence docs and run validators.
7. Run full required backend/frontend verification.
8. Run review and fix blocking findings.
9. Commit, push and open PR.

## Verification commands

- `mvn -f core/pom.xml test`
- `npm --prefix web ci`
- `npm --prefix web test`
- `npm --prefix web run build`
- `npm --prefix web exec -- playwright test secure-async-export.spec.ts --config web/playwright.config.ts --project=local-google-chrome`
- `/usr/bin/env ruby .planning/tools/validate-prd-obligations.rb --owner secure-async-export --assert-unique --assert-traced`
- `/usr/bin/env ruby .planning/tools/validate-ui-contract.rb --phase 46 --package secure-async-export --stage design`
- `/usr/bin/env ruby .planning/tools/validate-ui-contract.rb --phase 46 --package secure-async-export --stage production`
