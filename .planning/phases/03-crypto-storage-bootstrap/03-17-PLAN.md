---
phase: 03-crypto-storage-bootstrap
plan: "17"
type: execute
wave: 7
depends_on: [03-03, 03-08, 03-09, 03-26, 03-28]
files_modified:
  - core/src/main/java/com/ycsopen/sms/core/web/controller/TenantRegistrationObjectController.java
  - core/src/main/java/com/ycsopen/sms/core/common/security/object/TenantRegistrationObjectSessionService.java
  - core/src/test/java/com/ycsopen/sms/core/web/TenantRegistrationObjectApiTest.java
  - core/docs/API.md
  - docs/使用手册.md
autonomous: true
requirements: [REQ-NFR-DATA-PROTECTION, OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-002]
must_haves:
  truths:
    - "The API has one fixed session/multipart/upload-ID/atomic-claim contract and rejects every legacy external URL with a documented stable 422 error."
    - "One session-bound token uploads all five purposes sequentially and replaces a purpose while OPEN, but never crosses session/tenant-draft or works after claim, explicit close or expiry."
  artifacts:
    - path: core/src/main/java/com/ycsopen/sms/core/web/controller/TenantRegistrationObjectController.java
      provides: "Fixed session and purpose-bound multipart upload endpoints"
    - path: core/docs/API.md
      provides: "Runtime-parity API and compatibility contract"
  key_links:
    - from: core/src/main/java/com/ycsopen/sms/core/web/controller/TenantRegistrationObjectController.java
      to: core/src/main/java/com/ycsopen/sms/core/common/security/object/TenantRegistrationObjectSessionService.java
      via: "Create, upload into and close a bound registration-object session"
      pattern: "registrationObjectSession"
---

<objective>Implement and document the fixed staged tenant-registration evidence upload API.</objective>
<execution_context>@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@AGENTS.md
@.planning/phases/03-crypto-storage-bootstrap/03-DECISIONS.md
@.planning/phases/03-crypto-storage-bootstrap/ENVELOPE-CONTRACT.md
@.planning/phases/03-crypto-storage-bootstrap/03-PATTERNS.md</execution_context>

## Artifacts this phase produces

- Fixed registration-object session/upload API and docs/runtime parity tests.

<tasks>
<task type="auto">
  <name>Task 1: Implement the fixed staged evidence upload API and documentation</name>
  <files>core/src/main/java/com/ycsopen/sms/core/web/controller/TenantRegistrationObjectController.java, core/src/main/java/com/ycsopen/sms/core/common/security/object/TenantRegistrationObjectSessionService.java, core/src/test/java/com/ycsopen/sms/core/web/TenantRegistrationObjectApiTest.java, core/docs/API.md, docs/使用手册.md</files>
  <read_first>
    - core/src/main/java/com/ycsopen/sms/core/web/controller/TenantController.java
    - core/src/main/java/com/ycsopen/sms/core/common/security/object/ProtectedObjectService.java
    - core/src/main/java/com/ycsopen/sms/core/common/security/key/OpaqueTokenDigestPort.java
    - core/docs/API.md
    - docs/使用手册.md
    - .planning/phases/03-crypto-storage-bootstrap/ENVELOPE-CONTRACT.md
    - .planning/phases/03-crypto-storage-bootstrap/03-PATTERNS.md
  </read_first>
  <action>No repository object-upload analog exists, so implement DR-P03-009 and `ENVELOPE-CONTRACT.md` using constructor injection, before-allocation bounded input handling, stable errors and MVC/service tests per `03-PATTERNS.md`. `POST /api/v1/console/tenants/registration-object-sessions` returns opaque session ID, one `regup_v1_` session-bound upload token and expiry marker. Generate a nonsecret lookup ID plus 32-byte CSPRNG secret, use only `OpaqueTokenDigestPort` purpose `REGISTRATION_UPLOAD` with tenant-draft/session binding, and store its ACTIVE key version plus 32-byte digest. Every upload verifies the stored ACTIVE/RETIRING version in constant time; it never calls `BlindIndexPort` or accepts an object-capability token. The same token may upload all five purposes sequentially and replace the current STAGED object for one purpose while that exact tenant-draft/session is OPEN; it never crosses session or tenant draft. After media/size validation and before encryption/store work, atomically reserve an admitted-attempt slot only while OPEN: maximum three per purpose and fifteen per session. Concurrent callers cannot exceed either bound; any provider/store/reconciliation failure after reservation burns the slot and stable HTTP 429 `REGISTRATION_UPLOAD_LIMIT_REACHED` is returned at the boundary. Explicit close, successful claim or expiry invalidates it. Upload accepts exactly one multipart part `file` plus `X-Registration-Upload-Token` and returns only `pobj_v1_*`, purpose and expiry. Enforce exact content limits and before-allocation checks for all five purposes; reconcile replaced objects by operation ID. Document routes, token format/purpose/version policy, repeat-use/admission scope, terminal invalidation, states, errors, limits, privacy and legacy rejection; tests compare docs to runtime constants and cover attempts 2/3 and session 14/15/16, concurrent reservations, burned post-reservation failure, ACTIVE/RETIRING rotation, revoked/unknown version and cross-domain rejection.</action>
  <verify><automated>mvn -f core/pom.xml -Dtest=TenantRegistrationObjectApiTest test &amp;&amp; rg -n 'registration-object-sessions|businessLicenseObjectId|LEGACY_OBJECT_URL_NOT_ACCEPTED|PT24H' core/docs/API.md docs/使用手册.md</automated></verify>
  <acceptance_criteria>One token sequentially uploads five purposes and replaces one only inside its OPEN binding; atomic admission never exceeds three attempts per purpose or fifteen per session even under concurrency/failure; claim/close/expiry/cross-session/cross-tenant-draft reuse fails; all exact size/envelope boundaries and documents match runtime; no endpoint accepts a URL, bucket, key or public/presigned reference.</acceptance_criteria>
  <done>The staged upload API is deterministic, bounded, private and documented.</done>
</task>

</tasks>

<threat_model>
| Threat ID | STRIDE | Disposition | Mitigation |
| --- | --- | --- | --- |
| P03-17-T1 | Information disclosure | mitigate | Opaque session/object IDs and no token or storage detail in logs/errors |
| P03-17-T2 | Tampering | mitigate | Session/tenant-draft/purpose binding and terminal token invalidation |
| P03-17-T3 | Repudiation | mitigate | Explicit OPEN/CLOSED/CLAIMED/EXPIRED states and stable errors |
| P03-17-T4 | Denial of service | mitigate | Declared/actual plaintext and envelope bounds before allocation |
| P03-17-T5 | Elevation of privilege | mitigate | Keyed token digest and cross-session/cross-draft denial |
</threat_model>

<verification>Run registration-object API tests and docs/runtime parity scans.</verification>
<success_criteria>The staged evidence upload API is fixed, bounded, private and documented.</success_criteria>
<output>Create `03-17-SUMMARY.md` with route/state matrix, exact limits, errors and documentation parity.</output>
