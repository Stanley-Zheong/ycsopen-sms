---
phase: 03-crypto-storage-bootstrap
plan: "03"
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/lib/phase-03/service_checks.rb
  - scripts/lib/phase-03/test_service_checks.rb
  - scripts/lib/phase-03/softhsm-source.json
  - scripts/provision-phase-03-softhsm
  - core/src/test/java/com/ycsopen/sms/core/verification/Phase03ServiceHarness.java
  - core/src/test/java/com/ycsopen/sms/core/verification/Phase03FixturePreflightTest.java
  - core/src/test/resources/application-phase03-integration.yml
  - core/pom.xml
autonomous: true
requirements: [REQ-NFR-DATA-PROTECTION, OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003, OBL-CRYPTO-STORAGE-004]
must_haves:
  truths:
    - "SoftHSM 2.7.0 is verified, built or admitted from an exact existing path, initialized, tested and cleaned before PKCS11 evidence runs."
    - "No verify-only manifest command is misrepresented as an executable provider prerequisite."
    - "MySQL and MinIO retain their exact admitted digest and run-owned cleanup contracts."
  artifacts:
    - path: scripts/provision-phase-03-softhsm
      provides: "Pinned source verification, isolated build/token initialization and cleanup"
    - path: core/src/test/java/com/ycsopen/sms/core/verification/Phase03FixturePreflightTest.java
      provides: "Executable/library/token/mechanism identity preflight"
  key_links:
    - from: scripts/provision-phase-03-softhsm
      to: scripts/lib/phase-03/softhsm-source.json
      via: "Exact URL, tag, commit, SHA-256 and isolated destination"
      pattern: "be14a5820ec457eac5154462ffae51ba5d8a643f6760514d4b4b83a77be91573"
---

<objective>Establish executable real-service fixtures, including a real SoftHSM runtime rather than source-manifest admission alone.</objective>
<execution_context>@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@AGENTS.md
@.planning/phases/03-crypto-storage-bootstrap/03-VALIDATION.md
@.planning/phases/03-crypto-storage-bootstrap/03-DECISIONS.md</execution_context>

## Artifacts this phase produces

- Fixed source manifest for tag 2.7.0, commit `13e6e86`, codeload SHA-256 `be14a...573`.
- Isolated exact-destination build/admit flow, token/config/PIN lifecycle, mechanism/key-attribute preflight and cleanup.
- Java bridge plus opt-in MySQL/MinIO/SoftHSM profile; no mock substitution.

<tasks>
<task type="auto">
  <name>Task 1: Implement pinned SoftHSM provision or verified-path admission</name>
  <files>scripts/lib/phase-03/softhsm-source.json, scripts/provision-phase-03-softhsm, scripts/lib/phase-03/service_checks.rb, scripts/lib/phase-03/test_service_checks.rb</files>
  <read_first>
    - scripts/lib/phase-01/service_checks.rb
    - .planning/phases/03-crypto-storage-bootstrap/03-DECISIONS.md
  </read_first>
  <action>Per DR-P03-002, accept exactly one of two fail-closed branches: verify a pre-existing `softhsm2-util` plus native library at explicit canonical paths and require version 2.7.0, or fetch the immutable codeload URL, verify SHA-256 before extraction, build into a run-owned destination with fixed arguments, and discover the exact CLI/library below that destination. Initialize a run-owned token using a permission-restricted PIN source, create nonextractable AES/HMAC keys, verify required mechanisms/attributes, emit hashes/counts only, and clean token/config/build resources. No branch/tag/latest/package-manager fallback exists.</action>
  <verify><automated>/usr/bin/env ruby scripts/lib/phase-03/test_service_checks.rb &amp;&amp; scripts/provision-phase-03-softhsm --manifest scripts/lib/phase-03/softhsm-source.json --destination core/target/phase03/softhsm --provision --initialize-token --preflight --cleanup</automated></verify>
  <acceptance_criteria>Wrong/missing digest/path/version/mechanism/attribute/token/cleanup fails; successful output contains exact identities and no secrets.</acceptance_criteria>
  <done>A real isolated SoftHSM runtime is executable and cleanly removable.</done>
</task>

<task type="auto">
  <name>Task 2: Wire executable MySQL, MinIO and SoftHSM fixture preflight</name>
  <files>core/src/test/java/com/ycsopen/sms/core/verification/Phase03ServiceHarness.java, core/src/test/java/com/ycsopen/sms/core/verification/Phase03FixturePreflightTest.java, core/src/test/resources/application-phase03-integration.yml, core/pom.xml</files>
  <read_first>
    - core/src/test/java/com/ycsopen/sms/core/verification/Phase01MySqlIntegrationTest.java
    - scripts/lib/phase-03/service_checks.rb
    - core/pom.xml
  </read_first>
  <action>Add fixed-argument process bridging and an opt-in integration profile. The preflight must start/admit exact MySQL and MinIO identities and call the provisioned SoftHSM executable/library/token, not `--verify-only`. Assert mechanism and key-attribute identity, run-owned isolation, bounded redacted output, and exact cleanup. Ordinary Maven tests skip real lanes explicitly without replacing them.</action>
  <verify><automated>mvn -f core/pom.xml -Pphase03-integration -Dtest=Phase03FixturePreflightTest test &amp;&amp; /usr/bin/env ruby scripts/lib/phase-03/service_checks.rb assert-clean --all</automated></verify>
  <acceptance_criteria>All three real boundaries execute and clean; any unavailable prerequisite yields BLOCKED and cannot be relabeled.</acceptance_criteria>
  <done>Real fixture preflight reaches actual services and token mechanisms.</done>
</task>
</tasks>

<threat_model>
| Threat ID | STRIDE | Disposition | Mitigation |
| --- | --- | --- | --- |
| P03-03-T1 | Spoofing | mitigate | Exact immutable source/version/SHA or verified existing paths |
| P03-03-T2 | Information disclosure | mitigate | PIN/key/path redaction and run-owned permission boundary |
| P03-03-T3 | Tampering | mitigate | Verify before extract/build; canonical destination containment |
| P03-03-T4 | Repudiation | mitigate | Preflight records executable/library/token/mechanism identities |
| P03-03-T5 | Denial of service | mitigate | Exact cleanup on every success/failure branch |
</threat_model>

<verification>Run provisioner destructive tests, actual provision/preflight, Java fixture preflight and cleanup assertion.</verification>
<success_criteria>A real SoftHSM path exists and executes; source admission alone and deterministic adapters do not satisfy the gate.</success_criteria>
<output>Create `03-03-SUMMARY.md` with exact source/path identities, provision branch, mechanism/attribute facts and cleanup result.</output>
