---
phase: 03-crypto-storage-bootstrap
plan: 31
type: review-remediation
status: complete
source: https://github.com/Stanley-Zheong/ycsopen-sms/pull/15#issuecomment-3266165982
---

# Phase 03 Plan 31: PR review remediation

## Intent

Close the ten independently reported PR #15 findings without expanding Phase 03 beyond its existing crypto-storage, migration, object-session, routing-boundary, CI, and deployment-document contracts. This is one remediation plan, not ten new plans.

The implementation decisions and proof boundaries are fixed in `03-31-SOLUTION.md`.

## Admission standard

- Reproduce or prove each finding from the current production data flow before changing it.
- Keep the fix at the smallest existing ownership boundary.
- Add behavior-level regression coverage for every confirmed HIGH or MEDIUM finding.
- Do not regenerate committed hash-sealed result files; standard verification output is generated locally or by CI and remains untracked.
- Run one aggregated independent GSD review and one Claude review after implementation. Only BLOCKER/HIGH findings reopen implementation.

## Tasks

1. Migrate legacy `message_tasks.mobile_encrypted` together with its legacy locator/index transition so COMPLETE cannot retain plaintext.
2. Query both global and tenant-scoped blacklist blind indexes while preserving tenant-whitelist precedence.
3. Accept valid `SNAPSHOT_RECOVERY` rotation-required metadata and prove restart at the threshold.
4. Delay FIELD encryption and wrap reservation until routing accepts the message.
5. Serialize registration-session terminal transitions with final STAGED publication.
6. Fail closed for MOBILE frequency rules until Phase 18 supplies a cross-rotation stable identity.
7. Verify pull requests against the synthetic merge tree.
8. Generate Phase 03 real-service results during CI and remove committed `core/target` output.
9. Run the required frontend install, unit test, and production build in CI.
10. Replace obsolete `FIELD_ENCRYPTION_KEY` documentation with truthful disabled-default and PKCS#11 bootstrap guidance.
11. Restore the still-applicable Phase 01 and Phase 03 portable CI gates without restoring superseded committed-result or lifecycle self-attestation.
12. Make registration-session expiry part of the locked object-publication decision.
13. Prove `CLOSED` and `CLAIMED` publication races in both winner orders on real MySQL/InnoDB.
14. Derive migration-only blind indexes from the verified legacy digest so the locked 11-digit domain does not inherit the narrower online-writer validator.
15. Reject every `MESSAGE_TASK` migration state that is neither a complete admitted legacy row nor a canonical bound current row.
16. Revalidate legacy message plaintext format and SHA-256 equality at BACKFILLED/VERIFIED state advancement.
17. Decode current message envelopes structurally as fixed-length `DATABASE_FIELD` YCSE values before accepting their row binding.
18. Keep whole-message state validation off unbounded locking reads while preserving per-row migration locks and compare-and-set writes.
19. Prepare the existing digest-pinned MySQL and MinIO images explicitly in the clean real-integration CI job before the fail-closed fixture starts.
20. Validate MinIO's immutable repository digest, platform and release label plus the two exact Docker-store representations of its image ID; emit distinct manifest/config/store-ID fields, then prove the running container is bound to the validated pre-start store ID.
21. Make the real leak scan start correctly after fixture initialization on an otherwise clean generated-output tree: require the existing service-owned root to resolve to the exact repository path, then scan the same run's PKCS#11 proof output through the producer's shared closed-format validator as a temporary report input; preserve `REPORTS_INPUTS_EMPTY` for genuinely missing inputs and delete the bootstrap file after use.
22. Reconcile the active `03-VALIDATION.md` with final executable reality: verified plan statuses, completed Wave 0/sign-off criteria, explicit immutable CI prerequisite preparation, and the seven-suite real boundary that supersedes the absent monolithic fault class.

## Verification

- Focused tests named by each task.
- `mvn -f core/pom.xml test`
- `npm --prefix web ci`
- `npm --prefix web test`
- `npm --prefix web run build`
- Phase 03 real Maven profile with all named real integration classes executed and none skipped.
- `git ls-files 'core/target/**'` returns no paths.
- PR #15 synthetic-merge CI.
- Real MySQL object-session race regressions cover two terminal states and two winner orders.
- The final aggregate GSD review and Claude review report no unresolved BLOCKER/HIGH.

## Completion

Plan 31 completes only when every corresponding row in `TODO.md` is checked with executable evidence, the aggregate reviews contain no scoped correctness defect or unresolved BLOCKER/HIGH, and the correction commit is pushed to PR #15.
