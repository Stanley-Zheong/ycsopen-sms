---
phase: 03-crypto-storage-bootstrap
plan: "11"
type: execute
wave: 4
depends_on: [03-01, 03-05]
files_modified:
  - core/src/main/resources/db/migration/V1200__create_crypto_storage_metadata.sql
  - .planning/phases/03-crypto-storage-bootstrap/SCHEMA-CLAIMS.md
  - .planning/phases/03-crypto-storage-bootstrap/DESIGN.md
  - core/src/test/java/com/ycsopen/sms/core/verification/Phase03MigrationIntegrationTest.java
autonomous: true
requirements: [REQ-NFR-DATA-PROTECTION, OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003, OBL-CRYPTO-STORAGE-004]
must_haves:
  truths:
    - "V1200 creates only Phase-3 key/object/migration metadata and never alters V1."
    - "Fresh real MySQL applies immutable V1 plus the registered V1200 schema."
    - "Blind indexes store one target/key-version value per row with enforceable lookup, uniqueness, key-reference and binding reconciliation contracts."
  artifacts:
    - path: core/src/main/resources/db/migration/V1200__create_crypto_storage_metadata.sql
      provides: "Expand-only Phase-3 metadata"
  key_links:
    - from: .planning/phases/03-crypto-storage-bootstrap/SCHEMA-CLAIMS.md
      to: .planning/SCHEMA-OWNERSHIP.md
      via: "SCHEMA-P03 V1200-V1299 claim"
      pattern: "V1200"
---

<objective>Create and prove the Phase-3-owned expand schema without legacy DDL.</objective>
<execution_context>@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@AGENTS.md
@.planning/SCHEMA-OWNERSHIP.md
@skills/flyway-migration/SKILL.md
@.planning/phases/03-crypto-storage-bootstrap/03-PATTERNS.md</execution_context>

## Artifacts this phase produces

- V1200 key-reference, object/capability, migration run/checkpoint/event metadata and exact schema claims.

<tasks>
<task type="auto">
  <name>Task 1: Add V1200 expand schema and real-MySQL proof</name>
  <files>core/src/main/resources/db/migration/V1200__create_crypto_storage_metadata.sql, .planning/phases/03-crypto-storage-bootstrap/SCHEMA-CLAIMS.md, .planning/phases/03-crypto-storage-bootstrap/DESIGN.md, core/src/test/java/com/ycsopen/sms/core/verification/Phase03MigrationIntegrationTest.java</files>
  <read_first>
    - .planning/SCHEMA-OWNERSHIP.md
    - core/src/main/resources/db/migration/V1__init_schema.sql
    - .planning/phases/03-crypto-storage-bootstrap/03-DECISIONS.md
    - .planning/phases/03-crypto-storage-bootstrap/03-PATTERNS.md
  </read_first>
  <action>Follow the `V1__init_schema.sql` dialect/naming analog and owner-range rules in `03-PATTERNS.md`; do not reuse V1's raw mobile-hash semantics. Create only P03 metadata tables with constrained key states, opaque identifiers/digests, optimistic versions, leases/checkpoints, signed-manifest sequences and sanitized counters. Per DR-P03-007, create `ycs_crypto_blind_indexes` with `target_type`, `legacy_row_id`, `field_id`, `key_version`, 53-character ASCII-bin `index_value`, constrained `status`, original-row digest and optimistic version. Enforce unique `(target_type, legacy_row_id, field_id, key_version)`, lookup `(target_type, field_id, status, key_version, index_value)`, and a foreign key from canonical `key_version`/purpose to the Phase-owned key-reference registry. Document why polymorphic legacy rows cannot have one physical FK and require allowlisted target manifest, same-transaction row existence, digest predicate and orphan reconciliation. Add per-target checkpoint states `DISCOVERED/BACKFILLED/VERIFIED/CUTOVER/SCRUBBED/COMPLETE` plus accepted writer/snapshot manifest sequence/digest fields. Include registration-object session, purpose, single-claim, expiry and operation-reconciliation metadata required by DR-P03-009. Store no key/PIN/DEK/plaintext/raw URL/token. Assert V1200 before creation and V1201 after. Write exact claims and design declaration. Real MySQL must prove V1 checksum unchanged, Flyway validation, exact constraints/indexes, duplicate version/binding rejection, and no ALTER/DROP/RENAME/UPDATE against legacy objects.</action>
  <verify><automated>python3 skills/flyway-migration/scripts/next_flyway_version.py --owner crypto-storage-bootstrap --check V1201 &amp;&amp; /usr/bin/env ruby .planning/tools/test-planning-validators.rb &amp;&amp; mvn -f core/pom.xml -Pphase03-integration -Dtest='Phase01MySqlIntegrationTest,Phase03MigrationIntegrationTest#appliesV1200WithoutChangingV1' test</automated></verify>
  <acceptance_criteria>Schema claims pass; V1 is immutable; metadata supports one row per target/key version, strict checkpoints and staged object claims; only declared P03 objects exist and contain no prohibited columns.</acceptance_criteria>
  <done>The expand namespace is valid and physically proven.</done>
</task>
</tasks>

<threat_model>
| Threat ID | STRIDE | Disposition | Mitigation |
| --- | --- | --- | --- |
| P03-11-T1 | Tampering | mitigate | Owner-range claims and immutable V1 checksum |
| P03-11-T2 | Information disclosure | mitigate | Metadata allowlist and prohibited-column scan |
| P03-11-T3 | Denial of service | mitigate | Expand-only DDL and real metadata-lock test |
| P03-11-T4 | Repudiation | mitigate | Flyway/schema identity evidence |
</threat_model>

<verification>Run selector, planning/schema validators and focused real MySQL migration test.</verification>
<success_criteria>V1200 passes with no cross-owner DDL or prohibited metadata.</success_criteria>
<output>Create `03-11-SUMMARY.md` with schema claims, Flyway history and V1 checksum.</output>
