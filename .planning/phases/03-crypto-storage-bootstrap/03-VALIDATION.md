---
phase: 03
slug: crypto-storage-bootstrap
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-31
updated: 2026-09-06
completion_metric: scoped_todo_empty
---

# Phase 03 — Validation Strategy

> Phase 3 的逐任务反馈与最终证据合同。完成只由 `crypto-storage-bootstrap` 权威 TODO 是否为空决定。

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Unit/framework tests** | JUnit 5 + AssertJ + Mockito through Maven Surefire |
| **Database integration** | Existing digest-pinned MySQL 8.4 service harness and Flyway |
| **PKCS#11 integration** | Java 21 SunPKCS11 against an isolated SoftHSM token/config; deterministic adapter is unit evidence only |
| **Object integration** | AWS SDK v2 S3 client against the explicitly prepared, digest-pinned MinIO image |
| **Quick run command** | `mvn -f core/pom.xml test` with the task-specific `-Dtest=...` selector when available |
| **Full suite command** | `mvn -f core/pom.xml test` plus the exact seven-class `phase03-integration` boundary in `.github/workflows/ci.yml`, planning/service/scanner fixtures, cleanup and the three exact Web commands |
| **Completion signal** | All four owner obligations traced, evidence valid, reviews clear, and scoped TODO query empty |

No test process may perform an implicit or moving browser/container/native/service download. CI may explicitly prepare fixed system packages and the repository's immutable platform-digest MySQL/MinIO images before the fail-closed real test starts. Missing real prerequisites remain open TODOs; fixture code rejects absent or mismatched identities, and mocks cannot close a real boundary.

## Sampling Rules

- After every behavior-changing task commit, run its narrow automated test and all directly affected regression tests.
- After each plan wave, run all Phase 3 tests whose prerequisites exist and record any unavailable real-service lane as an open TODO.
- Before phase verification, run the complete Maven suite, real MySQL lane, real SoftHSM lane, real MinIO lane, evidence validators and leak scans.
- Never allow three consecutive implementation tasks without an automated verification command.
- A passing deterministic adapter test does not change the status of the PKCS#11 production-semantics TODO.

## Per-Plan Verification Map

| Plan | Requirement / obligation | Concrete task and secure behavior | Exact automated verification | Real-service boundary / root-registry lane | Final status |
|------|---------------------------|-----------------------------------|------------------------------|--------------------------------------------|----------------|
| 03-01 namespace-and-inventory | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-004 | Select the registered V1200 owner range, prove V1 immutable, and fail closed unless every current protected field and schema-only migratable digest has one reviewed disposition, binding, capacity, writer-fence rule and migration owner. | `python3 skills/flyway-migration/tests/test_next_flyway_version.py && python3 skills/flyway-migration/scripts/next_flyway_version.py --owner crypto-storage-bootstrap --check V1200 && mvn -f core/pom.xml -Pphase01-integration -Dtest=Phase01MySqlIntegrationTest test`<br>`/usr/bin/env ruby .planning/tools/test-phase-03-protected-inventory.rb && /usr/bin/env ruby .planning/tools/validate-phase-03-protected-inventory.rb --manifest core/src/main/resources/security/protected-data-inventory.json --schema core/src/main/resources/db/migration/V1__init_schema.sql --source-root core/src/main/java --acceptance` | Real MySQL namespace/V1 lane; root registry `namespace-inventory`. | verified |
| 03-02 exact-four-evidence-contract | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003, OBL-CRYPTO-STORAGE-004 | Define sanitized, same-subject, exact-four evidence schemas and destructive validators before any result can claim an obligation. | `/usr/bin/env ruby .planning/tools/test-phase-03-crypto-evidence.rb && /usr/bin/env ruby .planning/tools/validate-prd-obligations.rb --owner crypto-storage-bootstrap --assert-unique --assert-traced` | Deterministic validator lane; root registry `evidence-contract`. | sealed-subject verified; current validator fixtures verified |
| 03-03 real-service-fixtures | OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003, OBL-CRYPTO-STORAGE-004 | Admit pinned MySQL/MinIO identities and provision an executable isolated SoftHSM token; source-manifest verification and mocks cannot satisfy the lane. | `/usr/bin/env ruby scripts/lib/phase-03/test_service_checks.rb && scripts/provision-phase-03-softhsm --manifest scripts/lib/phase-03/softhsm-source.json --destination core/target/phase03/softhsm --provision --initialize-token --preflight --cleanup`<br>`mvn -f core/pom.xml -Pphase03-integration -Dtest=Phase03FixturePreflightTest test && /usr/bin/env ruby scripts/lib/phase-03/service_checks.rb assert-clean --all` | Real MySQL + MinIO + SoftHSM fixture/cleanup lane; root registry `fixture-preflight`. | verified |
| 03-04 canonical-envelope | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-003 | Implement byte-exact `YCSE/v1`, canonical context/AAD and strict pre-allocation parse rejection for malformed headers, lengths, versions, domains and overflows. | `mvn -f core/pom.xml -Dtest='EnvelopeCodecTest,ProtectionContextTest' test` | Deterministic crypto-contract lane; root registry `envelope-contract`. | verified |
| 03-05 opaque-key-ports | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-003 | Define one caller-visible opaque wrap operation whose production adapter alone owns durable admission, nonce generation and immutable wrapped result; define distinct object-capability and registration-upload digest purposes whose raw secrets never cross the port; forbid raw key export and unversioned raw hashes. | `mvn -f core/pom.xml -Dtest=VersionedBlindIndexTest test && ! rg -n 'getKey\|keyBytes\|sha256Hex' core/src/main/java/com/ycsopen/sms/core/common/security/key` | Deterministic key/token-digest port lane; root registry `opaque-key-ports`. | verified |
| 03-06 SunPKCS11-adapter | OBL-CRYPTO-STORAGE-003 | Implement the production SunPKCS11 provider/adapter with opaque KEK, mobile-index, object-capability-digest and registration-upload-digest aliases; enforce alias/domain isolation, sanitized failures and no decrement/reset/reuse escape hatch. | `mvn -f core/pom.xml -Dtest='Pkcs11ProviderFactoryTest,SunPkcs11KeyAdapterTest' test && ! rg -n 'getEncoded\(\|FIELD_ENCRYPTION_KEY\|CHANGE_ME_BASE64\|decrementWrap\|releaseWrap\|resetWrap' core/src/main/java/com/ycsopen/sms/core/common/security/key/pkcs11` | Deterministic adapter lane; real protocol proof is owned by 03-07. Root registry `pkcs11-adapter-unit`. | verified |
| 03-07 PKCS11-production-proof | OBL-CRYPTO-STORAGE-003 | Fail closed on production configuration, then prove opaque operations, purpose-separated token-digest aliases and persistent wrap reservations against real SoftHSM/MySQL at rotation, trigger, ceiling, concurrency, failure and restart boundaries. | `mvn -f core/pom.xml -Dtest=CryptoStorageStartupVerifierTest test && ! rg -n 'FIELD_ENCRYPTION_KEY\|key-base64\|CHANGE_ME_BASE64' core/src/main/resources/application.yml`<br>`mvn -f core/pom.xml -Pphase03-integration -Dtest=Phase03Pkcs11IntegrationTest test && /usr/bin/env ruby scripts/lib/phase-03/service_checks.rb assert-clean --service softhsm` | Real SoftHSM + MySQL reservation/token-digest lane; root registry `pkcs11-real`. | verified |
| 03-08 protected-entity-mapping | OBL-CRYPTO-STORAGE-001 | Make every current non-message protected entity accept opaque binary envelopes without truncation, coercion or accidental plaintext mapping. | `mvn -f core/pom.xml -Dtest=ProtectedEntityMappingTest test` | Deterministic persistence-mapping lane; root registry `protected-entity-mapping`. | verified |
| 03-09 message-protection-adapter | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-003 | Establish the sole context-bound message persistence owner, producing canonical envelopes and versioned blind-index metadata without exposing keys. | `mvn -f core/pom.xml -Dtest=MessageTaskProtectionAdapterTest test` | Deterministic message-adapter lane; root registry `message-protection-adapter`. | verified |
| 03-10 message-physical-boundary | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-003 | Remove `HashUtil`, prove raw MySQL contains only bound YCSE and canonical ACTIVE/RETIRING indexes, preserve blacklist/portability parity, and reject swaps/faults. | `mvn -f core/pom.xml -Dtest=BlindIndexLookupServiceTest test && mvn -f core/pom.xml -DskipTests compile && test ! -e core/src/main/java/com/ycsopen/sms/core/common/security/HashUtil.java && ! rg -n '\bHashUtil\b\|\bsha256Hex\s*\(' . --glob '*.{java,kt,groovy}'`<br>`mvn -f core/pom.xml -Pphase03-integration -Dtest=Phase03ProtectedPersistenceIntegrationTest test` | Real MySQL + production PKCS11 protected-message lane; root registry `message-persistence-real`. | verified |
| 03-11 expand-schema | OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003, OBL-CRYPTO-STORAGE-004 | Apply immutable V1 plus Phase-owned V1200 metadata and controlled expand-only V1201 object FIELD-reference reservations; validate exact reservation FK/CHECK semantics and leave V1202 next; no legacy DDL mutation. | `python3 skills/flyway-migration/scripts/next_flyway_version.py --owner crypto-storage-bootstrap --check V1202 && /usr/bin/env ruby .planning/tools/test-planning-validators.rb && mvn -f core/pom.xml -Pphase03-integration -Dtest='Phase01MySqlIntegrationTest,Phase03MigrationIntegrationTest#appliesV1200WithoutChangingV1' test` | Real MySQL/Flyway schema lane; root registry `expand-schema-real`. | verified |
| 03-12 migration-domain-preflight | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-004 | Classify every value explicitly and block all SQL mutation unless target, capacity, context, key and the inseparable writer/snapshot pair are resolved. | `mvn -f core/pom.xml -Dtest='ProtectedDataManifestTest,LegacyValueClassifierTest' test` | Deterministic migration-domain/preflight lane; root registry `migration-preflight-unit`. | verified |
| 03-13 resumable-migration | OBL-CRYPTO-STORAGE-004 | Implement checkpointed, integrity-bound, idempotent migration and a fixed CLI contract that never advances ambiguous/corrupt input. | `mvn -f core/pom.xml -Dtest='ProtectedDataMigrationRunnerTest,ProtectedDataMigrationCommandTest' test` | Deterministic runner/CLI lane; root registry `migration-runner`. | verified |
| 03-14 migration-and-snapshot-recovery | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-004 | Migrate every current target on real services and create a real MySQL dump as bounded encrypted chunks; atomically admit the writer/snapshot manifest pair and stream-authenticate restore into a fresh schema without a plaintext dump file. The local fixed client must execute only signed paths below code-fixed root-owned trust roots, validate all ancestors and an OS-fixed no-ACL proof before HSM/password access, require equal non-root effective/real UIDs from bounded `/usr/bin/id -u` and `/usr/bin/id -ru`, reject malformed/timeout/nonzero helper results, clear inherited process environments, and publish concurrent first completion directories without a false failure. The macOS real-service proof must not whitelist Homebrew: it retains signed production composition, real HSM/JDBC/store/admission behavior and injects only the test-source Docker MySQL-process boundary; fixed-client authority is proven separately against a root-owned system executable, and the test adapter must be absent from the production JAR. | `mvn -f core/pom.xml -Pphase03-integration -Dtest=Phase03MigrationIntegrationTest test && /usr/bin/env ruby .planning/tools/validate-phase-03-protected-inventory.rb --manifest core/src/main/resources/security/protected-data-inventory.json --schema core/src/main/resources/db/migration/V1__init_schema.sql --source-root core/src/main/java --mysql-evidence core/target/phase03/migration-inventory.json --acceptance`<br>`mvn -f core/pom.xml -Dtest='MySqlSnapshotProcessTest,EncryptedMySqlSnapshotServiceTest,ProductionMigrationCommandServicesFactoryTest' test`<br>`mvn -f core/pom.xml -Pphase03-integration -Dtest='Phase03MigrationIntegrationTest#createsAndRestoresEncryptedSnapshotIntoFreshSchema' test && /usr/bin/env ruby scripts/lib/phase-03/service_checks.rb assert-clean --service mysql` | Deterministic euid/ruid/ACL/trusted-root/environment/concurrency/production-composition proof plus real MySQL + PKCS11 chunked-snapshot/fresh-schema recovery lane; root registry `migration-recovery-real`. | verified |
| 03-15 private-object-store | OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003 | Implement a private ciphertext-only S3-compatible port; prohibit public ACLs, presigning and direct storage URLs. | `mvn -f core/pom.xml -Dtest=S3PrivateObjectStoreAdapterTest test && ! rg -n 'Presigner\|presign\|PUBLIC_READ\|public-read\|getUrl' core/src/main/java/com/ycsopen/sms/core/common/security/object` | Deterministic S3-adapter lane; real storage proof is owned by 03-30. Root registry `private-object-adapter`. | verified |
| 03-16 object-capability | OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003 | Issue opaque, expiring, context-bound application capabilities behind deny-by-default authorization; use only the capability PKCS11 digest alias/domain, ACTIVE-only issue and stored-version ACTIVE/RETIRING constant-time verification; reject tamper, replay, cross-binding and registration-token reuse. | `mvn -f core/pom.xml -Dtest=ObjectCapabilityServiceTest test` | Deterministic capability/token-digest lane; root registry `object-capability`. | verified |
| 03-17 registration-object-API | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-002 | Implement the documented staged session/upload/close API: one session-bound token may upload all five purposes and replace one while OPEN; atomic admission caps work at three attempts per purpose and fifteen per session even under races/failure; claim/close/expiry invalidates it; distinct upload-digest alias/domain applies; raw URLs and capability-token reuse are rejected. | `mvn -f core/pom.xml -Dtest=TenantRegistrationObjectApiTest test && rg -n 'registration-object-sessions\|businessLicenseObjectId\|REGISTRATION_UPLOAD_LIMIT_REACHED\|PT24H' core/docs/API.md docs/使用手册.md` | MVC/service/docs/token-digest/admission contract lane; root registry `registration-object-api`. | verified |
| 03-18 redaction-boundary | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003 | Centralize safe log values, redact security failures, and forbid exception/message/token/mobile/ciphertext leakage from owned logging paths. | `mvn -f core/pom.xml -Dtest='SafeLogValueTest,SecurityRedactionConverterTest,GlobalExceptionHandlerLoggingTest' test && ! rg -n 'log\.(trace\|debug\|info\|warn\|error)\([^\n]*(getMessage\|, ex\|token\|password\|mobile\|ciphertext)' core/src/main/java/com/ycsopen/sms/core/common/security core/src/main/java/com/ycsopen/sms/core/common/exception` | Deterministic logging/static-scan lane; root registry `redaction`. | verified |
| 03-19 multi-surface-leak-proof | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003 | Scan generated logs, DB/object metadata, evidence and artifacts with seeded canaries; missing/unreadable surfaces fail closed. | `mvn -f core/pom.xml -Dtest=SensitiveDataLeakScannerTest test && /usr/bin/env ruby .planning/tools/test-scan-phase-03-artifacts.rb && /usr/bin/env ruby .planning/tools/test-phase-03-crypto-evidence.rb`<br>`mvn -f core/pom.xml -Pphase03-integration -Dtest=Phase03LeakScanIntegrationTest test && /usr/bin/env ruby .planning/tools/scan-phase-03-artifacts.rb --phase-dir .planning/phases/03-crypto-storage-bootstrap --generated-root core/target/phase03 --output core/target/phase03/leak-report.json` | Real generated-surface leak lane; root registry `leak-scan-real`. | verified |
| 03-20 key-lifecycle | OBL-CRYPTO-STORAGE-003 | Activate new keys, rewrap DEKs, rotate blind indexes and token-digest aliases, and permit retirement only after a complete reference inventory proves zero live references for that exact purpose/version; never delete token keys automatically. | `mvn -f core/pom.xml -Dtest='KeyLifecycleServiceTest,EnvelopeRewrapServiceTest' test` | Deterministic key-lifecycle/token-reference lane; root registry `key-lifecycle`. | verified |
| 03-21 composed-real-faults | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003, OBL-CRYPTO-STORAGE-004 | Prove rotation, interruption, atomic manifest trust, chunked restore, all object purposes, terminal tokens and reference-gated retirement under composed faults with no fallback/data loss. | `mvn -f core/pom.xml -Pphase03-integration -Dtest='Phase03FixturePreflightTest,Phase03ProtectedPersistenceIntegrationTest,Phase03Pkcs11IntegrationTest,Phase03MigrationIntegrationTest,Phase03ObjectStorageIntegrationTest,Phase03LeakScanIntegrationTest,Phase03RotationRecoveryIntegrationTest' test` plus the zero-skip Surefire report assertion in `.github/workflows/ci.yml` | The seven-suite real MySQL + MinIO + SoftHSM boundary replaces the planned but never implemented monolithic fault class under `03-VERIFICATION.md` deviation `DR-P03-012` and the matching `03-DECISIONS.md` plan override; root lane `real-service-integration`. | current synthetic-merge verified |
| 03-22 root-registry-and-producer | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003, OBL-CRYPTO-STORAGE-004 | Register every deterministic, validator, real, fault, leak and cleanup lane as fixed argv; failure dominates, and evidence is produced only from current same-subject child results. | `/usr/bin/env ruby scripts/lib/phase-03/test_run_checks.rb && ./scripts/verify-phase-03 --all --result-root core/target/phase03/results`<br>`/usr/bin/env ruby .planning/tools/test-phase-03-crypto-evidence.rb && /usr/bin/env ruby .planning/tools/produce-phase-03-crypto-evidence.rb --phase-dir .planning/phases/03-crypto-storage-bootstrap --result-root core/target/phase03/results && /usr/bin/env ruby .planning/tools/validate-phase-03-crypto-evidence.rb --phase-dir .planning/phases/03-crypto-storage-bootstrap --require-owner crypto-storage-bootstrap` | Root registry itself: all unit/validator/MySQL/MinIO/SoftHSM/fault/leak/cleanup lanes. | sealed-subject verified; current registry fixtures verified |
| 03-23 truthful-closure | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003, OBL-CRYPTO-STORAGE-004 | Finalize exact-four sanitized evidence and review handoff only after current validation succeeds and the owner-scoped TODO is empty. | `/usr/bin/env ruby .planning/tools/validate-phase-03-crypto-evidence.rb --phase-dir .planning/phases/03-crypto-storage-bootstrap --require-owner crypto-storage-bootstrap`<br>`/usr/bin/env ruby .planning/tools/validate-phase-03-crypto-evidence.rb --phase-dir .planning/phases/03-crypto-storage-bootstrap --require-owner crypto-storage-bootstrap && /usr/bin/env ruby .planning/tools/scan-phase-03-artifacts.rb --phase-dir .planning/phases/03-crypto-storage-bootstrap --generated-root core/target/phase03 --output core/target/phase03/final-leak-report.json` | Final evidence/leak/review lane consuming root-registry results; root registry `truthful-closure`. | historical sealed closure verified; superseded by Plan 31/CR-12 delivery closure |
| 03-24 protected-field-codec | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-003 | Implement per-value YCSE with codec-owned fresh DEK/data nonce and exactly one opaque wrap call; accept only the adapter-owned reserved wrap nonce/wrapped DEK result and never reserve, supply a wrap nonce or retry. | `mvn -f core/pom.xml -Dtest=ProtectedFieldCodecTest test && test ! -e core/src/main/java/com/ycsopen/sms/core/common/security/FieldEncryptor.java && test ! -e core/src/test/java/com/ycsopen/sms/core/common/security/FieldEncryptorTest.java` | Deterministic canonical-codec lane; the Plan 24 legacy analog was intentionally retired by Plan 26. Root registry `protected-field-codec`. | verified |
| 03-25 current-reader-writer-fence | OBL-CRYPTO-STORAGE-001 | Route every enumerated current non-message protected reader/writer through the codec and preserve the inventory/schema/source correspondence. | `mvn -f core/pom.xml -Dtest=CurrentProtectedReaderFenceTest test && /usr/bin/env ruby .planning/tools/validate-phase-03-protected-inventory.rb --manifest core/src/main/resources/security/protected-data-inventory.json --schema core/src/main/resources/db/migration/V1__init_schema.sql --source-root core/src/main/java` | Deterministic current-path/inventory lane; root registry `current-reader-writer-fence`. | verified |
| 03-26 message-service-rewiring | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-003 | Rewire submission/routing through the sole message protection adapter and statically eliminate direct encryption/raw-hash/setter new-write paths. | `mvn -f core/pom.xml -Dtest='MessageSubmitServiceTest,RoutingEngineTest' test && ! rg -n 'FieldEncryptor\|sha256Hex\|setMobileEncrypted\|setMobileHash' core/src/main/java/com/ycsopen/sms/core/service core/src/main/java/com/ycsopen/sms/core/common/security --glob '!persistence/MessageTaskProtectionAdapter.java' --glob '!HashUtil.java'` | Deterministic service-rewiring/static lane; root registry `message-service-rewiring`. | verified |
| 03-27 atomic-manifest-pair | OBL-CRYPTO-STORAGE-004 | Implement bounded closed writer/snapshot schemas and one production Ed25519 verifier that admits both roles atomically under one sequence/signer/pair digest; concurrent identical tuples are idempotent, competing tuples have one winner, and splice/replay/half-admission loses with zero mutation. | `mvn -f core/pom.xml -Dtest='MigrationPreflightTest,SignedMigrationManifestVerifierTest' test` | Deterministic signed-schema/pair-verifier/concurrent-CAS lane; real pair races are owned by 03-21. Root registry `atomic-manifest-pair`. | verified |
| 03-28 encrypted-object-lifecycle | OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003 | Enforce purpose-specific plaintext/envelope limits before allocation, store only ciphertext, mediate authorized download, and reconcile staged/replaced/orphaned objects. | `mvn -f core/pom.xml -Dtest='ProtectedObjectServiceTest,ProtectedObjectAccessControllerTest' test` | Deterministic object lifecycle/controller lane; root registry `object-lifecycle`. | verified |
| 03-29 registration-persistence | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-002 | Protect every current tenant-registration input and atomically persist tenant data with claimed staged-object IDs; eliminate all legacy URL fields. | `mvn -f core/pom.xml -Dtest='TenantRegistrationProtectionTest,TenantRegistrationObjectApiTest' test && ! rg -n 'businessLicenseUrl\|legalRepIdFrontUrl\|legalRepIdBackUrl\|shortlinkDomainProofUrl\|trademarkProofUrl' core/src/main/java/com/ycsopen/sms/core/web/dto/TenantRegistrationRequest.java core/src/main/java/com/ycsopen/sms/core/service/tenant/TenantService.java` | Deterministic tenant transaction/legacy-URL lane; root registry `registration-persistence`. | verified |
| 03-30 object-registration-real-proof | OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-002, OBL-CRYPTO-STORAGE-003 | Prove private ciphertext objects, application-mediated access, five-purpose session behavior and protected/atomic tenant registration against production adapters and real services. | `mvn -f core/pom.xml -Pphase03-integration -Dtest=Phase03ObjectStorageIntegrationTest test && /usr/bin/env ruby scripts/lib/phase-03/service_checks.rb assert-clean --service minio` | Real MinIO + MySQL + PKCS11 object/registration lane; root registry `object-registration-real`. | verified |

## Revision-Resolved Contract Checks

| Contract | Owning plans | Required automated proof |
| --- | --- | --- |
| Multi-version blind index | 03-01, 03-05, 03-09 through 03-14, 03-20 through 03-22, 03-24 through 03-27 | V1200 metadata constraints; codec/current-path/message rewiring; per-target backfill/checkpoint/cutover/scrub; ACTIVE+RETIRING union; checkpoint-gated raw fallback; zero single-cell dual write; real-MySQL blacklist and portability hit parity before/during/after cutover. |
| Canonical YCSE/v1 contract | 03-04 through 03-07, 03-09 through 03-10, 03-14 through 03-17, 03-21 through 03-22, 03-24 through 03-30 | `ENVELOPE-CONTRACT.md` byte-exact golden vectors; every header/provider/key-reference/domain mutation; protected-field, five-purpose object and snapshot-chunk bounds; declared/actual mismatch, checked arithmetic, oversized lengths and before-allocation rejection. Plan 24 owns the codec, 25 current readers/writers, 26 message wiring, 28 object lifecycle, 29 registration persistence and 30 real object/registration proof. |
| KEK wrap ceiling | 03-05 through 03-07, 03-11, 03-20 through 03-22, 03-24, 03-28, 03-30 | V1200 monotonic reservation; 983040 rotation-required marker; 1048576 hard ceiling; failure consumes a slot; concurrency/restart/near-limit real-MySQL and SoftHSM proof. Every field/object/snapshot producer makes exactly one opaque wrap call; only the production adapter reserves, then generates the nonce, then invokes the provider. |
| Purpose-separated opaque token digests | 03-05 through 03-07, 03-11, 03-16 through 03-17, 03-20 through 03-22, 03-28 through 03-30 | `OpaqueTokenDigestPort` accepts only `OBJECT_CAPABILITY` or `REGISTRATION_UPLOAD`; each uses a distinct nonextractable versioned PKCS11 HMAC alias/domain and canonical binding. Only ACTIVE issues; verification addresses the stored ACTIVE/RETIRING version and compares 32 bytes in constant time. Cross-domain/binding, unknown/RETIRED/REVOKED versions and mobile `BlindIndexPort` reuse fail. Retirement requires zero live capabilities or OPEN sessions for the exact purpose/version; real SoftHSM/MySQL composition proves alias isolation and reference gating. |
| Signed migration preflight and atomic pair | 03-11 through 03-14, 03-21 through 03-22, 03-27 | Plan 27 owns bounded machine-readable writer/snapshot schemas and the production atomic pair verifier; ACTIVE/RETIRING pinned Ed25519 anchors, pair digest, global replay state, rollout/revocation/unknown-retired rejection and real-MySQL zero-mutation cases are mandatory. |
| Chunked encrypted snapshot recovery | 03-04, 03-07, 03-11 through 03-14, 03-21 through 03-22, 03-24, 03-27 | Plan 14 must bounded-stream the actual pinned-MySQL dump into ordered `mysql-encrypted-snapshot-chunk` envelopes, retain no plaintext dump, bind each chunk and checked totals in Plan 27's snapshot manifest, atomically admit the writer/snapshot pair, then authenticate and stream restore into a fresh schema. Manifest inspection alone cannot pass. |
| Staged tenant evidence API | 03-05 through 03-07, 03-11, 03-15 through 03-17, 03-20 through 03-22, 03-28 through 03-30 | Session/upload/close/register tests; one session-bound token sequentially uploads five purposes and replaces one purpose only inside its OPEN tenant-draft/session; an atomic pre-work reservation caps admitted attempts at three per purpose/fifteen per session, burns post-reservation failures and rejects concurrent overrun; claim/close/expiry/cross-binding rejection; purpose-specific digest alias/domain and zero-live-reference retirement; purpose plaintext/envelope bounds; Plan 28 lifecycle, Plan 29 atomic registration persistence, explicit legacy-URL rejection/docs parity and Plan 30 real MySQL/MinIO/PKCS11 proof. |
| Protected no-index targets | 03-01, 03-08, 03-11 through 03-14, 03-19, 03-21 through 03-22, 03-25 | `bulk_sending_items.mobile_encrypted` and `uplink_records.mobile_encrypted` remain explicit protected/migration rows, carry a documented no-equality-index disposition, pass current-reader/writer fencing, and cannot disappear from inventory/leak/evidence unions. |
| Schema-only migratable digest | 03-01, 03-11 through 03-14, 03-19, 03-21 through 03-22 | `third_party_risk_check_logs.mobile_hash` remains in the exact-five migration/scrub union because V1 schema/index and historical rows exist; inventory labels it `MIGRATABLE_SCHEMA_ONLY`, does not invent a Java reader/writer, and blocks cutover unless the deployed-writer fence proves no unknown external writer. |
| Current production-path ownership | 03-08 through 03-10, 03-24 through 03-26, 03-29 through 03-30 | Plan 24 supplies the only codec; Plan 25 fences every current non-message reader/writer; Plan 26 rewires message submission and removes direct-key/raw-hash new writes; Plan 29 persists protected registration values/object claims atomically; Plan 30 proves that composition on real services. |
| Project construction patterns | 03-04 through 03-30 | Every applicable task reads `03-PATTERNS.md`, names the nearest repository analog or exact research contract when none exists, and applies constructor injection, transaction boundaries, safe errors/logging and the smallest truthful test layer. |

## Wave 0 Requirements

- [x] **03-01:** Owner-range selection, immutable-V1 verification and the canonical protected-data inventory/schema validator are implemented and covered by Flyway/inventory fixtures.
- [x] **03-02:** Exact-four obligation evidence schemas, producer/validator and destructive fixtures are present; their sealed output is historical subject evidence, not a reason to track generated `core/target` files.
- [x] **03-03:** Exact-identity MySQL/MinIO/SoftHSM profiles, executable SoftHSM provisioning and cleanup are proven; CI prepares only the locked service images before the fail-closed fixture starts.
- [x] **03-04 / 03-05:** Canonical envelope, AAD/domain, allocation, opaque-key, blind-index, purpose-separated token-digest and wrap-admission contracts were fixed before their consumers.
- [x] **03-06 / 03-07:** Distinct nonextractable PKCS11 HMAC families and real SoftHSM rotation/cross-domain rejection are covered by unit and real-service suites.
- [x] **03-11:** V1200/V1201 expand-only tables and constraints cover reservations, versioned indexes/checkpoints, atomic manifest-pair admission and staged object/session lifecycle without changing V1.
- [x] **03-24:** The canonical protected-field/snapshot-chunk codec is implemented against the frozen envelope and opaque-key contracts.
- [x] **03-25:** The accepted inventory enumerates and fences every current non-message protected-data reader/writer.
- [x] **03-26:** Current message submission/routing uses the sole protection adapter and static checks reject direct encryption/raw-hash new-write paths.
- [x] **03-27:** Closed bounded writer/snapshot schemas and the atomic pair verifier have destructive splice/replay/partial/concurrency coverage.
- [x] **03-14 / 03-27:** Real fixed-argument MySQL dump output is chunk-encrypted without a plaintext dump file, atomically admitted and authenticated into a fresh-schema integrity comparison.
- [x] **03-28:** Bounded encrypted-object lifecycle, before-allocation limits, mediated access and reconciliation are implemented and verified.
- [x] **03-29:** Tenant registration accepts object IDs and atomically persists protected values with staged-object claims; legacy URL inputs are absent.
- [x] **03-30:** Production adapters execute the complete object/registration path against real MinIO, MySQL and PKCS11 boundaries.
- [x] **03-22:** The fixed root registry and destructive registry tests cover every deterministic, real-service, snapshot, cleanup and evidence lane; the final CI acceptance additionally executes the explicit seven-suite boundary directly.

## Final Acceptance Evidence

- Corrective code head `a7039ed245bb33167a9e72e39fefcc8c0daea89e` was tested through pull-request synthetic merge `f841497bf3635316c2cd58c86f9c296e5756ab5f` in GitHub Actions run `34010664879`.
- All five jobs passed. The real job executed the seven named suites as eight tests with zero failures, errors or skips, then passed owned-service cleanup and report upload.
- The real migration suite contributed two of those eight tests, including `appliesV1200WithoutChangingV1` and encrypted fresh-schema snapshot recovery.
- Default backend verification passed 387 tests with zero failures/errors and 17 explicitly profile-gated skips; the exact Web install/test/build commands passed.
- Plan 31 aggregate independent and Claude reviews both reported `BLOCKER 0 / HIGH 0`; no `core/target/**` path is tracked.
- The older exact-four/root-registry evidence remains evidence for its sealed subject. Plan 31 corrections are accepted by the current direct suites, reviews and synthetic-merge CI, without regenerating or committing run output.

## Real-Service Evidence Boundary

| Boundary | Required proof | What does not count |
|----------|----------------|---------------------|
| MySQL/Flyway | Digest-pinned MySQL applies V1 and the registered V1200+ set; raw-row, checkpoint, restart and integrity assertions pass | H2, SQL parsing, or migration filename inspection alone |
| PKCS#11/HSM semantics | SunPKCS11 loads an isolated SoftHSM token; keys are opaque; wrap/unwrap, HMAC, alias rotation and failure cases pass | Mockito or deterministic in-memory adapter |
| Private S3 semantics | AWS S3 client talks to digest-pinned MinIO; anonymous access fails and stored bytes contain no plaintext canary | In-memory maps or mocked S3 client |
| Production deployment | Deployment configuration identifies its actual KMS/HSM product, key aliases, access policy and private object-store policy | SoftHSM being described as a certified physical HSM |

## Manual-Only Verifications

No Phase 3 behavior is accepted as manual-only. Production hardware certification and deployment IAM policy are deployment evidence, not substitutes for automated code/adapter conformance tests and not claims made by this phase.

## Validation Sign-Off

- [x] Every implementation task has a narrow automated verification command or an explicit resolved Wave 0 prerequisite.
- [x] Sampling continuity records focused checks during implementation and full affected/default/real boundaries at integration points.
- [x] Every Wave 0 prerequisite is resolved with repository and executable evidence.
- [x] No watch-mode, moving download or test-owned implicit service fetch exists; CI prerequisite preparation is explicit and immutable where applicable.
- [x] Real MySQL, SoftHSM and MinIO results remain separately labeled from deterministic unit evidence.
- [x] All four obligation traces and sealed evidence targets pass integrity/traceability validators; Plan 31 corrections have separate current-head proof.
- [x] Independent goal/code review and Claude review contain no unresolved BLOCKER/HIGH finding for the implementation head.
- [x] Independent and Claude CR-12 reviews of this corrected contract contain no unresolved BLOCKER/HIGH finding — independent final review `0/0/0/0`; Claude session `d8e35bd0-f73a-437f-87c8-35fc857d88cf` `0/0/0/0`.
- [x] The pushed contract-correction head passes the complete pull-request CI boundary — head `2a320e4f374dd1bf741dcc7724130b40f130f8dd`, synthetic merge `fcd93e3c926d67bfb2627f65981e7fc96649d638`, run `34012096119`, five jobs PASS.

**Approval:** complete — the active contract, external reviews and contract-head CI are all satisfied; CR-12 closes in the separate status-only record commit.
