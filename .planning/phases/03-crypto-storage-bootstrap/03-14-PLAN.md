---
phase: 03-crypto-storage-bootstrap
plan: "14"
type: execute
wave: 10
depends_on: [03-03, 03-07, 03-10, 03-13, 03-17, 03-20, 03-29, 03-30]
files_modified:
  - core/src/test/java/com/ycsopen/sms/core/verification/Phase03MigrationIntegrationTest.java
  - core/src/test/java/com/ycsopen/sms/core/verification/Phase03EncryptedSnapshotHarness.java
  - core/src/main/resources/security/protected-data-inventory.json
autonomous: true
requirements: [REQ-NFR-DATA-PROTECTION, OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-004]
must_haves:
  truths:
    - "Real MySQL migrates every current migratable target or retains a blocking TODO; no current/migratable target is deferred."
    - "Interruption, duplicate workers, drift and recovery preserve rows/checkpoints and V1 immutability."
    - "Every rejected signed-preflight branch exits before any UPDATE, event or checkpoint, and all five blind-index targets reach HMAC-only COMPLETE with query parity."
    - "A real pinned-MySQL dump is bounded-streamed through ordered canonical snapshot-chunk envelopes, contains no seeded plaintext, restores by bounded streaming into a fresh disposable schema, and preserves row/envelope/blind-index/checkpoint integrity."
  artifacts:
    - path: core/src/test/java/com/ycsopen/sms/core/verification/Phase03MigrationIntegrationTest.java
      provides: "Real mixed-state migration/recovery evidence"
  key_links:
    - from: core/src/test/java/com/ycsopen/sms/core/verification/Phase03MigrationIntegrationTest.java
      to: core/src/main/resources/security/protected-data-inventory.json
      via: "Exercise every migratable reviewed target"
      pattern: "DEFERRED_OWNER"
    - from: core/src/test/java/com/ycsopen/sms/core/verification/Phase03MigrationIntegrationTest.java
      to: core/src/main/java/com/ycsopen/sms/core/common/security/migration/ProtectedDataMigrationCommand.java
      via: "Real command exit matrix and JDBC zero-update assertion"
      pattern: "preflight"
---

<objective>Prove complete in-place migration and recovery against real MySQL and PKCS11.</objective>
<execution_context>@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@AGENTS.md
@.planning/phases/03-crypto-storage-bootstrap/03-DECISIONS.md
@.planning/phases/03-crypto-storage-bootstrap/ENVELOPE-CONTRACT.md
@.planning/phases/03-crypto-storage-bootstrap/03-PATTERNS.md</execution_context>

## Artifacts this phase produces

- Real mixed-state, interruption, concurrency, restart, integrity, capacity and encrypted-snapshot recovery evidence.

<tasks>
<task type="auto">
  <name>Task 1: Execute complete real-MySQL migration and writer-fence proof</name>
  <files>core/src/test/java/com/ycsopen/sms/core/verification/Phase03MigrationIntegrationTest.java, core/src/main/resources/security/protected-data-inventory.json</files>
  <read_first>
    - core/src/main/java/com/ycsopen/sms/core/common/security/migration/ProtectedDataMigrationRunner.java
    - core/src/main/resources/security/protected-data-inventory.json
    - core/src/main/resources/db/migration/V1__init_schema.sql
    - .planning/phases/03-crypto-storage-bootstrap/03-PATTERNS.md
  </read_first>
  <action>Follow the real-service lifecycle and transaction assertions from `Phase01MySqlIntegrationTest` via `03-PATTERNS.md`. Invoke the real `phase03-migration preflight` command, not injected ports. For absent, empty, schema-invalid, unknown/stale/retired/revoked signer, future, replayed-across-rotation, forged, fingerprint-mismatched, shared migration-set/environment/schema/Flyway/database mismatch, cross-pair splice, separate replay, same-sequence digest change or simulated half-admission, assert the fixed nonzero exit code and pair-state/JDBC update/event/checkpoint counts remain zero. Prove the exact same accepted pair is idempotent and a higher sequence advances only as one ACTIVE-signer pair. Then create synthetic mixed rows for every currently migratable protected-field target, explicitly including no-index `bulk_sending_items.mobile_encrypted` and `uplink_records.mobile_encrypted`, plus the exact five blind-index targets from DR-P03-007. Cover approved plaintext, valid envelope, malformed magic, invalid encoding, null, 110-byte boundary, over-capacity and concurrent mutation. Use production PKCS11. Prove no-index targets receive canonical field envelopes without invented index rows. Prove each indexed target's idempotent BACKFILLED/VERIFIED/CUTOVER/SCRUBBED/COMPLETE sequence, one metadata row per ACTIVE/RETIRING version, binding/orphan reconciliation, legacy fallback only before COMPLETE, scrub and safe resume. On real MySQL, assert blacklist precedence and portability hits before backfill, during union compatibility and after HMAC-only cutover. Require the exact atomic pair digest, zero ambiguous/current plaintext/raw-SHA after successful scrub, and unchanged V1. A capacity, stale writer, missing object source or unresolved target remains a blocking TODO and prevents evidence; it is not converted to DEFERRED_OWNER.</action>
  <verify><automated>mvn -f core/pom.xml -Pphase03-integration -Dtest=Phase03MigrationIntegrationTest test &amp;&amp; /usr/bin/env ruby .planning/tools/validate-phase-03-protected-inventory.rb --manifest core/src/main/resources/security/protected-data-inventory.json --schema core/src/main/resources/db/migration/V1__init_schema.sql --source-root core/src/main/java --mysql-evidence core/target/phase03/migration-inventory.json --acceptance</automated></verify>
  <acceptance_criteria>All resolved fields and index targets migrate/restart safely; signed-preflight rejection mutates zero rows; blacklist/portability hits remain equivalent through cutover; over-capacity/stale/unresolved cases block; output is counts/digests only.</acceptance_criteria>
  <done>Real migration is complete and truthful or leaves exact blocking TODOs.</done>
</task>

<task type="auto">
  <name>Task 2: Create and restore a real encrypted MySQL snapshot</name>
  <files>core/src/test/java/com/ycsopen/sms/core/verification/Phase03EncryptedSnapshotHarness.java, core/src/test/java/com/ycsopen/sms/core/verification/Phase03MigrationIntegrationTest.java</files>
  <read_first>
    - core/src/test/java/com/ycsopen/sms/core/verification/Phase03ServiceHarness.java
    - core/src/main/java/com/ycsopen/sms/core/common/security/migration/SignedMigrationManifestVerifier.java
    - core/src/main/java/com/ycsopen/sms/core/common/security/persistence/ProtectedFieldCodec.java
    - .planning/phases/03-crypto-storage-bootstrap/ENVELOPE-CONTRACT.md
  </read_first>
  <action>Extend the pinned MySQL harness with an actual production-shaped snapshot drill; manifest verification by itself cannot satisfy this task. Seed a unique plaintext canary plus migrated envelopes, separate blind-index versions and migration checkpoints in a source disposable schema. Generate canonical `migration_set_id` and `snapshot_id`, then stream the exact MySQL image's fixed-argument dump stdout into `ENVELOPE-CONTRACT.md`'s `mysql-encrypted-snapshot-chunk` producer. Retain at most one 10,485,760-byte plaintext chunk plus one-byte lookahead, use production SunPKCS11 with fresh per-chunk DEK/data/wrap nonces, bind migration/snapshot/index/final identity in AAD, and persist each complete envelope before advancing. Enforce the 1 TiB plaintext, 104858-chunk, 1,099,526,832,186-byte ciphertext and 33,554,432-byte manifest hard bounds with checked arithmetic; never create a plaintext dump file or whole-dump buffer. Assert every stored chunk excludes the canary. Build both schema-valid manifests with the same migration set, subject, global sequence and signer version; the snapshot manifest binds snapshot ID, recovery key, ordered chunk count, every index/final/plaintext-size/envelope-size/digest and checked totals. Generate both role-separated signatures over one pair digest and atomically admit the pair. Restore only after a complete inventory pass, fetching/authenticating one exact bounded chunk at a time and streaming plaintext directly into the actual MySQL client for a newly created empty schema with a distinct name. Assert restored business-row counts/digests, strict envelope parse/decrypt, ACTIVE/RETIRING blind-index rows, target checkpoints and Flyway subject all equal the source. Test multi-chunk input, exact per-chunk and total boundaries using a bounded synthetic stream, and reject missing/duplicate/reordered/truncated/extra/post-final/wrong-context/digest-size-total-mismatched chunks, cross-pair manifests, tampered key and partial restore without pair admission or recovery completion. Clean source, restored schema and encrypted run chunks.</action>
  <verify><automated>mvn -f core/pom.xml -Pphase03-integration -Dtest='Phase03MigrationIntegrationTest#createsAndRestoresEncryptedSnapshotIntoFreshSchema' test &amp;&amp; /usr/bin/env ruby scripts/lib/phase-03/service_checks.rb assert-clean --service mysql</automated></verify>
  <acceptance_criteria>Every encrypted chunk excludes the canary; ordered multi-chunk bounded streaming restores a distinct fresh schema with matching row/envelope/index/checkpoint integrity; per-chunk/total/pair faults cannot admit or claim recovery; no whole-dump buffer/plaintext dump exists; cleanup passes.</acceptance_criteria>
  <done>Recovery is proven by real encrypted snapshot creation and restore rather than manifest inspection.</done>
</task>
</tasks>

<threat_model>
| Threat ID | STRIDE | Disposition | Mitigation |
| --- | --- | --- | --- |
| P03-14-T1 | Tampering | mitigate | Mixed-row and duplicate-worker real tests |
| P03-14-T2 | Repudiation | mitigate | Exact target/writer/manifest evidence |
| P03-14-T3 | Information disclosure | mitigate | No row values in output |
| P03-14-T4 | Denial of service | mitigate | Bounded snapshot chunks, hard totals, restart and streamed fresh-schema restore |
</threat_model>

<verification>Run real migration/inventory acceptance and exact cleanup.</verification>
<success_criteria>No current/migratable target is deferred; successful runs have zero plaintext/ambiguity and stable recovery.</success_criteria>
<output>Create `03-14-SUMMARY.md` with per-target counts, writer identities, capacity results and blocking TODOs.</output>
