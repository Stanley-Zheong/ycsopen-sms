---
phase: 03-crypto-storage-bootstrap
plan: "24"
type: execute
wave: 3
depends_on: [03-05]
files_modified:
  - core/src/main/java/com/ycsopen/sms/core/common/security/persistence/ProtectedFieldCodec.java
  - core/src/test/java/com/ycsopen/sms/core/common/security/persistence/ProtectedFieldCodecTest.java
  - core/src/test/java/com/ycsopen/sms/core/common/security/FieldEncryptorTest.java
autonomous: true
requirements: [REQ-NFR-DATA-PROTECTION, OBL-CRYPTO-STORAGE-001, OBL-CRYPTO-STORAGE-003]
must_haves:
  truths:
    - "Each protected value uses a codec-owned fresh DEK/data nonce and one adapter-owned durably reserved wrap nonce returned through the opaque key port."
    - "Strict unprotect authenticates canonical header/context/domain and exposes one sanitized failure category."
  artifacts:
    - path: core/src/main/java/com/ycsopen/sms/core/common/security/persistence/ProtectedFieldCodec.java
      provides: "Per-value YCSE protection owner"
  key_links:
    - from: core/src/main/java/com/ycsopen/sms/core/common/security/persistence/ProtectedFieldCodec.java
      to: core/src/main/java/com/ycsopen/sms/core/common/security/key/KeyProtectionPort.java
      via: "Wrap/unwrap per-value DEK under exact AAD"
      pattern: "wrap|unwrap"
---

<objective>Implement per-value YCSE protection over the single opaque wrapping operation.</objective>
<execution_context>@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@AGENTS.md
@.planning/phases/03-crypto-storage-bootstrap/ENVELOPE-CONTRACT.md
@.planning/phases/03-crypto-storage-bootstrap/03-PATTERNS.md</execution_context>

## Artifacts this phase produces

- Protected-field codec and regression vectors replacing the legacy direct-key boundary.

<tasks>
<task type="auto">
  <name>Task 1: Implement per-value envelope protection</name>
  <files>core/src/main/java/com/ycsopen/sms/core/common/security/persistence/ProtectedFieldCodec.java, core/src/test/java/com/ycsopen/sms/core/common/security/persistence/ProtectedFieldCodecTest.java, core/src/test/java/com/ycsopen/sms/core/common/security/FieldEncryptorTest.java</files>
  <read_first>
    - core/src/main/java/com/ycsopen/sms/core/common/security/FieldEncryptor.java
    - core/src/main/java/com/ycsopen/sms/core/common/security/envelope/EnvelopeCodec.java
    - core/src/main/java/com/ycsopen/sms/core/common/security/key/KeyProtectionPort.java
    - .planning/phases/03-crypto-storage-bootstrap/ENVELOPE-CONTRACT.md
    - .planning/phases/03-crypto-storage-bootstrap/03-PATTERNS.md
  </read_first>
  <action>Follow the `FieldEncryptor`/`FieldEncryptorTest` primitive and test analogs while rejecting their direct-key/unversioned-payload boundary. Implement only `ENVELOPE-CONTRACT.md`: generate a fresh 32-byte DEK and 12-byte data nonce, encrypt under the authenticated canonical header/context and data domain, then call `KeyProtectionPort.wrap(dek, authenticatedHeader, semanticContext)` exactly once. The codec never invokes `WrapOperationAdmissionPort`, never generates or supplies a wrap nonce, and never retries a failed wrap call; the adapter atomically reserves, generates the wrap nonce, invokes the provider and returns canonical key reference/wrap nonce/wrapped DEK in `WrappedDataKey`. Serialize that returned result under the distinct wrap domain and zero transient buffers where supported. Enforce the selected database, registration-object or snapshot-chunk plaintext and complete-envelope bound before encryption; snapshot orchestration remains Plan 14 and callers must supply its exact canonical chunk context. Strict unprotect reconstructs the exact domains and collapses wrong key/header/context/tag/tamper into one sanitized failure. Use constructor-injected final collaborators and port useful legacy vectors without retaining direct-key production ownership.</action>
  <verify><automated>mvn -f core/pom.xml -Dtest='ProtectedFieldCodecTest,FieldEncryptorTest' test</automated></verify>
  <acceptance_criteria>Fresh envelopes differ, exact-context round trips pass, each codec protect makes one opaque wrap call and accepts only its returned nonce/ciphertext, adapter admission precedes nonce use, and mutation/outage/capacity cases fail without retry, fallback or leakage.</acceptance_criteria>
  <done>Protected values cross one per-value envelope codec and opaque key port.</done>
</task>
</tasks>

<threat_model>
| Threat ID | STRIDE | Disposition | Mitigation |
| --- | --- | --- | --- |
| P03-24-T1 | Information disclosure | mitigate | Fresh DEK/nonces and opaque key port |
| P03-24-T2 | Tampering | mitigate | Exact header/context/domain authentication |
| P03-24-T3 | Denial of service | mitigate | Canonical selected-purpose capacity bounds |
</threat_model>

<verification>Run codec and legacy-regression suites plus direct-key/plaintext-fallback scans.</verification>
<success_criteria>Per-value protection follows the canonical contract with no direct-key or plaintext fallback.</success_criteria>
<output>Create `03-24-SUMMARY.md` with vector, nonce, bound and failure-category results.</output>
