---
phase: 03-crypto-storage-bootstrap
plan: "27"
type: execute
wave: 7
depends_on: [03-12]
files_modified:
  - core/src/main/java/com/ycsopen/sms/core/common/security/migration/SignedMigrationManifestVerifier.java
  - core/src/main/java/com/ycsopen/sms/core/common/security/migration/MigrationPreflightProperties.java
  - core/src/main/resources/security/migration/ycs-writer-fence-v1.schema.json
  - core/src/main/resources/security/migration/ycs-encrypted-snapshot-v1.schema.json
  - core/src/test/java/com/ycsopen/sms/core/common/security/migration/MigrationPreflightTest.java
  - core/src/test/java/com/ycsopen/sms/core/common/security/migration/SignedMigrationManifestVerifierTest.java
autonomous: true
requirements: [REQ-NFR-DATA-PROTECTION, OBL-CRYPTO-STORAGE-004]
must_haves:
  truths:
    - "Writer and encrypted-snapshot manifests are admitted only as one same-subject, same-sequence, same-signer pair."
    - "Role-separated Ed25519 signatures commit both individual manifest digests to one canonical pair digest."
    - "Cross-pair splice, separate replay, same-sequence change, signer rotation ambiguity and half-admission mutate no pair state or migration collaborator."
  artifacts:
    - path: core/src/main/java/com/ycsopen/sms/core/common/security/migration/SignedMigrationManifestVerifier.java
      provides: "Production atomic writer/snapshot pair verifier"
    - path: core/src/main/resources/security/migration/ycs-encrypted-snapshot-v1.schema.json
      provides: "Bounded ordered snapshot-chunk inventory schema"
  key_links:
    - from: core/src/main/java/com/ycsopen/sms/core/common/security/migration/SignedMigrationManifestVerifier.java
      to: core/src/main/resources/security/migration/ycs-writer-fence-v1.schema.json
      via: "Validate writer role before pair digest/signature admission"
      pattern: "ycs-writer-fence/v1"
    - from: core/src/main/java/com/ycsopen/sms/core/common/security/migration/SignedMigrationManifestVerifier.java
      to: core/src/main/resources/security/migration/ycs-encrypted-snapshot-v1.schema.json
      via: "Validate ordered snapshot chunks before atomic pair admission"
      pattern: "ycs-encrypted-snapshot/v1"
---

<objective>Implement the production signed writer/snapshot pair verifier and bounded machine schemas.</objective>
<execution_context>@/Users/laosanzheong/.codex/gsd-core/workflows/execute-plan.md
@AGENTS.md
@.planning/phases/03-crypto-storage-bootstrap/03-DECISIONS.md
@.planning/phases/03-crypto-storage-bootstrap/ENVELOPE-CONTRACT.md
@.planning/phases/03-crypto-storage-bootstrap/03-PATTERNS.md</execution_context>

## Artifacts this phase produces

- Closed writer/snapshot JSON schemas, versioned trust configuration, canonical pair verifier and destructive zero-mutation tests.

<tasks>
<task type="auto">
  <name>Task 1: Implement inseparable signed migration-pair admission</name>
  <files>core/src/main/java/com/ycsopen/sms/core/common/security/migration/SignedMigrationManifestVerifier.java, core/src/main/java/com/ycsopen/sms/core/common/security/migration/MigrationPreflightProperties.java, core/src/main/resources/security/migration/ycs-writer-fence-v1.schema.json, core/src/main/resources/security/migration/ycs-encrypted-snapshot-v1.schema.json, core/src/test/java/com/ycsopen/sms/core/common/security/migration/MigrationPreflightTest.java, core/src/test/java/com/ycsopen/sms/core/common/security/migration/SignedMigrationManifestVerifierTest.java</files>
  <read_first>
    - core/src/main/java/com/ycsopen/sms/core/common/security/migration/WriterFencePort.java
    - core/src/main/java/com/ycsopen/sms/core/common/security/migration/EncryptedSnapshotVerifier.java
    - core/src/main/resources/db/migration/V1200__create_crypto_storage_metadata.sql
    - .planning/phases/03-crypto-storage-bootstrap/03-DECISIONS.md
    - .planning/phases/03-crypto-storage-bootstrap/ENVELOPE-CONTRACT.md
    - .planning/phases/03-crypto-storage-bootstrap/03-PATTERNS.md
  </read_first>
  <action>No repository analog exists, so implement DR-P03-008 exactly with constructor-injected final collaborators and sanitized stable failures. Define closed schemas for `ycs-writer-fence/v1` and `ycs-encrypted-snapshot/v1`; both require identical canonical `migration_set_id`, environment, database-instance fingerprint, schema, Flyway-set digest, unsigned global sequence and `signer_key_version`, with bounded strings and no unknown fields. The writer role owns issued/expiry markers and unique compatible writer records. The snapshot role owns canonical snapshot ID, recovery key reference, completed marker, totals and an ordered array of at most 104858 chunk records, each containing zero-based index, exactly one terminal final flag, plaintext size at most 10485760, envelope size at most 10485905 and SHA-256 digest; enforce the 1 TiB plaintext, 1099526832186-byte ciphertext and 33554432-byte manifest bounds from `ENVELOPE-CONTRACT.md`. Read only bounded canonical regular files at explicit canonical paths and reject symlinks, duplicates and noncanonical JSON. Compute each canonical role digest and the exact DR-P03-008 pair digest. Verify the writer role signature over role byte `0x01` and the snapshot role signature over `0x02`, both committing the same pair digest and their role digest under the same configured signer version. Configure exactly one ACTIVE and explicit RETIRING Ed25519 X.509 fingerprints. Only ACTIVE may atomically CAS a higher global sequence with both role digests and pair digest; RETIRING may reverify only the exact accepted tuple. Test missing/duplicate/reordered/truncated/extra/post-final/size-total mismatch chunks, migration-set/subject/sequence/signer mismatch, cross-pair splice, individual replay, same-sequence digest change, simulated half-write, unknown/retired/revoked signer, rollout, old-anchor removal, compromise invalidation and recovery through one fresh higher-sequence pair. Every rejection asserts zero pair-state, lease, checkpoint, event and business-table mutations.</action>
  <verify><automated>mvn -f core/pom.xml -Dtest='MigrationPreflightTest,SignedMigrationManifestVerifierTest' test</automated></verify>
  <acceptance_criteria>Only two schema-valid manifests with exact shared subject, role-separated pair signatures, ordered bounded chunks and one atomic CAS pass; every split/replay/rotation/chunk fault is fail-closed and zero-mutation.</acceptance_criteria>
  <done>Migration preflight has one production atomic pair trust source rather than independently accepted manifest roles.</done>
</task>
</tasks>

<threat_model>
| Threat ID | STRIDE | Disposition | Mitigation |
| --- | --- | --- | --- |
| P03-27-T1 | Tampering | mitigate | Canonical role and pair digests with role-separated signatures |
| P03-27-T2 | Spoofing | mitigate | One ACTIVE signer version and pinned fingerprints |
| P03-27-T3 | Repudiation | mitigate | Atomic persisted pair tuple and global sequence |
| P03-27-T4 | Denial of service | mitigate | Bounded files, chunk arrays, sizes and checked totals |
</threat_model>

<verification>Run paired-schema/signature/trust-rotation/chunk-inventory tests and assert every rejection leaves all mutable collaborators untouched.</verification>
<success_criteria>Production migration admission cannot splice, separately replay or half-commit writer and snapshot manifests.</success_criteria>
<output>Create `03-27-SUMMARY.md` with schema bounds, signature vectors, pair-CAS matrix and zero-mutation results.</output>
