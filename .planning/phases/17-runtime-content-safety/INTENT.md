# Intent

Close the real bypass described in the PRD review: a user may inject risky text only through template variables after the template itself has passed resource review. Phase 17 therefore scans the actual final text used for sending, not the template pattern.
