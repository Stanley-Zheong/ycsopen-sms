#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

exec /usr/bin/env ruby "$REPO_ROOT/scripts/lib/phase-01/run_checks.rb" "$@"
